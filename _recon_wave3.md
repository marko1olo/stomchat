# Разведка волны 3: проверка чисел волны 2 и ответ на пункты 3-8

Лана: РАЗВЕДКА. **Ни одного `.py` не изменено и не создано в репозитории.** Все
скрипты замера лежат вне репозитория, в `C:\temp\recon_wave3\`. Боевые базы
открывались только `mode=ro` (в этом отчёте они вообще почти не нужны — работа по
дереву кода).

Путь: `C:\Users\Admin\Desktop\_Organized\08_Projects\stomchat` (сменился посреди
предыдущей волны; в старых отчётах встречается `C:\Users\Admin\Desktop\stomchat`).

Замеры: 2026-07-29, 12:00-12:40. **Дерево двигалось под другими агентами во время
замера**: `gemini_client.py` и `main.py` перезаписаны в 12:10-12:11, `assistant.py`
в 11:01. Каждое число ниже привязано ко времени и к mtime файла.

---

## 0. ПОПРАВКА К ЗАДАНИЮ: `_recon_wave2.md` отвечает не на 2 пункта, а на все 8

Задание волны 3 говорит: «_recon_wave2.md отвечает только на пункты 1-2 из 8».
Это неверно на момент чтения. Файл (87 348 Б, mtime **10:35**) содержит разделы
1-10, включая:

| пункт задания | раздел в `_recon_wave2.md` | строки |
|---|---|---|
| 3 немые пути | «3. НЕМЫЕ ПУТИ» | 186-303 |
| 4 TODO/FIXME | «4. TODO / FIXME / HACK / XXX — их НОЛЬ» | 307-345 |
| 5 дубли | «5. ДУБЛИРОВАНИЕ» + 5.3 «другие дубли» | 349-436 |
| 6 бюджеты | «6. ВЛОЖЕННОСТЬ БЮДЖЕТОВ» | 440-564 |
| 7 тесты, которых нет | «7. ТЕСТОВ НЕТ» | 568-644 |
| 8 отложенное из прежних отчётов | «8. ЧТО ИЗ ПРЕЖНИХ ОТЧЁТОВ...» | 648-786 |
| — | «9. САБОТАЖ» (8 диверсий в песочнице) | 790-886 |
| — | «10. СВОДКА ДЛЯ ЛИДА» | 889-920 |

Скорее всего лид составлял задание по срезу файла на ~09:30, когда там были только
пункты 1-2, а разведчик волны 2 дописал остальное к 10:35 и не умер.

**Второе: поправку про индекс волна 2 сделала сама.** Задание говорит «одно
утверждение опровергнуто трижды: `idx_cat` существует». Ровно это написано в
`_recon_wave2.md` §8.4 (строки 754-786): «Пункт 2 этого отчёта утверждал ... **Это
было неверно**», с выводом `objects: ['distilled_facts','sqlite_sequence','idx_cat']`
и заменой находки на точную («индекс есть, но `LIKE '%код%'` его не использует:
SCAN 11.4 мс против SEARCH 0.1 мс»). Заказывать эту проверку в четвёртый раз нечего.

Поэтому моя работа здесь — **пересчёт и опровержение**, а не второй пересказ.
Ниже по каждому пункту: моё число, число волны 2, и в чём расхождение.

---

## 3. НЕМЫЕ ПУТИ — три разных числа за три волны, и все три считали разное

Инструмент: `C:\temp\recon_wave3\excepts.py` и `C:\temp\recon_wave3\mute.py`
(`ast.parse`, только чтение). 38 боевых/инструментальных модулей корня,
58 артефактов сессии (`_probe_*`, `_measure_*`, `_sabotage_*`, `_smoke_*`, `_bak_*`,
`_verify_*`, `_skeptic_*`) и 72 `test_*` исключены из боевого счёта.

### 3.1 Сводка трёх замеров

| замер | обработчиков | «немых» | немых без переброса | голых `except:` |
|---|---|---|---|---|
| `_fix_observe.md` ЗАМЕР 2 (35 модулей) | 242 | 87 | 79 | 2 |
| `_recon_wave2.md` §3.3 (09:58, 38 модулей) | 279 | 104 | 94 | 1 |
| **моя (12:05, 38 модулей), метод волны 2** | **277** | **100** | **85** | **1** |
| **моя же, с учётом обёрток журнала** | **278** | — | **66 MUTE** | **1** |

**Главная поправка: 104 и 100 — завышены.** Оба прежних замера считали немым любой
обработчик, в теле которого нет `.debug/.info/.warning/.error/.exception/.critical/
.log/print`. Но в этом проекте журнал идёт и через обёртки:

* `blocking_tools._throttled(logging.WARNING, ...)` — 7 обработчиков,
* `runtime_guard.throttled_log(...)` — 1,
* `distiller.say(...)` — свой писатель в `distiller.log`,
* `runtime_guard.dump_error` — сохраняет причину в дамп.

С их учётом честная классификация 278 обработчиков (`mute.py`):

```
LOGGED  (пишет в журнал сам или через обёртку проекта)  187
CARRIES (не пишет, но причина уезжает наружу/перебросом) 25
MUTE    (причина не доходит ни до кого)                  66
```

То есть реальный долг немоты — **66 сайтов, а не 104**. Из них ровно `pass` в теле —
22 (`excepts.py`), остальные глушат подстановкой значения по умолчанию.

### 3.2 Голый `except:` — по-прежнему ОДИН, и это тот же сайт

`prompter.py:33`, `build_titan_prompts`, тело `cat_code = 'X.X.X'`. Совпадает с
§3.2 волны 2 дословно. **P3**: генератор промптов, врач его не видит; вред — `Ctrl-C`
посреди прогона по 52 файлам не остановит скрипт.

### 3.3 Пофайлово: LOGGED / CARRIES / MUTE (12:05)

| модуль | обработчиков | LOGGED | CARRIES | **MUTE** |
|---|---|---|---|---|
| `assistant.py` | 86 | 68 | 2 | **16** |
| `main.py` | 73 | 56 | 6 | **11** |
| `blocking_tools.py` | 19 | 12 | 3 | **4** |
| `gemini_client.py` | 14 | 6 | 0 | **8** |
| `media_tools.py` | 13 | 4 | 5 | **4** |
| `runtime_guard.py` | 10 | 2 | 3 | **5** |
| `distiller.py` | 6 | 1 | 1 | **4** |
| `database.py`, `summarizer.py`, `visionproc.py`, `reclass.py`, `deppd.py`, `import_videos.py` | 6/6/5/6/2/2 | все | | **0** |
| `tg_safety.py` | 3 | 0 | 1 | **2** |
| `videosi.py`, `vision.py`, `search_engine.py`, `gemini_knowledge.py` | 3 каждый | | | 2/1/1/1 |
| `run_all_tests.py` | 3 | 0 | 0 | **3** |
| `dental_vocab.py`, `prompter.py`, `savdel.py`, `web_lookup.py` | 1 каждый | | | 1 каждый |

Заметное расхождение с волной 2: `blocking_tools.py` у неё «15 немых из 19», у меня
**4 MUTE из 19** — разница целиком в `_throttled`. `database.py` и `summarizer.py`
чисты (0 MUTE), это подтверждает волну 2.

### 3.4 Что уже закрыто между 09:58 и 12:05 (лид: не заказывать дважды)

Волна 2 перечислила 13 горячих сайтов по номерам строк. **Все её номера строк
устарели** — `assistant.py` перезаписан в 11:01. Сверка по ИМЕНИ ФУНКЦИИ
(`C:\temp\recon_wave3\wave2sites.py`):

| функция | было у волны 2 | сейчас | вердикт |
|---|---|---|---|
| `check_and_trigger_assistant_media` | 3 немых (`2073, 2082, 2127`) | 7 обработчиков, **1** немой (`:2159`) | **2 из 3 закрыты**: `:2078` и `:2100` теперь пишут `logger.error/warning` |
| `handle_group_summary` | 1 немой (`3946`) | 2 обработчика, **0** немых (`:4006`, `:4020` пишут) | **закрыт** |
| `handle_private_message` | 3 немых (`2746, 2749, 3106`) | 20 обработчиков, **3** немых `pass` (`:2778, :2781, :3138`) + 3 с подстановкой (`:2887, :3251, :3345`) | не закрыт, сдвинулся |
| `save_state` | `418` | `:434` | не закрыт |
| `check_and_apply_silence` | `1255` | `:1271` | не закрыт |
| `handle_interactive_case_step` | `2564` | `:2596` (+`:2504` подстановка) | не закрыт |
| `check_and_trigger_referee` | `5041` | `:5158` | не закрыт |
| `media_tools._prepare_image_sync` / `_settle_after_kill` | `62` / `161` | `:62` / `:166` | не закрыты, оба с комментарием-обоснованием |

### 3.5 Немые НА ГОРЯЧЕМ ПУТИ: последствие для врача и приоритет

Горячий путь = замыкание по именам от 9 обработчиков, зарегистрированных
декоратором в `main.py` (`handle_new_message`, `handle_private_message`,
`handle_callback_query`, `handle_edited_message`, `handle_deleted_messages`,
`dump_handler`, `get_chat_id`, `manual_test_handler`, `manual_weekly_test`) — замыкание
422 имени. Оговорка: это ВЕРХНЯЯ оценка, разрешения типов нет.

MUTE на горячем пути — **37** по методу волны 2, из них с телом ровно `pass` — 10.
Разбор по последствию (текст обработчика прочитан, а не только имя):

| сайт (12:05) | что глохнет | последствие для врача | P |
|---|---|---|---|
| `assistant.py:1271` `check_and_apply_silence` | `get_messages(chat_id, ids=reply_to_msg_id)` при разборе «это ответ боту?» | сеть икнула — бот не понял, что врач отвечает ЕМУ, и режим тишины/извинения не применился: заговорит там, где велено молчать, и в журнале ни строки | **P1** |
| `assistant.py:2159` `check_and_trigger_assistant_media` | `datetime.fromisoformat(last_passive_media_run)` | битая метка в state → пассивный кулдаун 120 мин не сработал: бот комментирует снимки подряд, флудит группу врачей | **P1** |
| `assistant.py:5158` `check_and_trigger_referee` | `database.get_reply_chain_texts` | арбитр берёт решение о вмешательстве в спор на ПУСТОМ контексте — вмешается не туда либо промолчит, причина неизвестна | **P1** |
| `assistant.py:5633` `check_and_send_group_activity_pings` (тело `return`) | `json.loads` ответа модели про «горячую дискуссию» | молчаливый выход из всей рассылки пингов: никого не позвали, разбирательство «почему бот молчит неделю» не имеет ни одной зацепки | **P1** |
| `assistant.py:2778`, `:2781`, `:3138`, `:2596` | `bot_client.delete_messages(status_msg)` и `os.remove(temp_path)` | у врача навсегда остаётся «⏳ Думаю...» под готовым ответом (вечный спиннер — тот самый дефект, который в прошлой волне откатили), плюс мусор в `temp_media` | **P2** |
| `assistant.py:3251` (подстановка `content_hl = content`) | подсветка найденного в вике | врач получает ответ без выделения — терпимо | P3 |
| `assistant.py:3345`, `:2504`, `:4942`, `:5695` | длина ответа / история случая / голоса викторины / даты активности | подстановка разумная, но «викторина потеряла все голоса» и «случай начался заново» уходят без следа | P2 |
| `assistant.py:434` `save_state` | удаление `.tmp` после сбоя записи | вторичный: главная причина уже записана `logger.error` строкой выше | P3 |
| `media_tools.py:62`, `:166` | `img.close()` / добор убитого ffmpeg | обоснованы комментарием в коде; `:166` намеренно молчит, чтобы не мешать записи следующей строкой | P3 |
| `gemini_client.py` 8 MUTE (`ffmpeg_binary`, `_ffmpeg_to`, `transcribe_*`, `_save_expiry_map`) | поиск ffmpeg и уборка конвертата | «расшифровки нет» без причины; см. п.6 — там же живёт дефект вложенности | **P1** (совокупно) |
| `tg_safety.py:425` `_run_bounded` | `await task` после нашей же `cancel()` | ложная тревога: мы сами отменили задачу, докладывать нечего. **Не долг** | — |
| `runtime_guard.py:113`, `:353`, `:355` | настройка журнала и запись перед `os._exit(78)` | вынужденно: писать в журнал нельзя, он и есть предмет отказа | — |

**Чем доказать, что немота закрыта:** на каждый сайт — тест, который подсовывает в
зависимость исключение и требует (а) записи в перехваченном журнале с ТИПОМ
исключения, (б) отрицательного результата вызывающему. Проверка «в файле нет
`except Exception:\n    pass`» не считается: 44 из 66 MUTE-сайтов уже не `pass`, а
подстановка значения, и такая проверка их не видит.

**Чем доказать честность самого счёта:** пересчёт `mute.py` должен давать
LOGGED+CARRIES+MUTE = число обработчиков, а список обёрток журнала (`_throttled`,
`throttled_log`, `say`, `dump_error`) — сверяться с деревом, иначе следующая волна
опять получит 104 вместо 66.

---

## 4. TODO / FIXME / HACK / XXX — подтверждаю ноль, и нашёл, где долг живёт вместо них

### 4.1 Замер

| запрос (корень, вложенная копия исключена) | попаданий |
|---|---|
| `rg 'TODO\|FIXME\|HACK\|XXX' -g '*.py'` (регистрозависимый) | **0** |
| то же без учёта регистра | 4 — все `total_todo`, `distiller.py:847,855,879,880` (имя счётчика сита) |
| то же по НЕ-`.py` и НЕ-`.md` файлам | **0** |
| 19 русских шаблонов долга (`костыл`, `workaround`, `не реализован`, `недоделан`, `заплатк`, `надо бы`, `потом переписать`, `упрощ`, `пока просто`, `на будущее`, `в будущем`, `следует переделать`, ...) | **0 настоящих** |

Ложные попадания подтверждают тот же класс «рот в обороте», что и правила проекта:

* `ДОЛГ` — 5 попаданий, все внутри слов `долгий`/`долгая синхронизация`/`долг медиа`
  (`main.py:72,92,103,1278`, `tg_safety.py:21`). Ни одной пометки долга.
* `хак` — 8 попаданий, все внутри `ЛАЙФХАК` (`summarizer.py`, `prompter.py:52`).
* `не работает` — 5 попаданий, все описания уже ИСПРАВЛЕННОГО поведения в
  комментариях (`blocking_tools.py:698`, `assistant.py:1781`, `config.example.py:106`).

Волна 2 (§4) утверждала то же самое, и её вывод держится: **список долга в коде не
ведётся**. Пересчитано на текущем дереве в 12:15.

### 4.2 Чего волна 2 не назвала: долг ЕСТЬ в коде, но прозой в docstring

Пометок нет, а признания есть — и это те строки, которые следующему агенту нужны
больше, чем `# TODO`:

| место | что признаёт | видно ли врачу |
|---|---|---|
| `web_lookup.py:1-7` (docstring модуля) | «Не вызывает его НИКТО — grep по всем `.py` даёт только определения и тесты. То есть код есть, а функции у бота нет» | **ДА**: у врача нет веб-поиска |
| `web_lookup.py:315-321` (`clip_at_sentence`) | «Правило и поведение те же, что у `_clip_at_sentence` в `assistant.py`, — при следующей правке ядра эти два места надо свести в одно, пока ядро занято другим исполнителем» | нет (внутренний дубль, но см. п.5.2 — они РАСХОДЯТСЯ) |
| `import_videos.py:41` | «с другим маркером (с тегами `<b>`) и с непроверенным ...» — признание расхождения с `videosi.py` | **ДА**: часть видео-фактов размечена иначе |
| `assistant.py:1781` | «Поздний предохранитель почти не работает: гард пустого ...» | **ДА** |
| `assistant.py:2757` | «Сторож не спасает: он следит за живостью цикла событий» | **ДА** |
| `main.py:523` | «Добор по `is_summarized` этого не спасает: он включается ...» | **ДА**: реплики не попадают ни в один выпуск |
| `blocking_tools.py:790` | «Имя задаётся в `gemini_client.convert_to_wav`; правило продублировано здесь осознанно» | косвенно (утечка файлов) |
| `gemini_client.py:330` | ссылка на `test_fix_cascade.py:264` из боевого кода | нет — удобство разработки, но ломается при правке теста |

То есть шесть признаний долга с ПОСЛЕДСТВИЕМ ДЛЯ ВРАЧА лежат в прозе, где их не
найдёт ни один инвентарь. **P2. Чем доказать:** такие абзацы получают
машиночитаемый префикс (`# ДОЛГ:`), и тест считает их число, сверяя с числом в
отчёте волны; сейчас «`grep TODO` = 0» читается как «долга нет».

### 4.3 Устаревшая документация в коде: было 1 место, стало 2

Волна 2 нашла противоречие docstring и кода про `BOT_MENTION_SHADOW_MODE`. Сейчас
таких мест **два**:

```
assistant.py:3739   "...генерирует живой ответ и отправляет (shadow mode пока не промотировано)"
assistant.py:3758   BOT_MENTION_SHADOW_MODE = False   # Выкачено в боевой
main.py:2131        # Bot-mention trigger (always shadow mode until promoted)
```

**P3**, но цена конкретная: агент, который поверит комментарию `main.py:2131`, будет
искать непромотированный режим, которого нет, а тот, кто поверит коду, решит что
комментарий про другое.

---

## 5. ДУБЛИ, КРОМЕ ТАКСОНОМИИ — измерено, насколько копии расходятся

Таксономию не трогаю (её ведёт другой агент; отмечу только, что `taxonomy.py`
в дереве **уже появился** — `EXPORT_SLUGS`, 55 записей, `taxonomy.py:96`).
Инструменты: `C:\temp\recon_wave3\dupes.py` (AST: одноимённые функции + хеш тела),
`C:\temp\recon_wave3\diverge.py` (исходники извлечены `ast` и выполнены в
изолированном пространстве — ни один боевой модуль не импортирован).

### 5.0 P1, НОВОЕ И САМОЕ ГРУБОЕ: в корне репозитория лежит ПОЛНАЯ КОПИЯ репозитория

```
C:\...\08_Projects\stomchat\stomchat\     981 МБ, 108 .py, СВОЙ .git на том же HEAD a0376c3
md5 assistant.py    = 5724549d85de6e95e4039d8ea9c79b5a  (совпадает с корневым)
md5 main.py         = 39a2dd0d20dde2f5561592ec1741129b  (совпадает)
не junction/symlink (`dir /AL` пусто) — настоящий каталог, mtime 11:27-11:28
`git status` в корне видит его как `?? stomchat/`
```

Последствия, каждое проверяемое:

1. **`git add -A` затащит 981 МБ** во историю, включая `.env` и `*.db` вложенной
   копии — там свой `.gitignore`, но он применяется только к своему репозиторию,
   а внешний `git add` пройдёт по этим файлам как по обычным.
2. **Любой обход дерева удваивает числа.** Мой `rg` без `-g '!stomchat/**'` даёт
   ровно вдвое больше попаданий; все отчёты этой сессии, снятые после 11:28
   без этого фильтра, завышены.
3. `test_import_safety.py` сканирует `os.listdir` (не рекурсивно) — его копия
   не видит, но любой тест, который перейдёт на `os.walk`/`rglob`, начнёт
   импортировать вторые экземпляры боевых модулей со своим состоянием.
4. Агент, запустивший `python assistant.py` из вложенной папки, правит НЕ ТОТ файл,
   который лид увидит в diff.

**Чем доказать, что убрано:** `fd -t d '^stomchat$' .` в корне даёт 0, `git status`
не содержит `?? stomchat/`.

### 5.1 Одноимённые функции в разных модулях: 11 имён

| имя | копии | тела |
|---|---|---|
| `_env_int` | `gemini_client.py:25`, `main.py:47`, `vision.py:35` | **байт-в-байт одинаковые**, все три с MUTE-обработчиком `except ValueError: return default` |
| `clean_json_raw` | `reclass.py:99`, `videosi.py:63` | **одинаковые** |
| `clip_at_sentence` | `distiller.py:365`, `web_lookup.py:315` (+ `assistant.py:961` под именем `_clip_at_sentence`) | **РАСХОДЯТСЯ, см. 5.2** |
| `_json_exit` | `blocking_tools.py:88`, `media_tools.py:24` | расходятся (5 и 4 строки) |
| `perform_search` | `search_engine.py:33`, `search_engine_safe.py:10` | расходятся классом отказа (подтверждаю волну 2) |
| `init_db` | `database.py:126` (180 строк), `deppd.py:17` (23) | разные схемы разных баз |
| `main` | 8 инструментов | разные |
| `__init__`, `operation`, `_main`, `handle_private_message` | локальные/несвязанные | ложные срабатывания метода |

### 5.2 P2, НОВЫЙ ДЕФЕКТ: три копии «обрезки по предложению» расходятся на 6 входах из 8

Запуск всех трёх на одних данных (`diverge.py`, раздел A):

| вход (cap) | `assistant._clip_at_sentence` | `web_lookup.clip_at_sentence` | `distiller.clip_at_sentence` |
|---|---|---|---|
| `'короткий'` (50) — короче лимита | **`'короткий…'`** | `'короткий'` | `('короткий', 0)` |
| `'гипохлорит ... канала'` (30) | `'гипохлорит применяют в…'` | то же | `('гипохлорит применяют в', 36)` |
| `'Пункт первый.\n\n5. ...'` (25) | `'Пункт первый.'` | `'Пункт первый.'` | **`'Пункт первый.\n\n5.'`** (висящий номер) |
| тип возврата | `str` | `str` | **`tuple(str, int)`** |

Расхождения не косметические:

1. **`assistant._clip_at_sentence` НЕ имеет проверки `len(text) <= limit`** — она есть
   в обеих других копиях. Он приклеивает `…` к тексту, который никто не обрезал.
2. **Через `clean_html_formatting` это доходит до врача.** Замер
   (`C:\temp\recon_wave3\clipbug.py`): статья, у которой ПЛОСКИЙ текст 3 817
   символов (влезает в лимит Telegram 4096), но с тегами 4 160 → ветка обрезки
   включается, `_clip_at_sentence(plain, 3900)` режет по последней границе
   предложения, и врач получает:
   ```
   [Показано 3743 символов из 3817; ещё 74 не поместились в одно сообщение]
   ```
   **Последнее предложение статьи потеряно (в замере это была фраза с дозировкой
   «3 мг на кг»), а приписка «не поместились» — неправда: 3 817 символов помещаются.**
3. `assistant.py:950` и `:1018` (`entry`/`line` для промпта) вызывают ту же функцию
   на КОРОТКИХ записях: каждая запись корпуса и истории ЛС уходит в модель с
   приклеенным `…`, то есть модель видит «факт оборван» там, где он полный.
4. `distiller.clip_at_sentence` вернуть в это место нельзя без правки вызывающего —
   у него другой тип возврата. Свести три копии в одну можно только с решением
   про тип.

**Чем доказать:** одна функция в одном месте; тест подаёт (а) текст короче лимита и
требует РАВЕНСТВА входу без `…`, (б) HTML на 4 160 символов с плоским текстом
3 817 и требует, чтобы врачу ушёл ВЕСЬ текст (или чтобы приписка называла честное
число), (в) висящий `5.` не оставался.

### 5.3 Разбор JSON от модели: две копии, и они НЕ расходятся — расходится качество

Гипотеза «две реализации разбора JSON ведут себя по-разному» **опровергнута
замером**: `distiller.clean_json_string` и `reclass/videosi.clean_json_raw` на 6
реалистичных ответах модели дают **0 расхождений**. Зато обе одинаково ломаются:

| вход | результат обеих | `json.loads` |
|---|---|---|
| ` ```json\n{"a": 1}\n``` ` | `{"a": 1}` | ок |
| `Вот результат: {"a": 1} — надеюсь, помогло }` | `{"a": 1} — надеюсь, помогло }` | **падает** |
| `префикс {"a": 1} суффикс {"c": 3}` | `{"a": 1} суффикс {"c": 3}` | **падает** |
| `{"a": 1` (обрыв) | как есть | **падает** |

Причина у обеих — жадный поиск ДО ПОСЛЕДНЕЙ `}`: `text.rfind('}')` и
`re.search(r'\{.*\}', DOTALL)`. Любая закрывающая скоба в болтовне модели после
объекта ломает разбор всей пачки. **P2.** В `distiller.py` рядом уже живёт
`_iter_json_objects` (`:569`) — сканер со счётчиком глубины, то есть правильная
реализация в дереве ЕСТЬ и две наивные копии её не используют.
**Чем доказать:** обе точки зовут `_iter_json_objects`; тест подаёт ответ с
болтовнёй после объекта и требует, что факты разобраны, а не потеряна пачка.

### 5.4 Списки ключевых слов: один настоящий дубль, один ложный

| список | где | вердикт |
|---|---|---|
| `_RU_SUFFIXES` (25 строк) `assistant.py:794` против `RU_SUFFIXES` (25) `dental_vocab.py:205` | **ИДЕНТИЧНЫ** (сравнение множеств: 0 расхождений в обе стороны) | P3, свести в одно |
| `_SILENCE_DEMANDS` (11) + `_SILENCE_PHRASES` (20) `assistant.py` против `SILENCE_HALLUCINATIONS` (10) `main.py:1236` | **пересечение 0 и 0** — разные задачи (требование врача молчать против маркеров галлюцинации). **Не дубль**, гипотеза снята | — |
| `DENTAL_KEYWORDS` (276) `dental_vocab.py:30` против `STOP_WORDS` (55) `assistant.py:313` | разные роли | — |
| `JUNK_HOST_MARKERS` (25) + `AD_TEXT_MARKERS` (17) `web_lookup.py` | единственная копия | — |

### 5.5 Клиенты к моделям: ШЕСТЬ мест создают клиента, здоровье ключей знает ОДНО

| модуль | как создаёт | `banned_models.json` | `key_cooldowns` | `_is_retryable_*` |
|---|---|---|---|---|
| `gemini_client.py:142` | `OpenAI(...)` | **5 ссылок** | **4** | **2** |
| `vision.py:296` | `AsyncOpenAI(...)` | 0 | 1 | 0 |
| `gemini_knowledge.py:213` | `genai.Client` | 0 | 0 | 0 |
| `reclass.py:308` | `genai.Client` | 0 | 0 | 0 |
| `videosi.py:133` | `genai.Client` | 0 | 0 | 0 |
| `delist.py:9`, `benchmark.py:38` | `genai.Client` / `Groq` | 0 | 0 | 0 (обезврежены `SystemExit`) |

`banned_models.json` на диске содержит **4 записи** (имена моделей вида
`gemini-3.1-f...`), то есть механизм бана РАБОТАЕТ и уже что-то забанил. Но знает
о нём только `gemini_client.py`. Пять остальных путей продолжат бить в
забаненную модель и в ключ на кулдауне.

Последствие для врача: квота, которую съедает корпусный скрипт или путь зрения по
уже забаненной модели, — это та же квота, из которой оплачивается его ответ; при
исчерпании он видит «Попробуйте позже», а в журнале бана нет, потому что писал не
тот модуль. **P2** (P1, если корпусные скрипты гоняют одновременно с ботом).
**Чем доказать:** один модуль владеет здоровьем ключей/моделей, остальные ходят
через него; тест банит модель через API и требует, чтобы второй путь её не выбрал.

### 5.6 Прочие дубли, подтверждённые от волны 2

`videosi.py`/`import_videos.py` (оба `INSERT INTO distilled_facts`),
`search_engine.py`/`search_engine_safe.py`, `patch_assistant.py`/`_v2.py`,
`filemake.py`/`savdel.py`/`prompter.py`. Плюс `_bak_media_tools.py` (13 688 Б) —
живой бэкап-артефакт в корне; `_bak_assistant_tgdelivery.py`, который волна 2
называла, **уже удалён** (`fd '^_bak' -e py` даёт один файл, не два).

---

## 6. ВЛОЖЕННОСТЬ БЮДЖЕТОВ — таблица по пяти затребованным путям

Инвентарь: `C:\temp\recon_wave3\budgets.py` (**98** модульных констант бюджета,
**77** литеральных `timeout=<число>` в вызовах, из них 32 в `assistant.py`),
разбор путей — `C:\temp\recon_wave3\paths.py`, границы обращений к Telegram —
`C:\temp\recon_wave3\unbounded.py`. Замер 12:25.

### 6.1 Таблица

| # | путь | родительский дедлайн | дочерние таймауты | влезает |
|---|---|---|---|---|
| 1 | **доставка сводки** (дневная) | `main.SUMMARY_STALE_SECONDS` = `summarizer.GEMINI_GENERATION_TIMEOUT_SECONDS`(2100) + 600 = **2700**, но это **возраст файла статуса**, а не сумма пути (`main.py:456-479`, при превышении `os._exit(79)`) | генерация 2100 → подпроцесс 2110 (`+_SUBPROCESS_STARTUP_SLACK`), Telegraph 60, поиск дубля 90, отправка 90, повтор plain-текстом 90, закреп 30, БД 30+30 = **2520** | **ДА** (запас 180, а не 600 как у волны 2 — она не считала повтор plain и скан дубля). Оговорка: на ВЕСЬ путь дедлайна нет вовсе, сторож ловит только «статус не двигался 2700 с» |
| 2 | **расшифровка голосового** (группа) | `asyncio.wait_for(transcribe_group_voice, item_budget)`, `item_budget` ← `VOICE_ITEM_BUDGET_SECONDS` = 120+60+45 = **225** (`main.py:1206, 1608`) | скачивание 120 (`:1301`), расшифровка 60 (`:1312`) = 180 | **ДА** на уровне `main` |
| 2a | ...внутри подпроцесса расшифровки | `blocking_tools._run_json_tool`: `deadline = timeout + 10` = **70** (`blocking_tools.py:482`) | `gemini_client._ffmpeg_to` → `subprocess.run(timeout=**120**)` (`:742`); `_probe_ffmpeg` `timeout=30` (`:671`) × **до 4 кандидатов** = до 120 | **НЕТ. До 240 с внутри 70 с.** Подтверждаю §6.2 волны 2 и уточняю её число: она считала 2 кандидата (60), а код перебирает до 4 (env, PATH, `imageio_ffmpeg`, `static_ffmpeg`) |
| 3 | **зрение / медиа** | **РОДИТЕЛЯ НЕТ**: `media_analysis_worker` запущен `runtime_guard.create_task` (`main.py:956`), внутри `media_analysis_worker` (`:1128-1143`) ни одного ограниченного вызова | скачивание 120, кадр 60, `vision.describe_image` 258, три записи в БД по 30 = **528** на один снимок | нечему влезать; каждый шаг ограничен, но **один снимок держит единственный воркер до 528 с** при очереди на 128 |
| 3a | ...внутри разбора снимка | `MEDIA_ANALYSIS_TIMEOUT_SECONDS` = max(180, каскад+60) = **258** (`main.py:130`) | каскад зрения: `VISION_MODEL_POOL_SIZE`(3) × `VISION_KEYS_PER_MODEL`(2) × (`GROQ_HTTP_TIMEOUT`(30)+`MIN_CALL_INTERVAL`(3)) = **198**, подготовка снимка 45 = 243 | **ДА**, запас 15 |
| 4 | **веб-поиск** (`/web`, `/найди`) — **сшит в эту сессию**, `assistant.py:3321` | `WEB_COMMAND_TIMEOUT_SECONDS` = 20+215+90+15 = **340** (`assistant.py:93`) — **объявлен и НЕ ИСПОЛЬЗУЕТСЯ НИ В ОДНОМ вызове** (`rg` даёт только определение) | статус 20, `run_lookup` 215, уборка статуса 15, доставка 90 = 340 | **арифметически ДА**, принудительно — нет: сумма соблюдается только потому, что этапы последовательны и каждый ограничен сам |
| 4a | ...внутри `run_lookup` | 215 (`web_lookup.LOOKUP_TOTAL_COST_SECONDS`) | поиск 2 × (45+10) = 110 (`:744`), генерация = **остаток** + 10 (`:803`), троттлинг 5 | **ДА** — и это единственный путь в проекте, где ребёнку выдаётся ОСТАТОК, а не своё желаемое число |
| 5 | **викторина: генерация** (`handle_group_quiz`, `assistant.py:4351-4467`) | **РОДИТЕЛЯ НЕТ** | генерация 90 (`:4375`) + **4 сырых `bot_client.send_message` без таймаута** (`:4358, 4361, 4379, 4462`) + `delete_messages` (`:4376`) | **НЕТ ПОТОЛКА.** По собственному замеру проекта (`assistant.py:44-49`) один такой `await` висит до **~500 с** (`timeout=30` × `request_retries=10`) |
| 5a | **викторина: нажатие кнопки** (`handle_quiz_callback`, `assistant.py:4834-5194`, 361 строка) | **РОДИТЕЛЯ НЕТ**: `main.handle_callback_query` (`:2317`) зовёт `await assistant.handle_quiz_callback(...)` НАПРЯМУЮ, без `wait_for` | правки сообщения ограничены 25 с через хелпер `edit_callback_message` (`:4830`), но **16 сырых `event.answer()` без таймаута** | **НЕТ.** Спиннер на кнопке снимает именно `event.answer()`, и он единственный в этой функции не ограничен ничем |

### 6.2 P1: `event.answer()` — 16 неограниченных вызовов ровно там, где проект уже понял риск

Docstring `edit_callback_message` (`assistant.py:4810-4829`) написан правильно:

> «Спиннер на кнопке снимает `event.answer()`, а он стоит строкой НИЖЕ правки. Пока
> правка была без границы, зависший Telegram означал: врач смотрит на крутящуюся
> кнопку до 500 с ... Страховка в `main.py:2315` тут не помогает: она ловит
> исключение, а зависание не исключение».

Правку под срок увели, а сам `event.answer()` — нет: **16 сайтов** в
`handle_quiz_callback` (`:4850, 4864, 4886, 4927, 4945, 4958, 4977, 4995, 5011,
5035, 5069, 5105, 5107, 5121, 5138, 5157`) плюс 2 в `main.py`. Последствие для
врача ровно то, которое docstring и описывает, только причина сместилась на одну
строку: кнопка крутится, врач жмёт снова, дедупликатор
(`main.py:2312 HANDLED_CALLBACK_IDS`) второй тап отбрасывает — и второй
`event.answer()` в ветке дедупликации **тоже без срока**.
**Чем доказать:** мок клиента, у которого `answer()` не возвращается дольше
бюджета; проверка, что обработчик завершился за отведённое время и записал причину.

### 6.3 Границы обращений к Telegram: честный пересчёт заявки волны 2

Волна 2: «из 110 обращений `assistant.py` через слой идут 5 (6.4 % с учётом
`tg_safety.edit_message`), 103 прямые». Мой пересчёт с учётом ОБЪЕМЛЮЩЕГО
`asyncio.wait_for` (`unbounded.py`, 12:25):

| файл | обращений | через `tg_safety` | внутри `wait_for` | **БЕЗ ГРАНИЦ ВООБЩЕ** |
|---|---|---|---|---|
| `assistant.py` | 111 | **11** | 2 | **98** |
| `main.py` | 18 | 0 | 6 | **12** |
| `summarizer.py` | 4 | 0 | 4 | **0** |

Принятие выросло с 5 до **11** сайтов (кто-то в этой сессии подключил ещё шесть),
но 98 обращений `assistant.py` по-прежнему не ограничены ничем: 69 `send_message`,
16 `answer`, 8 `delete_messages`, 4 `get_messages`, 1 `get_sender`.
`summarizer.py` — единственный модуль с полным покрытием (4 из 4 в `wait_for`),
подтверждаю волну 2.

### 6.4 Пути БЕЗ родительского дедлайна — отдельная находка, а не «влезает»

`runtime_guard.create_task` (`runtime_guard.py:404`) — голый
`asyncio.create_task` с именем; ни `wait_for`, ни бюджета. Через него запущено
**15** долгоживущих задач:

```
main.py:956   media_analysis_worker      main.py:2176  run_assistant_safe
main.py:1748  run_media_assistant_safe   main.py:2307  run_pm_safe
main.py:2057  _process_album_after_delay main.py:2646  transcribe_voice_backlog
main.py:2675  handle_new_message (догон) main.py:2757  heartbeat
main.py:2781  scheduler                  main.py:2782  pm_ping_scheduler
main.py:2783  runtime_telemetry          main.py:2784  summary_watchdog
main.py:2785  health_watchdog            main.py:2786  media_recovery
assistant.py:3695  _bg_portrait
```

Из них на горячем пути врача — `run_pm_safe` (личный вопрос), `run_assistant_safe`
(ответ в группе), `run_media_assistant_safe` (снимок), `_bg_portrait`. У них НЕТ
верхнего срока: сумма внутренних этапов и есть потолок. Для `/web` это 340 с, для
викторины — не ограничено. **P2** как класс: не «нарушение вложенности», а
отсутствие внешнего контура, из-за которого замок на пользователя
(`main.py:2302 _pm_user_lock`) может держаться сотни секунд, и все следующие
сообщения врача ждут молча.

### 6.5 Один бюджет двумя числами — состояние заявки волны 2

| смысл | константа | литерал | сейчас |
|---|---|---|---|
| расшифровка | `main.VOICE_TRANSCRIBE_TIMEOUT_SECONDS` = 60 | `assistant.py:2818` `timeout=60` | **разъезд остался** |
| кадр из видео | `main.MEDIA_FRAME_TIMEOUT_SECONDS` = 60 | `assistant.py:3599` `timeout=60` | **разъезд остался** |
| генерация ответа | константы нет | `timeout=90` в `assistant.py` — **7 сайтов** (`:2023, 2329, 2643, 3160, 3459, 3857, 4156, 4262, 4375` — 9 попаданий, 90 в семи функциях) | **не сведено** |
| доставка/правка | `SUMMARY_DELIVERY_TIMEOUT_SECONDS` 90, `CALLBACK_EDIT_TIMEOUT_SECONDS` 25, `PM_STATUS_EDIT_TIMEOUT_SECONDS` = 90//2 | — | **сведено правильно**: выведены друг из друга |

Прогресс есть: `assistant.py:50-98` — новый блок, где бюджеты ВЫВОДЯТСЯ
(`PM_STATUS_EDIT = SUMMARY_DELIVERY // 2`, `WEB_COMMAND = сумма этапов`), с
комментарием «класс „внутренний срок больше внешнего“ всплывал четыре раза».
Это правильное лечение класса, применённое к двум путям из семи.

**Чем доказать по пункту 6:** тест сравнивает пары ЧИСЕЛ (не строк исходника) для
каждой строки таблицы 6.1 и падает при `>=`; для 2a — сравнение
`gemini_client` `subprocess.run(timeout=...)` c
`VOICE_TRANSCRIBE_TIMEOUT_SECONDS + _SUBPROCESS_STARTUP_SLACK_SECONDS`; для 5a —
поведенческий тест с зависшим `answer()`. Помнить про капкан из §9.1 волны 2:
`test_voice_pipeline.py:425-428` приколачивает `"timeout=120" in исходнике`, то есть
**правильная починка 2a уронит существующий тест** — его надо править в той же волне.
Это я проверил диверсиями 1 и 2, см. §9.

---

## 7. ТЕСТОВ НЕТ — было 21 модуля без покрытия, стало 11

Инструмент: `C:\temp\recon_wave3\coverage.py`. «Покрыт» = имя модуля появляется в
`import` какого-нибудь `test_*.py` (верхняя оценка, как и у волны 2). Отдельно
выделен класс «тест читает модуль как ТЕКСТ» — таких в проекте много и они не
проверяют поведение. Замер 12:30: **39 боевых/инструментальных модулей, 77 тестов**
(волна 2 в 09:58 видела 39 и 65).

### 7.1 Не тронут ни одним тестом — 11 модулей

| модуль | строк | флаги опасности | вердикт |
|---|---|---|---|
| `visionproc.py` | 225 | **SQL-WRITE + боевая БД + сеть** | **P1**: пишет `vision_description` в боевую базу, ходит в Telegram и в зрение, ноль тестов |
| `deppd.py` | 158 | **SQL-WRITE + боевая БД + сеть** | **P1**: выкачивает историю в `stomat_archive.db` (117 847 строк) |
| `run_all_tests.py` | 170 | боевая БД в списке md5-охраны | P2: раннер, который сам сторожит целостность, не проверен ничем (и по заданию его запускать нельзя — md5-охрана даёт ложную тревогу при параллельных агентах) |
| `filemake.py` | 61 | читает боевую БД, пишет 280 файлов | P2 |
| `pathcheck.py` | 62 | читает боевую БД | P3 |
| `debugdist.py` | 51 | читает боевую БД | P3 |
| `prompter.py` | 100 | — | P3, и в нём единственный голый `except:` в дереве |
| `search_engine_safe.py` | 26 | — | P2: **всё ещё мёртв.** Сшитый в эту сессию `/web` зовёт `blocking_tools.web_search_async` НАПРЯМУЮ (`assistant.py:3372`), а не `perform_search` |
| `inspector.py`, `id.py`, `delme.py` | 55/29/21 | сеть (Telegram) | P3, разовые |

Против волны 2 закрыто **десять** модулей: `checker.py`, `reclass.py`, `savdel.py`,
`videosi.py`, `import_videos.py`, `web_lookup.py`, `gemini_knowledge.py`,
`dental_vocab.py`, `html_safe.py`, `distiller.py` теперь импортируются тестами, плюс
появился новый `taxonomy.py` (1 тест). То есть заявка волны 2 «семь скриптов с правом
записи в боевые данные и ноль тестов на всех семерых» **устарела: осталось два**
(`visionproc.py`, `deppd.py`).

### 7.2 Класс «тест читает модуль как текст» — 6 модулей

`benchmark.py`, `delist.py`, `patch_assistant.py`, `patch_assistant_v2.py` (все
через `test_import_safety.py`), `search_engine.py` (через новый
`test_dead_tools.py`, 24 522 Б, создан 12:34), `config.example.py` (через
`test_config_contract.py` и `test_report_targets.py`).

Это законно для реестра мёртвых инструментов (импортировать их и есть авария —
docstring `test_dead_tools.py` описывает случай 28 июля, когда импорт переписал
`assistant.py`), но по правилу проекта «проверка „в исходнике есть строка“ не
считается» такие проверки НЕ дают покрытия поведения.

### 7.3 Побочная находка: `benchmark.py` не разбирается `ast.parse`

`ast.parse(open('benchmark.py').read())` падает: `SyntaxError: invalid
non-printable character U+FEFF (line 1)` — в файле BOM, и при чтении без
`utf-8-sig` он попадает в первую строку. Значит **любой инвентарь дерева на `ast`
молча теряет этот файл**, и мой первый прогон тоже (исправил на `utf-8-sig`).
Волна 2 считала 128 файлов тем же способом — её числа по `benchmark.py` пустые не
потому, что там нечего считать. **P3, но это дефект инструментов инвентаризации, а
не кода**: проверять, что число разобранных файлов равно числу найденных.

**Чем доказать по пункту 7:** тест, который для каждого модуля из списка 7.1
вызывает хотя бы одну функцию и проверяет наблюдаемый результат (для `visionproc` и
`deppd` — на временной копии базы, с проверкой что боевой файл не изменился по mtime
и md5). Пересчёт `coverage.py` должен давать меньше 11.

---

## 8. ЧТО ИЗ ПРЕЖНИХ ОТЧЁТОВ ДО СИХ ПОР НЕ СДЕЛАНО (проверено на живом коде 12:40)

Прочитаны целиком `_recon_observe.md` (320 строк), `_recon_telegram.md` (306),
`_recon_distill.md` (250). Проверка — по коду и данным, не по отчётам о правках.
Инструменты: `C:\temp\recon_wave3\deferred.py`, `datastate.py`.

### 8.1 `_recon_observe.md`

| пункт | состояние | доказательство (12:40) |
|---|---|---|
| Н1 строка в журнал перед `os._exit(78)` | СДЕЛАНО | подтверждаю волну 2 |
| Н2 релей stderr в `media_tools` | СДЕЛАНО | `media_tools.py:113-120` `_drain_stderr` |
| Н3 тип и трейсбек падения ребёнка | СДЕЛАНО | подтверждаю волну 2 |
| Н4 один сайт `health_check` | СДЕЛАНО | подтверждаю волну 2 |
| **Н4 как КЛАСС** | **НЕ СДЕЛАНО** | **113 вызовов журнала печатают только `str(исключения)`** без типа и без `exc_info`: `assistant.py` **64**, `main.py` **24**, `gemini_client.py` 6, `visionproc.py` 4, `summarizer.py` 3, `blocking_tools.py` 2, `database.py` 2, `search_engine.py` 2, `vision.py` 2, ещё 4 по одному. Разведка заявляла 109, волна 2 — 115; сейчас 113 |
| Н5.1 отметка медиа | СДЕЛАНО | `test_media_loop_guard.py` |
| Н5.2 `ALTER TABLE` молчит только на дубле колонки | СДЕЛАНО | `database.py`, 0 MUTE-обработчиков во всём модуле |
| Н5.3 голый `except:` у `REPORT_TARGETS` | СДЕЛАНО | голых `except:` в `main.py` ноль |
| Н5.4 писатели дампа | СДЕЛАНО | причина уезжает в `dump_error` |
| **Н5.5 семь точек «отвечать или молчать»** | **ЧАСТИЧНО: было 7, осталось 5** | `check_and_apply_silence` (`:1271`) — да; `check_and_trigger_assistant_media` — **было 3, остался 1** (`:2159`), два получили `logger.error/warning`; `check_and_trigger_referee` (`:5158`) — да; `check_and_send_group_activity_pings` — 2 сайта (`:5633` тело `return`, `:5695` подстановка дат) |
| **п.4.1 сканирование доставки навсегда запрещено боту** | **НЕ СДЕЛАНО** | `summarizer.py:173-174` по-прежнему `logger.warning(...)` + `return None`; слов «навсегда»/«не поддерживается ботом» в модуле НЕТ |
| **п.4.2 гео-блокировка как временная ошибка** | **НЕ СДЕЛАНО** | `gemini_client.py:88`: `retry_markers` = `['deadline','timeout','timed out','temporarily','unavailable',`**`'failed_precondition'`**`,'connection','transport']`; строки `User location` в модуле нет вообще |
| **п.4.3 Groq 413 короткое замыкание** | **НЕ СДЕЛАНО** | `413` встречается в `gemini_client.py` дважды, оба раза в КОММЕНТАРИИ (`:652-653`), кода различения нет |
| **п.6 усечь `bot.log`** | **НЕ СДЕЛАНО** | `bot.log` = **12 373 строки**, mtime **2026-07-28 10:00** (за эту сессию не тронут), внутри 648 × «файл занят другим процессом», 150 × «падение до event.answer()», 11 × MagicMock, 46 × MESSAGE LOST — всё тестовая выдумка |

### 8.2 `_recon_telegram.md`

| пункт | состояние | доказательство |
|---|---|---|
| **Н1 один хелпер отправки** | **ЧАСТИЧНО: 11 из 111** | принятие выросло с 5 до 11 сайтов; **98 обращений без границ вообще** (§6.3). Но: диверсия 3 (§9) показала, что теперь это ДЕРЖИТ тест — 26 провалов из 71 |
| Н2 FloodWait не отказ пользователя | СДЕЛАНО | `assistant.py:5732` и `:5930` — `tg_safety.classify(send_err) == KIND_FLOOD` в ОБОИХ путях пингов (ЛС и группа), `ping_failures` не растёт |
| **Н3 дубль разрешения сущностей** | **НЕ СДЕЛАНО** | `assistant.py:1319` (`check_and_apply_silence`) и `:1599` (`check_and_trigger_assistant`) — **тот же чат, тот же `reply_to_msg_id`, два сетевых `get_messages`**; плюс `:1642` в цикле цепочки и `:2148` на пути медиа. Всего 4 сайта |
| Н4 логгер telethon | СДЕЛАНО | `runtime_guard.py:132` `telethon` → ERROR, `:140` `telethon.client.users` → **INFO** (FloodWait снова виден) |
| Н5 `get_me()` на горячем пути медиа | СДЕЛАНО | в `assistant.py` остался **1** вызов `get_me()` (`:268`, `resolve_bot_identity`), на пути медиа комментарий `:2134-2135` объясняет замену; `BOT_ID` — 24 упоминания, `BOT_USERNAME` — 12 |

### 8.3 `_recon_distill.md` — код починен, БОЕВЫЕ ДАННЫЕ не тронуты ВООБЩЕ

Числа разведки перепроверены на живой базе **и совпали в точности**:

| замер | разведка | мой (12:40) |
|---|---|---|
| фактов, у которых ВСЕ токены `source_ids` нечисловые | 353 | **353** |
| нечисловых токенов всего | 2 073 | **2 073** |
| восстановимых уникальных id | 1 985 | **1 985** |
| `media_links` непусто | 0 из 12 784 | **0** |
| `confidence=10` | 12 731 | **12 731** |
| `is_case=1` | 10 862 | **10 862** |
| различных значений `category_code` | — | **6 711** |
| `is_processed_for_wiki=0` | 444 | **444** (кандидатов к дистилляции 0) |
| последняя дата архива | 2026-02-19 | **2026-02-19 13:13:49** (160 дней) |

Схема `distilled_facts` (12:40):
`['id','category_code','content','source_ids','media_links','is_case','confidence','processed_at','is_reclassified']`
— **`category_code_prev` НЕТ, `content_hash` НЕТ**, индекс ровно один (`idx_cat`),
mtime базы **2026-02-19 17:48:57**.

Вывод: `reclass.py` умеет делать `VACUUM INTO` и миграцию `category_code_prev`
(волна 2 это подтвердила по коду), но **на боевой базе миграция не выполнялась**,
то есть защита существует только в коде. Сброс `is_processed_for_wiki` сегодня
по-прежнему удвоит базу: UNIQUE нет.

### 8.4 Что из прежних отчётов лид может НЕ заказывать (закрыто волнами 1-2)

Голые `except:` в `main.py` (0), релей stderr в `media_tools`, тип исключения у
падения ребёнка, `os._exit(78)` без записи, `ALTER TABLE` молча, `REPORT_TARGETS`
без валидатора, `get_me()` на горячем пути медиа, логгер telethon, FloodWait как
отказ пользователя, зацикливание медиа, `handle_group_summary` без записи,
2 из 3 немых в `check_and_trigger_assistant_media`, покрытие тестами десяти
корпусных модулей, сшивка `/web` (с корректно вложенным бюджетом).

---

## 9. САБОТАЖ: 6 диверсий, все в песочнице ВНЕ репозитория

Лана разведки не правит `.py`, поэтому диверсии применены к копии дерева в
`C:\temp\recon_wave3\sandbox\` (201 файл: все `.py` корня + `stomat_wiki.db` +
`banned_models.json` + состояние). Драйвер — `C:\temp\recon_wave3\sabotage.py`:
он сам делает `.sabbak`, применяет замену, гоняет тесты, восстанавливает файл и
печатает результат сверки. **Ни один файл репозитория не изменён.**

Опорный прогон в песочнице ДО диверсий: `test_budget_nesting` **29/0**,
`test_voice_pipeline` **52/0**, `test_web_lookup` **110/0**, `test_tg_delivery` **71/0**.

| # | диверсия | прогнано | упало | поймано |
|---|---|---|---|---|
| 1 | **правильная починка** 6.2: ffmpeg `120` → `45` (влезает в 70) | `test_voice_pipeline` 52, `test_budget_nesting` 29, `test_voice_offline` 56 | **1** (только voice_pipeline) | инверсия: тест ЗАЩИЩАЕТ дефект |
| 2 | ffmpeg `120` → `10000` (дефект усилен в 83 раза) | те же 137 | **1** — та же самая проверка | формально да, по смыслу нет |
| 3 | `edit_callback_message` откачен на прямой `bot_client.edit_message` без срока | `test_tg_delivery` 71, `test_protocols_ui` 32, `test_group_quiz` 28, `test_wiki_pagination` 40 | **26** | **ДА** |
| 4 | из `web_lookup.clip_at_sentence` убрана проверка «текст короче лимита» | `test_web_lookup` 110 | **1** | **ДА** |
| 5 | с отправки сводки снята граница `asyncio.wait_for` | `test_tg_delivery` 71, `test_summary_delivery` 41, `test_report_targets` 25 | **0** | **НЕТ** |
| 6 | запас на подъём подпроцесса `10` → `0.1` | `test_budget_nesting` 29, `test_fix_subprocess` 59 | **2** (по одной в каждом) | **ДА** |

### 9.1 Диверсии 1 и 2 подтверждают заявку волны 2 независимо. P1

`120 → 45` (дефект УСТРАНЁН) и `120 → 10000` (дефект усилен в 83 раза) дают
**идентичный результат: одна и та же проверка падает**. Значит проверка реагирует
на изменение литерала, а не на нарушение вложенности, и агент, который починит
п.6.2/2a правильно, получит красный тест. `test_budget_nesting.py` — тот, чья
объявленная область и есть вложенность бюджетов, — **не видит ни одного из двух
случаев** (29/0 в обоих).

### 9.2 Диверсия 3 ОПРОВЕРГАЕТ вывод волны 2 о `tg_safety`

Волна 2 (§9.3): «Ничто в дереве не удерживает принятие `tg_safety`. Слой можно
откатить целиком, и 194 проверки трёх тестов не заметят». Сейчас это неверно:
`test_tg_delivery.py` (71 проверка, создан и усилен скептиком в этой сессии до
71) падает **26 проверками** на откате одного хелпера. Держит именно он: три
других теста (100 проверок) молчат.

### 9.3 Диверсия 4: тест есть у КОПИИ и нет у оригинала

Проверка «текст короче лимита возвращается как есть» существует для
`web_lookup.clip_at_sentence` (падает при удалении гарда), а ровно этот дефект
живёт в `assistant._clip_at_sentence` (§5.2) — и его не проверяет никто. То есть
покрытие ушло к вспомогательной копии, а не к той, что видит врач.

### 9.4 Диверсия 5: единственный полностью ограниченный модуль ничем не удержан. P2

`summarizer.py` — 4 обращения к Telegram, все в `asyncio.wait_for` (§6.3).
Снятие границы с отправки сводки не заметили **137 проверок трёх тестов**.
Значит правку, которая вернёт сводку к неограниченному `send_message` (сон
telethon до ~500 с на FloodWait), никто не остановит.
**Чем доказать:** тест подсовывает клиент, чей `send_message` спит дольше бюджета,
и требует, чтобы `_send_message_once` вернул отказ за отведённое время.

### 9.5 Восстановление и доказательство

* Все шесть диверсий применялись к **копии** в `C:\temp\recon_wave3\sandbox\`;
  драйвер после каждой печатал `восстановлено: True` (побайтовая сверка с исходным
  текстом). Песочница вместе с копией `stomat_wiki.db` **удалена** после прогона.
* В репозитории **ни один `.py` не изменён и не создан мной**. `fd -H
  '\.(sabbak|skepbak|fixedbak)$'` в корне даёт **0** файлов.
* Боевые базы: открывались только `mode=ro`; `stomat_wiki.db` mtime
  **2026-02-19 17:48:57** (за сессию не тронут), `stomat_archive.db` не открывался
  на запись ни разу.
* **Продакшн восстановлен (не был изменён), тесты в САМОМ РЕПОЗИТОРИИ снова
  зелёные: `test_budget_nesting` 29/0, `test_voice_pipeline` 52/0,
  `test_web_lookup` 110/0, `test_tg_delivery` 71/0** — те же числа, что опорный
  прогон в песочнице.
* `run_all_tests.py` не запускался (запрещён заданием), поэтому о состоянии набора
  ЦЕЛИКОМ я ничего не утверждаю.

---

## 10. СВОДКА ДЛЯ ЛИДА: что волна 3 подтвердила, что опровергла, что нашла нового

### 10.1 Опровергнуто или устарело в прежних отчётах (не заказывать)

| заявка | как было | как есть |
|---|---|---|
| «`_recon_wave2.md` отвечает только на пункты 1-2» (задание) | — | отвечает на все 8 + саботаж + сводка, 948 строк |
| «в боевой вике индексов нет вообще» | `_recon_wave2.md` §2 | **сама волна 2 это опровергла в §8.4**; `idx_cat` есть с февраля |
| «104 немых обработчика» / «87 немых» | волна 2 §3.3 / `_fix_observe.md` | **66 MUTE**: обе цифры считали немыми обработчики, пишущие через `_throttled`/`throttled_log`/`say` |
| «13 немых на горячем пути по этим строкам» | волна 2 §3.4 | номера строк устарели все; `handle_group_summary` закрыт, `check_and_trigger_assistant_media` 3 → 1 |
| «ничто в дереве не удерживает принятие `tg_safety`» | волна 2 §9.3 | **держит** `test_tg_delivery.py`: 26 провалов из 71 на откате (диверсия 3) |
| «7 скриптов с правом записи в боевые данные и ноль тестов» | волна 2 §7.1 | осталось **2** (`visionproc.py`, `deppd.py`) |
| «21 модуль без покрытия» | волна 2 §7.1 | **11** |
| «веб-поиск не подключён / 110 с без родителя» | волна 2 §1.1, §6.6 | **сшит** (`assistant.py:3321`), бюджет вложен корректно: 20+215+15+90 = 340 |
| «`_bak_assistant_tgdelivery.py` в корне» | волна 2 §1 поправка | удалён |
| «две реализации разбора JSON расходятся» (моя гипотеза) | — | **0 расхождений на 6 входах**; расходится не поведение, а качество — обе ломаются одинаково |
| «списки тишины дублируются» (моя гипотеза) | — | **пересечение 0**, разные задачи |

### 10.2 Новое в этой волне, по величине эффекта

| P | что | где | чем доказать |
|---|---|---|---|
| **P1** | **981 МБ полная копия репозитория внутри репозитория**, со своим `.git`, байт-в-байт | `stomchat/stomchat/` | `fd -t d '^stomchat$'` = 0; `git status` без `?? stomchat/` |
| **P1** | **16 сырых `event.answer()` без срока** — ровно тот вызов, который снимает спиннер, и единственный неограниченный на пути кнопки | `assistant.py:4850…5157`, +2 в `main.py` | мок с зависшим `answer()`: обработчик завершился в срок и записал причину |
| **P1** | путь викторины и путь ЛС **не имеют внешнего дедлайна вообще** (`create_task`), при этом `/web` внутри них стоит до 340 с и держит `_pm_user_lock` | `main.py:2176, 2307`, `runtime_guard.py:404` | замок отпускается не позже согласованного бюджета; тест на второй вопрос врача во время первого |
| **P1** | ffmpeg 120 с (+ до 4 проб по 30 с) внутри родительских 70 с — **подтверждено**, и починку роняет собственный тест | `gemini_client.py:742, 671` против `main.py:1195` + `blocking_tools.py:224`; `test_voice_pipeline.py:425-428` | диверсия `120 → 45` должна ПРОХОДИТЬ, `120 → 10000` падать |
| **P2** | **три копии обрезки по предложению расходятся на 6 входах из 8**; у копии в `assistant.py` нет проверки «текст короче лимита», и через `clean_html_formatting` врач теряет последнее предложение статьи с ложной припиской «ещё 74 не поместились» | `assistant.py:961`, `web_lookup.py:315`, `distiller.py:365` | плоский текст 3 817 символов доходит целиком; текст короче лимита возвращается БЕЗ `…` |
| **P2** | оба разбора JSON от модели жадные до последней `}` — болтовня модели после объекта теряет ВСЮ пачку фактов, при том что правильный сканер уже есть рядом | `distiller.py:556` и `reclass.py:99`/`videosi.py:63` против `distiller.py:569` `_iter_json_objects` | ответ с текстом после объекта разбирается, а не отбрасывается |
| **P2** | здоровье ключей/моделей знает **один модуль из шести**: `banned_models.json` (4 записи) читает только `gemini_client.py` | `vision.py:296`, `gemini_knowledge.py:213`, `reclass.py:308`, `videosi.py:133` | забаненная модель не выбирается вторым путём |
| **P2** | границу отправки сводки **не удерживает ни один тест** (137 проверок молчат) | `summarizer.py:199` | клиент, спящий дольше бюджета, → отказ в срок |
| **P2** | `_env_int` продублирован байт-в-байт в 3 модулях, `_RU_SUFFIXES` — в 2 (25 строк, идентичны), `clean_json_raw` — в 2 | §5.1, §5.4 | один источник |
| **P2** | 113 записей журнала печатают только `str(исключения)` (`assistant.py` 64, `main.py` 24) — класс Н4 из `_recon_observe.md` не закрыт | §8.1 | пересчёт `deferred.py` даёт меньше |
| **P3** | долг не помечен в коде ни разу (0 `TODO/FIXME/HACK/XXX`), но **6 признаний долга с последствием для врача лежат прозой в docstring** | §4.2 | машиночитаемый префикс + инвентарь |
| **P3** | документация в коде противоречит коду в **двух** местах про `BOT_MENTION_SHADOW_MODE` | `assistant.py:3739`, `main.py:2131` против `assistant.py:3758` | — |
| **P3** | `benchmark.py` не разбирается `ast.parse` (BOM U+FEFF) — любой инвентарь дерева его молча теряет | §7.3 | число разобранных файлов = числу найденных |
| **P3** | `search_engine_safe.py` остался мёртв даже после сшивки поиска: `/web` зовёт `blocking_tools.web_search_async` напрямую | `assistant.py:3372` | реестр мёртвых инструментов обновлён осознанно |

### 10.3 Что осталось НЕ проверенным (честно)

* **Дерево двигалось всё время замера.** `assistant.py` перезаписан в 11:01, 12:39;
  `gemini_client.py` и `main.py` — в 12:10-12:11; `web_lookup.py` — 12:31; за
  сессию появились `taxonomy.py`, `test_dead_tools.py` (12:34),
  `test_search_command.py`, `test_taxonomy_single.py`. Все номера строк — на
  12:05-12:55, и к моменту чтения могут сдвинуться. Контрольный пересчёт в 12:55:
  280 обработчиков / 66 MUTE / 98 обращений без границ в `assistant.py` / 11
  модулей без покрытия — **стабильно** относительно 12:05.
* **Горячий путь посчитан графом вызовов ПО ИМЕНАМ**, без разрешения типов —
  верхняя оценка, как и у волны 2 и `_fix_observe.md`.
* **Покрытие = «имя модуля в `import` теста»** — тоже верхняя оценка. Реальное
  покрытие (`coverage.py` от Python) я не запускал.
* **Ни один дефект не наблюдён на живом боте**: бот работает на другой машине,
  `bot.log` не обновлялся с 2026-07-28 10:00. Дефекты §5.2 и §6.2 доказаны
  выполнением кода на стенде (`clipbug.py`, `diverge.py`) и чтением чисел, а не
  наблюдённым отказом у врача.
* **Частоту пути перекодирования аудио измерить нечем** (в архиве 0 реплик с
  `media_type` voice/audio) — ограничение то же, что у волны 2.
* `event.answer()` без срока: я не проверял ЭКСПЕРИМЕНТАЛЬНО, что telethon на нём
  спит до 500 с. Число 500 = `timeout=30 × request_retries=10` взято из замера,
  который сделал автор `assistant.py:44-49`, и я его не перепроверял.
* `run_all_tests.py` не запускался; о наборе целиком не утверждаю ничего.
