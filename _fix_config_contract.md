# Лана: контракт конфигурации

Файлы во владении: `config.example.py`, `test_config_contract.py` (создать).
Отчёт пишется инкрементально: каждая находка попадает сюда сразу после замера.

---

## Замер 1. Что именно читает код (AST по 113 файлам, не regex)

Инструмент замера — разбор через `ast` всех `*.py` в корне (113 файлов,
подкаталогов с кодом нет, проверено `fd -I -e py`). Regex по `config\.ИМЯ`
даёт мусор: `config.py` в комментарии совпадает как имя `py`,
`config.example.py` — как `example`, `config.json_loads` в docstring
`test_report_targets.py` — как `json_loads`. Ни одно из них не атрибут.
Поэтому и в замере, и в тесте используется AST: `ast.Attribute` над
`ast.Name` с учётом `ctx` (чтение против присваивания) плюс разбор
`getattr`/`hasattr` по числу аргументов.

**16 имён читаются как обязательные:**

```
API_HASH  API_ID  BOT_TOKEN  DB_PATH  GEMINI_MODEL  GOOGLE_KEYS  GROQ_KEYS
GROQ_MODEL  REPORT_CHAT_ID  REPORT_HOUR  REPORT_TARGETS  SEARCH_PROVIDER
SESSION_NAME  SOURCE_CHAT_ID  TAVILY_API_KEY  TELEGRAPH_TOKEN
```

Самые горячие: `SOURCE_CHAT_ID` — 29 обращений (27 в боевом коде),
`DB_PATH` — 17, `GOOGLE_KEYS` — 11.

**2 имени читаются только с запасным значением (необязательные):**

| Имя | Где | Как |
|---|---|---|
| `DENTAL_KEYWORDS` | `dental_vocab.py:92` | `getattr(config, "DENTAL_KEYWORDS", None)` — необязательное расширение словаря |
| `OPENROUTER_KEYS` | `gemini_client.py:293-294` | имя лежит в кортеже цикла, читается `getattr(config, attr, None)`; в живом `config.py` его нет вообще |

**Правило «обязательное против необязательного»** (оно же зашито в тест):

* `config.ИМЯ` в контексте чтения — **обязательное**. Нет имени → `AttributeError`.
* `getattr(config, "ИМЯ")` в два аргумента — **обязательное**: `getattr` без
  третьего аргумента бросает то же `AttributeError`.
* `getattr(config, "ИМЯ", запас)` в три аргумента — **необязательное**: у кода
  есть свой запас, отсутствие имени законно.
* `hasattr(config, "ИМЯ")` — **необязательное**: это прямая проверка на наличие.
* `config.ИМЯ = ...` (контекст `Store`) — **не чтение**: так тесты подменяют
  конфиг. В обязательные не идёт (таких имён, встречающихся только на
  присваивании, в дереве нет — все они и читаются тоже).

## Замер 2. Дыра в шаблоне до правки

Загрузка `config.example.py` через `importlib` проходит **без ошибки** — то есть
установка «по шаблону» не сообщает о проблеме ни звуком. Из 16 обязательных имён
в шаблоне не объявлено **3**: `API_ID`, `SOURCE_CHAT_ID`, `REPORT_TARGETS`.

Когда каждое из них укусит (замер по AST: уровень модуля против тела функции):

| Имя | Первое обращение | Когда падает | Что видит врач |
|---|---|---|---|
| `API_ID` | `main.py:888` — `TelegramClient(...)` на уровне модуля | при импорте `main.py` | бот не стартует, `AttributeError` в консоли — громко |
| `SOURCE_CHAT_ID` | `main.py:1776` — `WATCHED_CHATS = [...]` на уровне модуля | при импорте `main.py` | бот не стартует — громко |
| `REPORT_TARGETS` | `main.py:591` внутри `resolve_report_targets()` | в рантайме, **проглатывается** `except Exception` | бот работает, но ни дайджеста, ни недельной сводки не получает НИКТО из 749 врачей; в журнале одна строка ERROR |

Уточнение к формулировке задачи: «падает в рантайме, а не на старте» верно ровно
для `REPORT_TARGETS`. Два остальных имени валят **импорт** `main.py`, потому что
`TelegramClient` и `WATCHED_CHATS` собираются на уровне модуля. Хуже как раз
третий случай — молчаливый: `resolve_report_targets()` ловит любое исключение,
пишет ERROR и возвращает пустой список, так что бот выглядит живым.

## Замер 3. Имена, объявленные в шаблоне и не читаемые никем

`GROQ_VISION_MODEL`, `SUMMARY_INTERVAL`, `MAX_MSGS` объявлены и в живом
`config.py`, и в шаблоне, но `rg` по всему дереву не находит ни одного
обращения `config.ИМЯ` к ним — только сами строки объявления. Это мёртвый груз,
но он есть в ЖИВОМ конфиге, а задача — соответствие форме живого конфига,
поэтому из шаблона они не удаляются. Отмечено как долг, решение за лидом.

## Замер 4. Утечка в самом шаблоне (до правки)

В версионном `config.example.py` строкой 3 стоит `SESSION_NAME = "bot_session"` —
это имя файла сессии Telethon, и рядом в корне лежит `bot_session.session`
(139 264 байта), то есть имя не выдумано, а взято с рабочей машины.
`*.session` в `.gitignore`, а имя сессии — нет. Правкой убрано:
в шаблоне остаются только имена переменных окружения и пустые значения.

---

## Правка

`config.example.py` переписан по форме живого `config.py`: те же импорты,
`load_dotenv()`, функция `get_env(key, default, required)`, функция
`parse_keys()`, те же три блока валидации (int-приведение `API_ID`,
int-или-None для `SOURCE_CHAT_ID`, `json.loads` для `REPORT_TARGETS`), те же
два предупреждения (нет ни одного ключа нейросети; выбран Tavily без ключа).

Из живого `config.py` **не скопировано ни одно значение**: ни токена, ни ключа,
ни id чата, ни числа, ни имени сессии, ни имени модели. В шаблоне только имена
переменных окружения (`TG_API_ID`, `SOURCE_CHAT_ID`, `REPORT_TARGETS`, ...),
пустые значения нужного типа и русские комментарии с последствием для врача.
Значения, которые в версионном шаблоне были и раньше (`DB_PATH`,
`SUMMARY_INTERVAL`, `MAX_MSGS`, `REPORT_HOUR = 0`) сохранены как были —
`REPORT_HOUR = 0` намеренно, на него ссылаются комментарии `main.py:488` и
`test_fix_scheduler.py:12,194`.

Шаблон cp1251-безопасен: ни одного символа вне cp1251 (это уже сторожит
`test_report_targets.py`, проверка `[9]`, и она остаётся зелёной).

## Тест

`test_config_contract.py` — поведенческий, не текстовый:

1. Собирает контракт AST-разбором всех `*.py` (правило обязательности выше).
2. **Загружает** шаблон как модуль через `importlib` с поддельным окружением и
   проверяет `hasattr` каждого обязательного имени на настоящем объекте модуля.
   Это не «в исходнике есть такая строка», а «модуль после исполнения отдаёт
   атрибут» — то есть ровно то, что делает `import config` у врача.
3. Проверяет ТИПЫ и валидации поведением: пустой `REPORT_TARGETS` даёт список,
   битый JSON даёт список и не роняет импорт, нечисловой `TG_API_ID` даёт
   `SystemExit`, пустой `SOURCE_CHAT_ID` даёт `None`, `parse_keys` разбирает
   запятые.
4. Проверяет, что в шаблоне НЕТ значений: ни одной строки длиной >= 20 из
   алфавита ключей, ни отрицательного id чата, ни `.session`.
5. Мета-проверка с зубами: делает во временном каталоге копию шаблона с
   вырезанным обязательным именем и убеждается, что проверка (1)+(2) на ней
   ПАДАЕТ. Без этой проверки пункт (2) мог бы ничего не значить.

Результаты прогонов и саботажа — ниже, дописываются по ходу.

---

## Прогон 1 (после восстановления работы: предыдущий проход умер здесь)

`python test_config_contract.py` -> **PASSED: 80   FAILED: 0**.

## Замер 5. Перепроверка полноты сборщика (независимая, `_probe_cfgnames.py`)

Сборщик видит только `config.ИМЯ`, `getattr`/`hasattr`. Проверил, чем можно
прочитать конфиг ПОМИМО этого — то есть где контракт мог бы молча недосчитать:

| Способ обойти сборщик | Найдено в дереве |
|---|---|
| `from config import ИМЯ` (это `ImportFrom`, не `Attribute`) | **0** |
| `importlib.import_module("config")` / `__import__("config")` | 1, и это `_probe_silent.py:79` — черновик другого агента, не боевой код |
| `vars(config)` / `config.__dict__` / `dir(config)` | **0** (три совпадения — в моём же пробнике) |
| `.py` в подкаталогах (сборщик берёт `os.listdir`, не `os.walk`) | **0** |
| файлы, которые сборщик молча пропускает по `except SyntaxError` | **0 из 131** |

То есть на сегодняшнем дереве `Attribute`-разбора достаточно, и тихого недобора
нет. Файлов в корне уже 131 против 113 в замере 1 — параллельные агенты плодят
черновики, набор обязательных имён при этом не изменился (16).

**Слабое место, называю честно:** ноль в первой и последней строке таблицы — это
свойство сегодняшнего дерева, а не гарантия. Появится `from config import X` —
сборщик его не увидит, и тест промолчит. Появится файл с синтаксической ошибкой —
он выпадет из контракта без единого слова. Второе лечится дешево: заменить
`except SyntaxError: continue` на падающую проверку. Не делаю: это заденет
контракт с чужими файлами в момент, когда семь агентов пишут в дерево, и
непарсящийся черновик уронил бы мой тест по чужой причине.

## Замер 6. Провенанс: не собран ли контракт из мусорных черновиков

Тест берёт `os.listdir`, поэтому черновик `_probe_*.py` чужого агента может
добавить имя в контракт и уронить проверку по чужой причине. Проверил каждое из
16 имён на наличие хотя бы одного обращения в БОЕВОМ файле:

**У всех 16 есть боевое обращение — ни одно имя не пришло только из черновиков.**
Перекос заметен, но не опасен: `DB_PATH` — 17 обращений, из них боевых 4;
`GEMINI_MODEL` — 4 обращения, боевое 1; `REPORT_HOUR` — 5, боевых 2. Самые
горячие боевые: `SOURCE_CHAT_ID` 27, `GOOGLE_KEYS` 12, `API_HASH` 7, `API_ID` 7.

**Риск на будущее:** черновик, читающий `config.НОВОЕ_ИМЯ`, добавит его в
`REQUIRED`, и проверка «покрыто каждое обязательное имя» станет красной, хотя
шаблон ни при чём. Сейчас это не так, проверено. Разделение боевых и черновых
файлов в сам тест не зашивал: список префиксов черновиков — соглашение сегодняшнего
дня, а не правило репозитория, и зашивать его значит однажды тихо выкинуть из
контракта настоящий боевой файл.

## Замер 7. Поправка к замеру 2 (моя же прошлая формулировка была неточной)

Проверял глубину обращений сам, разбором `main.py` с раскруткой родителей до
`FunctionDef` и до `except Exception`. Результат частично **опровергает** строку
таблицы из замера 2:

| Имя | Первое ТЕКСТОВОЕ обращение | Первое обращение на УРОВНЕ МОДУЛЯ | Что на самом деле |
|---|---|---|---|
| `API_ID` | 888 (уровень модуля) | 888, 902 | падает импорт `main.py` — громко. Замер 2 верен |
| `SOURCE_CHAT_ID` | **1031, внутри `recover_pending_media_analysis()`, под `except Exception`** | 1776, 1782, 1784 | падает импорт всё равно, но «первое обращение — 1776» было НЕВЕРНО |
| `REPORT_TARGETS` | 591, внутри `resolve_report_targets()`, под `except Exception` | нет ни одного | молчаливый отказ рассылки. Замер 2 верен |

Из 16 обращений к `SOURCE_CHAT_ID` в `main.py` четыре стоят на уровне модуля, а
девять — внутри функций под `except Exception`. Вывод «бот не поднимется, зато
громко» держится (модульных обращений четыре, хватит и одного), но точная
формулировка теперь: не «первое обращение», а «первое обращение на уровне
модуля». Держу поправку в отчёте вместо того, чтобы затирать замер 2: цифра
1776 уже названа в докстринге теста, и лид должен видеть, что именно уточнено.

## Замер 8. Аудит утечки в шаблон (сверка литералов, значения в вывод не идут)

Сверил множества литералов живого `config.py` и шаблона. Латиницей совпадают
ровно четыре, и все четыре — структура, а не значение:

| Совпадение | Почему это не утечка |
|---|---|
| `,` | разделитель в `parse_keys` |
| `[]` | пустой JSON по умолчанию для `REPORT_TARGETS` |
| `stomat_bot.db` | относительное имя файла базы, было в версионном шаблоне и до правки, ни имени пользователя, ни секрета |
| `tavily` | условие ветки `if SEARCH_PROVIDER == "tavily"`, то есть часть валидации |

Совпадения кириллицей — тексты диагностических сообщений; это и есть та форма,
которую шаблон обязан повторять.

**Пять латинских литералов живого конфига (длины 10, 18, 20, 23, 45) в шаблон НЕ
попали** — это имя сессии, три имени моделей и провайдер поиска по умолчанию.
Ни один не назван ни здесь, ни в шаблоне. Целочисленных совпадений — только `1`.

---

## Саботаж: 30 диверсий, замечены все 30

Гонял так: порча одного файла -> прогон `test_config_contract.py` отдельным
процессом -> восстановление из памяти -> сверка md5. Контроль на целом шаблоне
перед каждым проходом: **87 проверок, 0 падений**.

### Проход 1 и 2 — порча шаблона (20 диверсий)

| Диверсия в `config.example.py` | Упало проверок |
|---|---|
| убрано `REPORT_TARGETS` (тот самый дефект: 749 врачей без рассылки) | 8 |
| убрано `SOURCE_CHAT_ID` | 8 |
| убрано `API_ID` | 6 |
| убрано `DB_PATH` | 6 |
| убрано `BOT_TOKEN` | 5 |
| убрано `GOOGLE_KEYS` | 30 |
| убрано `REPORT_HOUR` | 5 |
| убрано `API_HASH` | 5 |
| убрано `SESSION_NAME` | 5 |
| убрано `REPORT_CHAT_ID` | 5 |
| убрана функция `parse_keys` | 32 |
| убрана функция `get_env` (переименована везде) | 1 |
| зашито имя сессии с рабочей машины | 1 |
| зашита модель Gemini | 2 |
| вписано `.session` в значение | 2 |
| снят `int()` с `API_ID` | 4 |
| `SOURCE_CHAT_ID` по умолчанию `0` вместо `None` | 1 |
| эмодзи U+2705 в шаблоне | 1 |
| id чата подставлен в stdout | 1 |
| снято `required=True` с `TG_BOT_TOKEN` | 2 |
| убрано предупреждение про ключи нейросетей | 1 |
| убрано предупреждение про Tavily | 1 |
| убрано слово ПРЕДУПРЕЖДЕНИЕ у битого `SOURCE_CHAT_ID` | 1 |
| `REPORT_TARGETS` -> dict вместо list | 4 |
| снят `try/except` с `REPORT_TARGETS` | 3 |
| добавлен новый ключ `.env`, которого тест не знает | 1 |

Диверсия «убрана функция `get_env`» уронила ровно одну проверку — и это
правильная одна: шаблон продолжает работать, у него просто другое имя функции,
и краснеет именно «`get_env` и `parse_keys` есть в шаблоне».

### Проход 3 — порча САМОГО ТЕСТА (4 диверсии)

Проверки в блоках `[1]`, `[8]`, `[9]` сторожат не шаблон, а сборщик контракта.
Порчей шаблона их уронить нельзя по определению, поэтому ломал тест:

| Диверсия в `test_config_contract.py` | Упало | Итог |
|---|---|---|
| сборщик отдаёт пусто (`config_aliases -> set()`) | 14 | замечено |
| мутация мета-блока сломана (`rename_assignment` возвращает вход) | 5 | замечено |
| `lenient` всегда True (любой `getattr` считается необязательным) | **0 -> стало 1** | сначала НЕ ЗАМЕЧЕНО |
| снята проверка `ast.Load` (присваивание идёт в обязательные) | **0 -> стало 1** | сначала НЕ ЗАМЕЧЕНО |

## Две настоящие дыры, которые нашёл саботаж (и почему они были)

Последние две диверсии **не уронили ни одной проверки из 80**. Это ровно тот
случай, про который сказано «проверка, которая не падает на сломанном коде, не
существует» — и вот причина, замеренная, а не угаданная:

* обращений вида `getattr(config, "ИМЯ")` **в два аргумента в дереве 0**;
* присваиваний `config.ИМЯ = ...` **58**, но все они трогают имена, которые код
  и так читает (`DB_PATH`, `REPORT_CHAT_ID`, `GOOGLE_KEYS`), поэтому набор
  обязательных от снятия проверки `ast.Load` не менялся.

То есть правило «обязательное против необязательного» — главное, что просили
сделать осмысленно — было НАПИСАНО, но не ОХРАНЯЛОСЬ: обе его ветки на
сегодняшнем дереве не исполняются. Сломай классификацию, и контракт молча начнёт
либо требовать от шаблона лишние имена, либо прощать пропущенные.

**Правка:** вынес разбор одного файла в `classify_tree()` и добавил
`classify_source()` — тот же разбор по тексту. Новый блок `[1a]` проверяет правило
на СИНТЕТИКЕ, а не на том, что случайно оказалось в дереве: 7 проверок на
`config.ИМЯ` на чтение, `getattr` в 2 и в 3 аргумента, `hasattr`,
`config.ИМЯ = ...`, `import config as cfg`, и имя, читаемое и с запасом и без.
После правки обе диверсии краснеют. Проверок стало 87.

## Ещё одна правка от саботажа: подсказка вела в мусорный файл

Диверсии 2 и 3 показали текст `[FAIL] шаблон объявляет API_ID -- читается в
delme.py:6` и `... _bak_assistant_tgdelivery.py:5461`. Подсказка брала первое
обращение по алфавиту и отправляла человека чинить `delme.py` вместо `main.py`.
Первая попытка (откинуть файлы по префиксу `_`, `delme`, `test_`) дала
`deppd.py:61` — то есть префиксы угадать нельзя. Замерил распределение: у всех
имён больше всего обращений в `main.py` (у `DB_PATH` — в `database.py`), поэтому
подсказка теперь берёт файл с НАИБОЛЬШИМ числом обращений. Стало
`читается в main.py:888, всего обращений 7`. На вердикт не влияет — только текст;
обязательность по-прежнему считается по всем файлам, иначе боевой файл однажды
тихо выпал бы из контракта.

## Прогоны (финальные)

| Тест | Проверок | Падений |
|---|---|---|
| `test_config_contract.py` (мой) | 87 | 0 |
| `test_import_safety.py` | 257 | 0 |
| `test_report_targets.py` | 25 | 0 |
| `test_fix_scheduler.py` | 61 | 0 |
| `test_startup_boot.py` | 51 | 0 |

`run_all_tests.py` не запускал: его md5-охрана даёт ложную тревогу, пока в дерево
пишут параллельные агенты.

`config.example.py` после всех 30 диверсий побайтово равен исходному:
md5 `460db6ecbcacf7b535ff35812b0244b9`, сверено с бэкапом в `C:\temp\cfgbak\`.

## Черновики убраны

Пробники и драйверы саботажа (`_probe_cfgnames*.py`, `_probe_cfgleak.py`,
`_sabotage_cfgcontract*.py`) удалены. Причина не косметическая: драйверы саботажа
пишут в `config.example.py` НА УРОВНЕ МОДУЛЯ, то есть это тот самый класс, что
`patch_assistant.py` — импорт такого файла портит продакшн-файл, а прерванный на
середине импорт оставил бы шаблон сломанным. Защиты `raise SystemExit` в них не
было, и оставлять их в дереве нельзя. Метод и все числа записаны здесь.

## Что осталось НЕ проверенным — честно

1. **Бот с этим шаблоном не поднимался.** Ни `main.py` не импортировался, ни
   Telethon не подключался: бот живёт на другой машине, а `config.py` здесь —
   свой. Доказано, что шаблон ИСПОЛНЯЕТСЯ и отдаёт все 16 имён с правильными
   типами. НЕ доказано, что бот на чужой машине с ним взлетает.
2. **Полнота сборщика — свойство сегодняшнего дерева, а не гарантия.**
   `from config import ИМЯ` в дереве 0, и такой способ сборщик не увидит вовсе.
   Появится — тест промолчит.
3. **`except SyntaxError: continue` глотает непарсящиеся файлы молча.** Сейчас
   таких 0 из 131. Файл с синтаксической ошибкой выпадет из контракта без
   единого слова. Не лечил: семь агентов сейчас пишут в дерево, и чужой
   непарсящийся черновик уронил бы мой тест по чужой причине.
4. **Затенение имени `config` даёт фантомные имена.** В `_probe_silent.py` есть
   локальная переменная `config` (это `GenerationConfig` от Gemini), и её
   атрибуты `response_mime_type`, `response_schema` попали в контракт. Сейчас они
   приходят из `getattr` с запасом, то есть в НЕобязательные, и шаблона не
   касаются. Если такое затенение появится с обычным `config.attr` на чтение,
   тест потребует от шаблона фантомное имя. Сделал видимым печатью (не
   проверкой — проверка краснела бы от чужого черновика).
5. **Контракт растёт вместе с деревом.** Тест берёт `os.listdir`, поэтому
   черновик другого агента, читающий `config.НОВОЕ`, уронит проверку «покрыто
   каждое обязательное имя», хотя шаблон ни при чём. Сейчас все 16 имён имеют
   боевое обращение — проверено, но это свойство момента.
6. **Тайминги не мерил** — в этой лане их нет.

---

## Для лида (правки ВНЕ моих файлов, сам не делал)

**1. `.env.example` не существует, хотя `.gitignore` его специально разрешает.**
Строка 4 `.gitignore` — `!.env.example`, то есть исключение из-под `.env.*`
написано под файл, которого в репозитории НЕТ (проверено `ls`). Это вторая
половина той же дыры, что и моя лана: я привёл в порядок форму `config.py`, но
имена переменных ОКРУЖЕНИЯ версионного шаблона по-прежнему не имеют. Сейчас их
можно вычитать только из вызовов `get_env(...)` в моём `config.example.py` — все
16 там названы. Создать `.env.example` не мой файл; в нём нужны только имена и
пустые значения, значений с рабочей машины там быть не должно.

**2. Три имени объявлены в живом `config.py` и не читаются НИКЕМ:**
`GROQ_VISION_MODEL`, `SUMMARY_INTERVAL`, `MAX_MSGS`. В шаблоне я их сохранил —
задача была «соответствие форме живого конфига», и молча выкидывать из шаблона то,
что есть в живом файле, значит создать новое расхождение. Если это мёртвый груз,
убирать надо из ОБОИХ файлов одной правкой, иначе мой тест `[7]` покраснеет.

**3. Два имени код читает, а в живом `config.py` их нет вовсе:**
`DENTAL_KEYWORDS` (`dental_vocab.py:92`) и `GEMINI_KNOWLEDGE_MODELS`
(`gemini_knowledge.py:59`), плюс `OPENROUTER_KEYS` (`gemini_client.py:294`, имя
берётся из кортежа цикла). Все три — `getattr` с запасом, поэтому падения нет, но
это значит, что соответствующие куски работают на встроенном значении ВСЕГДА и
настройкой не управляются. Если хоть один задумывался настраиваемым — имя нужно
добавить в `config.py`, и тогда же в шаблон.

**4. `main.py:212` и `main.py:216` присваивают `config.REPORT_CHAT_ID` в рантайме.**
Боевой код меняет модуль конфигурации на ходу. Само по себе может быть намеренным,
но это единственное такое место среди 58 присваиваний `config.ИМЯ = ...` (все
остальные — в тестах), поэтому называю.

**5. Контракт раздувают мёртвые файлы.** `config.API_ID` читают, помимо `main.py`,
ещё `delme.py`, `deppd.py`, `id.py`, `inspector.py`, `visionproc.py`, а
`config.SOURCE_CHAT_ID` — `_bak_assistant_tgdelivery.py`. Файл буквально с именем
`delme.py` участвует в контракте конфигурации. Пока они в дереве, любое имя,
которое они читают, мой тест считает обязательным.

**6. `_probe_silent.py` (файл другого агента) затеняет имя `config`** локальной
переменной — это `GenerationConfig` от Gemini. Из-за этого в анализ контракта
попадают `response_mime_type` и `response_schema`. Сейчас безвредно (оба через
`getattr` с запасом, шаблона не касаются), но любой разбор конфигурации по этому
дереву будет спотыкаться об это же.

**7. Моё сознательное решение, которое можно отменить одной строкой.** В сборщике
контракта стоит `except SyntaxError: continue` — непарсящийся файл выпадает из
контракта молча. Оставил мягким специально: пока в дерево пишут семь агентов,
чужой сломанный черновик уронил бы мой тест по причине, не связанной с шаблоном.
Когда параллельная волна закончится, эту строку стоит сделать падающей проверкой —
это 2 строки, и я рекомендую это сделать.
