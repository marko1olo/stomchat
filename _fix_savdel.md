# savdel.py — таксономия выгрузки (лана savdel)

Файлы лана: `savdel.py`, `test_savdel_taxonomy.py`.
Боевые базы открывались только `mode=ro`; ни одного INSERT/UPDATE/DELETE/VACUUM не выполнялось.
Отчёт писался по ходу работы, а не в конце.

---

## Замеры на живых данных (до правки)

Инструменты замера: `_measure_savdel.py`, `_measure_savdel2.py`, `_measure_savdel3.py` (одноразовые,
`mode=ro`; удаляются в конце работы, чтобы не мусорить в diff лида).

| Что | Число |
|---|---|
| фактов в `stomat_wiki.db` `distilled_facts` | 12 784 |
| фактов с мульти-тегом (несколько кодов через запятую) | 12 667 |
| различных **сырых** значений `category_code` | 6 711 |
| различных **кодов** после разбора по запятой | 110 |
| кодов в `CAT_MAP` | 52 |
| кодов базы, отсутствующих в `CAT_MAP` | **58** |
| фактов, не попадающих НИ В ОДИН файл выгрузки | **51** |
| реплик в `stomat_archive.db` `archive_messages` | 117 847 |

`category_code` в боевой вики оказался чистым: 0 NULL, 0 табов/переводов строки, из символов только
цифры, точки, запятые и пробелы. Это важно для правки Д2 — нормализация может ограничиться пробелами.

---

## Д1. `CAT_MAP` не покрывает таксономию: 58 кодов мимо, 51 факт невидим

### Что заявлено в задаче и что оказалось на самом деле

Задача: «CAT_MAP не содержит кодов 8.1.1, 9.1.1, 10.1.1 → 115 фактов детской стоматологии и
материаловедения не попадают в выгрузку».

**Числа 115 в текущей вике нет, и кодов 8.1.1 / 9.1.1 / 10.1.1 в ней нет вообще — ни одного факта.**
Замер по `stomat_wiki.db`: кодов с L1 = 8, 9 или 10 всего один — `10.1` (5 присвоений).

Откуда взялось 115: это замер из `_recon_distill.md` по **до-реклассовому** снимку
`backup_wiki_18_0140.db` — 80 фактов с L1=8 (Детская) + 35 с L1=9 (Материаловедение) = 115. Их
уничтожил `reclass.py`, чьё дерево «5.0» заканчивается на разделе 7: он переклеил их в 1–7 ещё до
того, как дело дошло до выгрузки. `savdel.py` тут ни при чём, и добавление трёх кодов в `CAT_MAP`
сегодня вернуло бы **0 фактов** — все три файла напечатали бы «пусто».

Это не значит, что коды не нужны: `distiller.py:283` объявляет `FALLBACK_CODE = "10.1.1"`, а `8.1.1`
и `9.1.1` — легальные листья его дерева (`distiller.py:259-264`). Как только дистилляция пойдёт
дальше, факты с этими кодами начнут появляться, и без них в `CAT_MAP` они молча пропадут. Добавляю их
как страховку на будущее и честно фиксирую: **на сегодняшних данных они пустые**.

### Настоящее последствие Д1 — 58 кодов, а не 3

Полный список кодов, которые есть в боевой вике и отсутствуют в `CAT_MAP`, с числом присвоений:

**L3-листья (реальная потеря содержимого):**
`6.1.2` — 82, `7.2.2` — 19, `7.1.2` — 13, `2.5.1` — 9, `2.1.5` — 5, `3.1.6` — 5, `7.1.3` — 5,
`2.3.6` — 3, `2.6.1` — 3, `7.3.3` — 3, `3.2.6` — 2, `2.6.3` — 2, `6.1.3` — 2, `3.1.4` — 1,
`2.6.2` — 1, `2.3.4` — 1, `6.3.3` — 1, `6.5.1` — 1.

**Коды-заглушки `x.y.0` и `x.0.0` (брак классификатора):**
`2.0.0` — 19, `1.1.0` — 17, `1.0.0` — 13, `3.0.0` — 13, `1.2.0` — 4, `2.1.0` — 4, `3.2.0` — 4,
`6.0.0` — 3, `4.0.0` — 3, `1.3.0` — 2, `7.1.0` — 2, `2.4.0` — 1, `2.3.0` — 1, `5.0.0` — 1,
`7.0.0` — 1, `3.1.0` — 1.

**Коды уровня L2 (в `CAT_MAP` только L3, поэтому мимо):**
`1.1` — 21, `2.1` — 18, `1.2` — 13, `2.2` — 12, `2.0` — 11, `3.2` — 10, `2.6` — 8, `6.3` — 7,
`2.3` — 7, `7.1` — 6, `7.3` — 5, `10.1` — 5, `3.1` — 5, `2.4` — 4, `6.1` — 3, `2.5` — 2, `1.6` — 2,
`1.3` — 1, `4.1` — 1, `3.0` — 1, `6.2` — 1, `7.2` — 1, `1.4` — 1.

Большинство этих присвоений — вторые/третьи теги у фактов, у которых есть и «хороший» код, поэтому
факт всё-таки попадает в какой-то файл. **Полностью невидимы для выгрузки 51 факт** — те, у которых
НИ ОДИН код не покрыт `CAT_MAP`. Из них 5 сидят на `10.1` (фолбэк `reclass.py`), остальные 46 — на
заглушках `x.y.0`/L2. Совпадает с замером в комментарии `distiller.py:274` («51 факт»), т.е. лана
дистилляции пришла к тому же числу независимо.

**Последствие для врача:** 51 разобранный факт не существует ни в одном файле ревью — их никто не
прочитает и не заметит, что их нет. Ещё 58 кодов не имеют имени, поэтому даже при ручном разборе
непонятно, что за раздел потерян.

---

## Д2. `LIKE '%{cat}%'` — совпадение подстрокой. Измерено: 51 факт в 14 чужих файлов

`savdel.py:89-90` подставлял `f'%{cat_code}%'`. Точка в `LIKE` — обычный символ, границы токена нет.

На **текущей** вике столкновений **0** — потому что `reclass.py` выровнял все коды до одноразрядных
сегментов. Двузначных сегментов в ней не осталось ни одного, поэтому «на живой базе сегодня дефект не
проявляется». Если бы я остановился здесь, вывод был бы «дефект теоретический». Он не теоретический.

Тот же запрос на `backup_wiki_18_0140.db` (12 691 факт, тот же конвейер, та же схема, снимок до
реклассификации) даёт **51 факт, разложенный по 14 чужим файлам**:

| Файл категории | Лишних фактов | Чей код на самом деле |
|---|---|---|
| `1.3.1` Профгигиена_GBT | 21 | `1.3.10`, `1.3.11`, `1.3.12`, `1.3.13` |
| `3.1.1` Хирургия_Удаление | 5 | `1.3.1.1` (совпадение в середине строки) |
| `1.1.1` Эндо_Доступ_МБ2 | 5 | `11.1.1` — другой раздел верхнего уровня |
| `3.3.1` Пластика_Десны | 5 | `3.3.10` |
| `2.3.1` Гнатология_Окклюзия | 4 | `2.2.3.1` |
| `3.1.2` Хирургия_Апикальная | 3 | `1.3.1.2` |
| `3.1.3`, `1.1.3`, `1.2.1`, `2.1.1`, `2.1.2`, `2.3.2`, `2.2.3`, `5.1.1` | по 1 | `1.3.1.3`, `11.1.3`, `11.2.1`, `12.1.1`, `12.1.2`, `12.3.2`, `2.2.3.1`, `5.1.10` |

Видны **три** независимых направления одного дефекта, а не одно из задачи:
1. хвост — `1.3.1` затягивает `1.3.10` (это и есть заявленный «2.1.1 в 2.1.10»);
2. голова — `1.1.1` затягивает `11.1.1`, то есть код чужого раздела верхнего уровня;
3. середина — `2.3.1` затягивает `2.2.3.1`, совпадение начинается со второго символа.

Двузначные сегменты в том снимке реальны, а не выдуманы: `1.3.10`(8), `1.3.11`(3), `1.3.12`(1),
`1.3.13`(1), `1.4.10`(1), `2.3.10`(2), `2.3.11`(1), `3.10.1`(6), `3.10.2`(4), `3.11.1`(5),
`3.12.1`(2), `3.13.1`(2), `3.14.1`(1), `3.3.10`(5), `5.1.10`(1). Модель их придумывала — именно
поэтому `distiller.py:730` отдельным пунктом промпта запрещает «создавать свои индексы (1.10.1)».
Запрет в промпте — не гарантия: снимок показывает, что она их всё равно создавала.

**Последствие для врача:** он открывает файл «Профгигиена GBT» и читает в нём 21 факт из другого
раздела как относящийся к профгигиене. Это хуже потери: потерянного факта не видно, а подложенный
читается как проверенный.

---

## Д3. Путь базы подставлялся в текст SQL. Измерено: кавычка в пути роняет весь экспорт

`savdel.py:79`: `await db.execute(f"ATTACH DATABASE '{ARCHIVE_DB}' AS archive")`.

Замер (`_measure_savdel3.py`, временная база в каталоге `q'uote_.../arch'ive.db`):

* f-строка: `OperationalError: near "uote_en08grpk": syntax error` — падает целиком;
* параметр `ATTACH DATABASE ? AS archive`: возвращает данные, кавычка в пути безвредна.

Проверено, что параметр в `ATTACH` вообще допустим и что ro-URI через `ATTACH` действительно
удерживает режим чтения — на боевых базах:

* `ATTACH` с `file:...?mode=ro` открылся, `archive_messages` = 117 847 строк;
* `UPDATE` в main → `attempt to write a readonly database`;
* `UPDATE` в `archive.` → `attempt to write a readonly database`.

То есть после правки экспорт физически не может испортить ни вику, ни архив — а до правки основная
база открывалась на запись без всякой на то нужды: `savdel.py` только читает.

**Последствие для врача:** экспорт целиком не запускается (ни одного файла ревью), причём сообщение
об ошибке указывает на середину пути, а не на путь, — час на диагностику вместо секунды.

---

## Побочная находка (не правил как дефект, отмечаю честно)

`savdel.py:113`: `if x.strip().isdigit()` для `source_ids`. `str.isdigit()` истинен для не-ASCII
цифр: `'²'.isdigit()` → `True`, `'٣'.isdigit()` → `True` (проверено вживую). Такой «id» проходил
фильтр и попадал в `IN (...)`, собранный склейкой строк, → `OperationalError` на всём экспорте.
Замер по боевой вике: таких токенов сейчас **0**, NULL в `source_ids` тоже 0. Дефект латентный.
Правку сделал заодно, потому что она в один символ (`.isascii() and .isdigit()`) и убирает склейку
SQL в том же месте, где Д3.

---

## Д4 (нашёл сам, не было в задаче). `python savdel.py > log` падает на первом же print

`savdel.py` печатал 7 символов, которых нет в cp1251: `🚀 📦 ✅ 🌟 🏁 📍 📷`, и не имел
`reconfigure(encoding=...)`.

Замер на этой машине: при перенаправленном или конвейерном stdout `sys.stdout.encoding` = **cp1251**
(проверено: `python -c "import sys; print(sys.stdout.encoding)"` через конвейер → `cp1251`), и
`print('\U0001F680')` даёт `UnicodeEncodeError: 'charmap' codec can't encode character '\U0001f680'`.
То есть `python savdel.py > export.log` — обычный способ запустить выгрузку и сохранить журнал —
умирал на строке 76, **до** первого обращения к базе: ноль файлов ревью, а в журнале трейсбек про
кодировку вместо причины. В интерактивной консоли Windows тот же скрипт работает (Python пишет в
консоль через `WriteConsoleW`), поэтому дефект и не был замечен: он проявляется только там, где
вывод сохраняют.

Правка: тот же блок `reconfigure(encoding="utf-8", errors="replace")`, что стоит в тестах набора.
Новый вывод про свалку намеренно печатается без эмодзи — он должен быть виден и если сам
`reconfigure` не удался.

---

## Что изменено в `savdel.py`

| Место | Было | Стало |
|---|---|---|
| шапка модуля | — | `reconfigure(utf-8)` для stdout/stderr (Д4) |
| `CAT_MAP` | 52 кода | 55: добавлены `8.1.1`, `9.1.1`, `10.1.1` (Д1) |
| `CODE_RE` | — | код категории только цифры/точки, 2–4 уровня |
| `read_only_uri()` | — | `file:///...?mode=ro` для обеих боевых баз (Д3) |
| `category_patterns()` | `f'%{cat}%'` | `('%,код,%', '%,код.%')` — токен и подкоды (Д2) |
| `WHERE` | `category_code LIKE ?` | `(',' \|\| REPLACE(category_code,' ','') \|\| ',') LIKE ?` ×2 |
| `ATTACH` | f-строка с путём | `ATTACH DATABASE ? AS archive` + ro-URI (Д3) |
| `SELECT` категории | без `id` | c `f.id` — чтобы знать, что осталось вне карты |
| `IN ({ids_sql})` | склейка id в текст SQL | `IN (?,?,…)`, id параметрами |
| фильтр id | `.isdigit()` | `.isascii() and .isdigit()` (побочная находка) |
| после цикла | — | свалка `00_НЕРАЗОБРАННОЕ.md` + громкая строка в выводе |

Свалка — единственное добавление сверх трёх заявленных дефектов. Причина: добавление трёх кодов из
задачи не возвращает **ни одного** факта (проверено прогоном: все три печатают «пусто»), а
измеренная потеря — 51 факт на кодах, которых в каноническом дереве нет вообще. Придумывать им
названия было бы выдумыванием таксономии, поэтому они уходят в один файл вместе со своими сырыми
кодами. Расширение `.md`, а не `.txt`, выбрано потому, что `prompter.py:19` забирает из этой папки
каждый `.txt` и заказывает по нему монографию у Гемини — по мусорному коду это платный запрос в
никуда.

Проверено, что добавленные `8.1.1/9.1.1/10.1.1` — это ровно те легальные листья, которых не хватало,
а не отсебятина: дерево `distiller.py` даёт 55 L3-листьев, и разница с `CAT_MAP` была ровно
`['10.1.1', '8.1.1', '9.1.1']`; дерево `reclass.py` даёт 52 — совпадает с прежним `CAT_MAP` до кода.

## Прогон настоящего экспорта по боевым базам (`mode=ro`)

`export_v7()` запущен целиком, вывод уведён во временный каталог (в `wiki_final_review` не писал):

* 53 файла: 52 категории + свалка;
* `8.1.1`, `9.1.1`, `10.1.1` → «пусто» — как и предсказано замером;
* `[!] Вне карты: 51 фактов, 35 кодов -> 00_НЕРАЗОБРАННОЕ.md` (35 — это коды, встречающиеся именно
  у невидимых фактов; всего кодов без имени в базе 58, остальные 23 живут рядом с «хорошим» кодом);
* суммарно 41 975 записей по файлам (с повторами: 12 667 фактов имеют несколько тегов);
* время 42,3 с;
* `mtime` и md5 `stomat_wiki.db` / `stomat_archive.db` после прогона не изменились.

---
---

# Волна 2: перепроверка волны 1 и закрытие её главного пробела

Волна 1 оставила `savdel.py` изменённым, но **не создала `test_savdel_taxonomy.py` и не делала
саботажа**. То есть ни одна из трёх правок не была закреплена проверкой: любой следующий агент мог
снять границу токена или откатить `ATTACH ?`, и об этом не узнал бы никто. Волна 2 занимается
ровно этим и заново измеряет всё, на что волна 1 ссылалась числами.

Замеры волны 2 делались скриптами **вне репозитория** (`C:\temp\savdel_probe\probe1..3.py`), чтобы
не попасть в `os.listdir()` набора тестов и в diff лида. Все боевые базы — `mode=ro`.

## Что из отчёта волны 1 подтвердилось

| Утверждение волны 1 | Мой замер | Итог |
|---|---|---|
| фактов в вике 12 784 | 12 784 | сходится |
| сырых значений `category_code` 6 711 | 6 711 | сходится |
| токенов 110, в `CAT_MAP` 52, вне карты 58 | 110 / 52 / 58 | сходится |
| полностью невидимых фактов 51, кодов у них 35 | 51 / 35 | сходится |
| реплик в архиве 117 847 | 117 847 | сходится |
| `category_code` чистый: 0 NULL, только цифры/точки/запятые/пробелы | 0 грязных, 0 пустых | сходится |
| `8.1.1`/`9.1.1`/`10.1.1` в текущей вике дают 0 фактов | 0 / 0 / 0 | сходится |
| столкновений подстрокой в текущей вике 0 | 0 | сходится |
| «115 фактов» — это `backup_wiki_18_0140.db`, L1=8 (80) + L1=9 (35) | 80 + 35 = 115 | сходится |
| ro через `ATTACH ?` физически держит запись | `UPDATE` в main и в `archive.` → `attempt to write a readonly database` | сходится |

Вывод по заявленному Д1: **в задаче число 115 указано по чужому снимку.** В боевой вике
`stomat_wiki.db` фактов на кодах 8.x/9.x нет вообще, единственный код десятого раздела — `10.1` (5
присвоений), и он в `CAT_MAP` по-прежнему отсутствует (в карту добавлен `10.1.1`, а не `10.1`).

## Что из отчёта волны 1 НЕ подтвердилось

Волна 1 заявила по Д2: «**51 факт, разложенный по 14 ЧУЖИМ файлам**», и в таблице приписала файлу
`1.3.1` **21** лишний факт. Мой пересчёт того же снимка `backup_wiki_18_0140.db` (12 691 факт),
сравнение старого `LIKE '%код%'` с новым токенным запросом по каждому из 55 кодов `CAT_MAP`:

* лишних **присвоений** — **42**, не 51;
* чужих файлов — **13**, не 14;
* уникальных фактов, попавших хоть в один чужой файл — **33**;
* фактов, которые новый запрос потерял бы по сравнению со старым — **0**.

У файла `1.3.1` лишних **13**, а не 21 — причём это видно из самой таблицы волны 1: перечисленные
там же коды дают 8+3+1+1 = 13. Число 51 в её таблице — это, судя по совпадению, случайно
перенесённое число невидимых фактов из Д1 (там 51 действительно верно). **Проверять надо было
числа, а не только правку.** Сама правка Д2 корректна, ошибочна только её метрика.

Разбивка по чужим файлам (мой замер, снимок до реклассификации):

| Файл категории | Лишних присвоений | Чьи коды на самом деле |
|---|---|---|
| `1.3.1` Профгигиена_GBT | 13 | `1.3.10`(8), `1.3.11`(3), `1.3.12`(1), `1.3.13`(1) |
| `1.1.1` Эндо_Доступ_МБ2 | 5 | `11.1.1`(5) — чужой раздел верхнего уровня |
| `3.1.1` Хирургия_Удаление | 5 | `1.3.1.1`(4), `13.1.1`(1) |
| `3.3.1` Пластика_Десны | 5 | `3.3.10`(5) |
| `2.3.1` Гнатология_Окклюзия | 4 | `2.3.10`(2), `2.3.11`(1), `2.2.3.1`(1) |
| `3.1.2` Хирургия_Апикальная | 3 | `1.3.1.2`(3) |
| `1.1.3`, `1.2.1`, `2.1.1`, `2.1.2`, `2.3.2`, `3.1.3`, `5.1.1` | по 1 | `11.1.3`, `11.2.1`, `12.1.1`, `12.1.2`, `12.3.2`, `1.3.1.3`, `5.1.10` |

Три направления дефекта (хвост `1.3.1`←`1.3.10`, голова `1.1.1`←`11.1.1`, середина
`2.3.1`←`2.2.3.1`) подтверждаются — это в отчёте волны 1 верно.

## Насколько добавление трёх кодов вообще закрывает Д1 (уточнение)

Волна 1 написала «добавляю как страховку, сегодня они пустые» — верно, но не измерила, какую долю
раздела эти три кода покрывают. Замер по `backup_wiki_18_0140.db`:

* раздел 8 — **13 разных L3-кодов**, 80 присвоений; `8.1.1` из них покрывает **37**;
* раздел 9 — **11 разных L3-кодов**, 35 присвоений; `9.1.1` покрывает **12**;
* раздел 10 — **8 разных L3-кодов**, 24 присвоения; `10.1.1` покрывает **10**;
* фактов, у которых **все** коды лежат в 8/9/10 (то есть исчезли бы целиком) — **139**.

Итого три добавленных кода вернули бы 59 присвоений из 139 таких фактов. **Формулировка задачи
«дополни CAT_MAP тремя кодами» закрывает меньше половины раздела.** Остальные 29 кодов
(`8.1.2`…`8.4.2`, `9.1.2`…`9.4.3`, `10.2.1`…`10.4.2`) имён по-прежнему не имеют; на сегодняшних
данных их 0, поэтому я их не выдумываю, а фиксирую как известный предел правки.

## Проверенный и отброшенный след (чтобы не искали второй раз)

`IN ({holders})` в блоке фото собирает столько `?`, сколько id в `source_ids`. Если бы их было больше
предела SQLite, экспорт падал бы целиком. Замер: **максимум 100 id** на факт (и в боевой вике, и в
снимке), фактов с >999 id — **0**, `SQLITE_LIMIT_VARIABLE_NUMBER` на этой сборке (SQLite 3.50.4) —
**32 766**. Запас 327×. Дефекта нет, догадка отброшена замером, а не рассуждением.

## Д6 (нашла волна 2, исправлено). Опустевшая категория оставляет файл на диске

`savdel.py` печатал `пусто` и шёл дальше, **не снимая файл предыдущей выгрузки**. Имя файла
вычислялось уже после этой проверки, поэтому снять его было нечем.

Почему это не гипотеза, а измеренная история: снимок `backup_wiki_18_0140.db` содержит **139 фактов,
все коды которых лежат в разделах 8/9/10**, и `reclass.py` вычистил эти разделы целиком — в боевой
вике их сейчас 0. То есть категория, у которой были факты, **уже один раз опустела на реальных
данных**. Если бы выгрузка прошла до реклассификации, `8.1.1_Детская_Прием_детей.txt` с 37 фактами
лежал бы в `wiki_final_review` до сегодняшнего дня.

**Последствие для врача:** `prompter.py:19` берёт из этой папки **каждый** `.txt` и заказывает по нему
платную монографию у Гемини. Врач получает методичку по фактам, которых в вике больше нет, и ничем не
отличает её от актуальной — а деньги за запрос уже уплачены. Отличить нельзя даже по дате: файл
выглядит как все остальные.

Правка: имя файла считается **до** проверки на пустоту, и пустая категория снимает свой файл, громко
сообщая об этом в вывод. Удаляется только тот путь, который `savdel.py` сам и пишет (имя собрано из
`CAT_MAP`), поэтому чужие файлы задеть нечем. Тем же куском снимается устаревшая свалка, когда
невидимых фактов больше нет.

---

# Проверка: `test_savdel_taxonomy.py` (29 проверок)

Это то, чего волна 1 не сделала вообще. Тест поведенческий: он гоняет **настоящий `export_v7()`** по
временным базам и читает получившиеся файлы — то есть проверяет ровно то, что увидит врач. Ни одной
проверки вида «в исходнике есть такая строка».

Стенд: временная вика и временный архив, **схема прочитана из боевых баз** через `mode=ro`
(`SELECT sql FROM sqlite_master`), поэтому стенд не может разойтись с реальной схемой. Временный
каталог намеренно называется `savdel_q'uote_100%_…` — апостроф и процент в пути и есть проверка Д3.
В боевую вику тест не пишет: единственное обращение к ней — `mode=ro` для схемы и контрольного замера.

Что доказано (по группам):

* **Д2, граница токена, 9 проверок.** `2.1.1` попадает в свой файл; `2.1.10` в него **не** попадает
  (хвост); `12.1.1` не попадает (голова); `2.2.3.1` не попадает в `2.3.1` (середина), но попадает в
  файл своего настоящего родителя `2.2.3`; подкод `2.1.1.5` читается вместе с родителем; мультитег
  `1.1.1, 2.1.1` виден в **обоих** файлах; код с пробелами по краям находится.
* **Д1, 6 проверок.** Каждый из `8.1.1`/`9.1.1`/`10.1.1` даёт непустой файл с фактом внутри. Факты с
  безымянными кодами (`2.1` — L2, `3.0.0` — заглушка) уходят в свалку, их коды **названы в её шапке**,
  и в выводе есть строка с числом. Отдельная проверка: коды, отвергнутые границей токена (`2.1.10`,
  `12.1.1`), не теряются, а тоже попадают в свалку — иначе правка Д2 превратила бы подлог в пропажу.
* **Д3, 4 проверки.** Экспорт из каталога с апострофом и `%` доходит до конца и подтягивает фото через
  `ATTACH`; `read_only_uri` физически отклоняет `UPDATE`; код с кавычкой, `_`, `%`, пустой и `abc`
  отвергаются `ValueError` до похода в SQL.
* **Д6/Д5, 5 проверок.** Файл опустевшей категории снят с диска, об этом сказано в выводе, соседние
  категории не пострадали; свалка не имеет расширения `.txt`.
* **Кодировка.** Подпроцесс с `PYTHONIOENCODING=cp1251` импортирует `savdel` и печатает эмодзи —
  раньше это роняло `python savdel.py > log`.
* **Контроль на боевых данных.** Токенный запрос на живой вике возвращает 1426 фактов по `2.1.1`,
  непустых категорий 52 из 55 — то есть запрос работает на реальной схеме, а не только на стенде.

## Саботаж: 9 диверсий, замечены 9

Порядок: копия эталона → поломка → прогон теста → восстановление из копии. Скрипт вне репозитория.

| Диверсия | Упало проверок | Замечено |
|---|---|---|
| S1 вернуть `LIKE '%код%'` (совпадение подстрокой) | 5 | да |
| S2 вернуть f-строку с путём в `ATTACH DATABASE` | 17 | да |
| S3 убрать `8.1.1`/`9.1.1`/`10.1.1` из `CAT_MAP` | 6 | да |
| S4 снова оставлять файл опустевшей категории | 2 | да |
| S5 убрать `?mode=ro` — база открывается на запись | 1 | да |
| S6 переименовать свалку в `.txt` | 1 | да |
| S7 убрать `REPLACE(category_code,' ','')` | 3 | да |
| S8 убрать проверку `CODE_RE` | 1 | да |
| S9 не писать свалку вовсе | 4 | да |

**Не замеченных диверсий нет.** `savdel.py` восстановлен байт в байт (md5 сверен).

Первый прогон саботажа вскрыл слабость **самого теста**: S2 и S3 роняли его трейсбеком
(`OperationalError`, `KeyError: '8.1.1'`) вместо именованного `[FAIL]`, и одна поломка прятала все
последующие проверки. Исправлено: `run_export` перехватывает падение экспорта и пишет
`ЭКСПОРТ УПАЛ: …` в перехваченный вывод, `file_for` возвращает `None` для кода, которого нет в
`CAT_MAP`. После этого S2 даёт 17 честных `[FAIL]` вместо трейсбека, S3 — 6. Числа в таблице выше —
после исправления.

## Прогон по боевым базам после правки (`mode=ro`, вывод в TEMP)

`wiki_final_review` не трогал. В временный каталог заранее подложен фальшивый устаревший
`8.1.1_Детская_Прием_детей.txt`, чтобы Д6 проверялась на живых данных, а не только на стенде:

* время **38,0 с**; файлов **53** = 52 `.txt` + свалка `.md`;
* `📦 Сборка: 8.1.1 … пусто (снят устаревший файл прошлой выгрузки)` — **устаревший файл снят**;
* `[!] Вне карты: 51 фактов, 35 кодов -> 00_НЕРАЗОБРАННОЕ.md` — сходится с независимым замером;
* записей по `.txt` — **41 924**; вместе с 51 в свалке это 41 975, то есть число волны 1 сходится
  ровно тогда, когда в него включена свалка;
* `stomat_wiki.db` и `stomat_archive.db` — mtime, размер и md5 **не изменились**.

## Соседние тесты

* `test_import_safety.py` — **254 проверки, 0 провалов**: новый `test_savdel_taxonomy.py` не нарушает
  запрет на запись продакшн-кода при импорте.
* `test_distill_pipeline.py` — **143 прошло, 1 провал**, и провал НЕ мой: см. раздел «Для лида».
* `run_all_tests.py` не запускался — параллельные агенты дают его md5-охране ложную тревогу.

## Что осталось непроверенным (честно)

1. **Д2 на боевой вике не воспроизводится и воспроизвестись не может** — там 0 столкновений
   подстрокой. Все 42 лишних присвоения измерены на `backup_wiki_18_0140.db`. Правка защищает от
   будущего брака модели (`distiller.py:730` запрещает ей выдумывать индексы вроде `1.10.1` промптом,
   а снимок доказывает, что она их всё равно выдумывала), а не лечит сегодняшнюю потерю.
2. **29 кодов разделов 8/9/10 так и остались без имени** (`8.1.2`…`8.4.2`, `9.1.2`…`9.4.3`,
   `10.2.1`…`10.4.2`). На сегодняшних данных их 0, и придумывать им названия — выдумывать таксономию.
   Код `10.1` (5 присвоений, единственный живой код десятого раздела) в `CAT_MAP` тоже отсутствует:
   в карте только L3, а `10.1` — L2. Эти 5 фактов уходят в свалку.
3. **L2-коды и заглушки `x.y.0` по-прежнему не экспортируются** — 51 факт виден только в свалке,
   монографию по ним никто не заказывает. Это сознательное решение, а не закрытый дефект.
4. **Реальную оплату Гемини за устаревший файл я не наблюдал** — читал `prompter.py:19`
   (`os.listdir` + `endswith(".txt")`) и не запускал его. Цепочка «устаревший `.txt` → платный
   запрос» доказана статически, снятие файла — поведенчески.
5. **`reconfigure` проверен подпроцессом с `PYTHONIOENCODING=cp1251`**, а не настоящим
   `python savdel.py > export.log` на боевой машине.
6. **Бот работает на другой машине.** Всё измерено на снимках баз, лежащих здесь; что выгрузка
   отдаёт в бою, я не наблюдал.

---

# Для лида: правки ВНЕ моих файлов (я их не делал)

## 1. Конфликт лан: `distiller.py:287` противоречит `savdel.py` CAT_MAP (провал теста)

`test_distill_pipeline.py` падает на проверке «листья сита вне CAT_MAP объявлены явно»:
`расхождение: ['10.1.1', '8.1.1', '9.1.1']`.

Причина: волна 1 добавила эти три кода в `CAT_MAP`, а `distiller.py:287` держит **захардкоженный
снимок** прежнего состояния карты:

```python
NON_EXPORTABLE_LEAVES = frozenset({"8.1.1", "9.1.1", "10.1.1"})
```

и `distiller.py:446` вешает на такой код заметку «код 8.1.1 легален, но его нет в savdel.py CAT_MAP —
факт сохранится, но ни в один файл экспорта не попадёт». **Теперь эта заметка — ложь:** код в карте
есть, файл создаётся (проверено поведенчески в `test_savdel_taxonomy.py`). Хорошая новость: `raw`
возвращается неизменным, факт никуда не переклеивается, так что содержимое не портится — цена
ограничена ложным предупреждением в журнале и падающим тестом.

Правка нужна в **не моих** файлах, поэтому я её не делал. Что я бы поменял, по возрастанию качества:

* минимум: `NON_EXPORTABLE_LEAVES = frozenset()` в `distiller.py:287`;
* правильно: **вычислять** это множество из `savdel.CAT_MAP`, а не дублировать списком — тогда оно не
  сможет разойтись снова. Это уже третий случай в проекте, когда дубликат карты рассинхронизировался.

Проверил, что провал не мой: `diff` моей версии `savdel.py` с версией волны 1 не касается `CAT_MAP`
(55 кодов до, 55 после, симметрическая разница пустая), а проверка читает из `savdel.py` только коды
регуляркой `"(\d{1,2}\.\d\.\d)":`.

## 2. Волна 1 оставила в репозитории 6 своих одноразовых скриптов

Её отчёт утверждает: «одноразовые, `mode=ro`; **удаляются в конце работы**, чтобы не мусорить в diff
лида». Они на месте, в корне репозитория, датированы сегодня 09:06–09:38:

`_measure_savdel.py`, `_measure_savdel2.py`, `_measure_savdel3.py`, `_measure_savdel4.py`,
`_measure_savdel5.py`, `_smoke_savdel.py`

Проверил на опасность: боевые базы они открывают через `mode=ro`, `UPDATE` в них — только внутри
`try` как проверка «запись отклоняется», а `INSERT` — в свою временную базу.
`test_import_safety.py` их сканирует и проходит (254/0). То есть они безвредны, но это 6 лишних файлов
в diff. Удалять не стал: не мои файлы.

## 3. Число в задаче было взято из чужого снимка

Формулировка Д1 «115 фактов детской стоматологии и материаловедения не попадают в выгрузку» относится
к `backup_wiki_18_0140.db`, а не к боевой `stomat_wiki.db`, где таких фактов **0**. Если брифы
следующих волн опираются на `_recon_distill.md`, стоит требовать в них имя базы: без него число
выглядит как боевое.

Мои файлы: `savdel.py` (правка Д6), `test_savdel_taxonomy.py` (создан, 29 проверок). Больше ничего
не менял. Ничего не оставил запущенным: замеры и саботаж — короткие синхронные скрипты в
`C:\temp\savdel_probe\`, все завершились; временные каталоги экспорта удалены.
