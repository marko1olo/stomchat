# Recon: что и КАК видит врач (assistant.py тексты команд, summarizer.py шапки/подвалы)

Замеры на живых базах: `stomat_archive.db` = 117 847 реплик, `stomat_wiki.db` = 12 784 факта
(python-проба, `count(*)`). `stomat_bot.db` для выводов о поведении не использовался.
Находки отсортированы по важности.

---

## Находка 1 (главная). Аббревиатуры врача не находятся НИКОГДА: SQLite `LIKE` не складывает регистр кириллицы

**Место:** четыре точки, все с одним дефектом — ключ приходит из `extract_keywords`, который
делает `text.lower()` (`assistant.py:510`), а поиск идёт через `LIKE '%ключ%'`:

| строка | что это для врача |
|---|---|
| `assistant.py:793` | справка вики в промпте на ЛЮБОЙ клинический вопрос в ЛС |
| `assistant.py:824` | архив в промпте на любой клинический вопрос в ЛС |
| `assistant.py:2846` | команда `/search` |
| `assistant.py:4070` | статьи подтемы в `/wiki` (там ещё и захардкожен lowercase `"внчс"`, строка 4065) |

**Корень.** SQLite приводит регистр только для ASCII:
```
select 'А' LIKE 'а', 'A' LIKE 'a';   ->   (0, 1)
```
Латиница — совпадает, кириллица — нет. А дистиллированные факты вики написаны нормальной прозой,
то есть аббревиатуры в них ЗАГЛАВНЫЕ. Ключ `'внчс'` не совпадает с текстом `'ВНЧС'`.

**Сценарий отказа:** врач пишет в ЛС «какая экспозиция ЭДТА при финишной ирригации» или жмёт
`/search ВНЧС`. Ключ становится `эдта`/`внчс`, `LIKE` возвращает 0 строк, справка уходит в промпт
пустой — и модель отвечает по памяти. При этом `/start` (строка 2548) обещает «подробно отвечу
с использованием базы знаний», `/help` (строка 2622) — «Я отвечу с использованием базы знаний».
Наиболее пострадали именно аббревиатуры, которые врач всегда пишет капсом.

**Замер по stomat_wiki.db** (SQL `LIKE '%термин%'` в нижнем регистре против честного
регистронезависимого подсчёта тех же 12 784 строк):

| термин | находит `LIKE` | реально есть | потеряно |
|---|---|---|---|
| ВНЧС | **0** | 88 | 88 (100%) |
| КЛКТ | **0** | 21 | 21 (100%) |
| МТА | **0** | 18 | 18 (100%) |
| ЭДТА | **0** | 13 | 13 (100%) |
| ТРГ | **0** | 5 | 5 (100%) |
| БОПТ | **0** | 4 | 4 (100%) |
| ЭОД | **0** | 2 | 2 (100%) |
| цирконий | 49 | 61 | 12 |
| адгезив | 607 | 627 | 20 |
| кт | 3 264 | 3 401 | 137 |
| коффердам | 282 | 287 | 5 |

По 20 проверенным терминам: `LIKE` отдаёт 6 606 фактов вместо 6 958 — **352 факта (5.1%) невидимы
всегда**, и семь профильных аббревиатур невидимы полностью.

**Замер по stomat_archive.db** (с фильтром из строк 825-827: 51 107 реплик проходят):
`внчс` 22 вместо 42, `мта` 30 вместо 45, `клкт` 18 вместо 28, `бопт` 151 вместо 175, `кт` 6 271
вместо 6 581. В чате пишут строчными чаще, поэтому здесь теряется 30-50%, а не 100%.

**Чем НЕЛЬЗЯ починить** (проверено пробой на `:memory:`):
`LOWER('ВНЧС')` → `'ВНЧС'` (без изменений), `UPPER('внчс')` → `'внчс'`, `'ВНЧС'='внчс' COLLATE
NOCASE` → `0`. И `LOWER()`, и `NOCASE` в SQLite тоже ASCII-only, так что `WHERE LOWER(content)
LIKE ?` и `COLLATE NOCASE` дефект не лечат.

**Минимальная правка:** зарегистрировать питоновский регистр —
`conn.create_function("pylower", 1, lambda s: s.lower() if s else s)` и искать
`WHERE pylower(content) LIKE ?` (проба подтверждает: `pylower('ВНЧС')` → `'внчс'`); при заметной
цене полного скана — хранить предпосчитанную lowercase-колонку с индексом.

---

## Находка 2. Порог «слово короче 4 символов» глушит второй пласт аббревиатур ещё до поиска

**Место:** `assistant.py:519`
```python
if len(w) < 4 and w not in _SHORT_DENTAL_TERMS:
    continue
```
`_SHORT_DENTAL_TERMS` (`assistant.py:260`) выводится как
`frozenset(t for t in DENTAL_KEYWORDS if len(t) < 4)` (`dental_vocab.py:89`) и содержит РОВНО 8
элементов: `бор, зуб, игл, кт, мту, свш, сдг, сст`. `мта` в нём нет, а `мту` есть — при том что
в архиве `МТА` встречается отдельным словом 34 раза, а `МТУ` — 1 раз. Дополнительно
`re.sub(r"[^\w\s-]", " ", ...)` (строка 510) рубит `CAD/CAM` на два трёхбуквенных куска.

**Сценарий отказа:** `keywords` выходит пустым, цикл по нему не выполняется ни разу, и `/search`
печатает `«По запросу ... ничего не найдено в базе знаний»` (строка 2868). Поиск не выполнялся —
врачу сказано, что база пуста.

**Замер** (проба `extract_keywords(q)` + наличие в stomat_wiki.db без учёта регистра):

| запрос врача | ключи | есть в вики | что видит врач |
|---|---|---|---|
| `МТА` | `[]` | 18 | ничего не найдено |
| `GIC` | `[]` | 115 | ничего не найдено |
| `CAD/CAM` | `[]` | 97 | ничего не найдено |
| `3D` | `[]` | 127 | ничего не найдено |
| `SDR` | `[]` | 5 | ничего не найдено |
| `ТРГ` | `[]` | 5 | ничего не найдено |
| `ЭОД` | `[]` | 2 | ничего не найдено |
| `PRF` | `[]` | 1 | ничего не найдено |
| `pH` | `[]` | 16 | ничего не найдено |
| `мм` | `[]` | 856 | ничего не найдено |

`ВНЧС`/`ЭДТА`/`КЛКТ` порог проходят (4 символа) и всё равно дают ноль — по находке 1. Это два
независимых дефекта, и лечить надо оба.

**Отдельно: текст «не найдено» не говорит, что делать дальше.** `assistant.py:2868` заканчивается
точкой — ни «переформулируйте», ни «спросите обычным сообщением», ни «/wiki». А выход есть и он
сильный: обычный вопрос в ЛС идёт через `search_knowledge_corpus` (`assistant.py:772-848`), которая
читает и вики, и архив. `/search` архив не открывает вообще: из 130 631 строки корпуса
(117 847 + 12 784) команда видит 9.8%.

**Минимальная правка:** если `extract_keywords` вернул пустой список — искать по сырому
`query_param`; в тексте «не найдено» предлагать задать вопрос обычным сообщением.

---

## Находка 3. `/search` показывает 5 фактов из 282 и не сообщает, что есть ещё

**Место:** `assistant.py:2846` (`LIMIT 5` на ключ) и `assistant.py:2870`
(`"\n\n".join(wiki_facts[:8])`). Ни счётчика, ни листания.

**Сценарий отказа:** врач вводит `/search коффердам`, получает 5 фактов и закрывает тему как
исчерпанную — про остальные 277 ему не сказали.

**Замер** (та же проба; однословный запрос отдаёт ровно 5 строк):

| запрос | показано | всего в stomat_wiki.db |
|---|---|---|
| `коффердам` | 5 | 282 |
| `имплантация` | 5 | 364 |
| `BOPT` | 5 | 163 |
| `цирконий` | 5 | 49 |
| `протокол травления емакс` | 8 | 356 |

`BOPT` — пример, который бот сам предлагает врачу в `/help` (2619), в ошибке пустого запроса (2837)
и в инструкции по поиску (4222). Главный демо-запрос показывает 3% доступного.

**Несогласованность формата рядом:** остальные поверхности счётчик дают — `/bookmarks` пишет
«Показано 10 из N закладок. Страница X из Y» (2829) и «Показано 10 из N совпадений» (2824),
энциклопедия листается с общим числом (`query_wiki_fact_page`, 3974). Без числа только `/search`,
то есть ровно та команда, для которой полнота выдачи и есть смысл существования.

**Минимальная правка:** дописать в подвал `/search` общее число совпадений и подсказку сузить
запрос — как уже сделано для `/bookmarks`.

---

## Находка 4. `/bookmarks <число>` — числовой поиск невозможен, и ошибка это скрывает

**Место:** `assistant.py:2760-2767`
```python
arg = text[10:].strip()
if arg:
    if arg.isdigit(): page = int(arg)
    else: query_filter = arg
```
и текст ошибки `assistant.py:2788`: «⚠️ Страница {page} не существует. Всего страниц: {total_pages}.»

**Сценарий отказа:** врач сохранил пост про предел артикаина и ищет `/bookmarks 500`; или
`/bookmarks 17` (ЭДТА 17%), `/bookmarks 4` (артикаин 4%), `/bookmarks 37` (гипохлорит 3-7%). Любое
чисто числовое слово читается как номер страницы. Ответ говорит про страницы и ни словом не
упоминает, что число было принято за номер страницы и что искать по числу нельзя — врач делает
вывод «таких закладок нет».

**Замер:** `'500'.isdigit() == True` → `page=500`; арифметика строк 2783-2789 при 12 закладках даёт
`total_pages=2` → на поисковый запрос приходит «Страница 500 не существует. Всего страниц: 2».
Числовые запросы осмысленны: по stomat_wiki.db `'%4%'` = 453 факта, `'%500%'` = 38, `'%17%'` = 22,
`'%37%'` = 14.

**Минимальная правка:** считать `arg` номером страницы только если он ≤ `total_pages`, иначе искать;
в тексте ошибки добавить ветку «если это был поисковый запрос — ...».

---

## Находка 5. `/start` обещает меню внизу, а клавиатура исчезает после первого нажатия

**Место:** `assistant.py:2556-2577`
```python
keyboard = types.ReplyKeyboardMarkup(
    rows=[...],            # 7 кнопок, строки 2558-2573
    resize=True,
    single_use=True,
    persistent=False
)
```
против текста `assistant.py:2553`:
«ℹ️ Используйте кнопки меню внизу для быстрого доступа к функциям или напишите /help!»

**Сценарий отказа:** `single_use=True` просит клиента скрыть клавиатуру сразу после нажатия,
`persistent=False` не даёт её вернуть. Врач нажимает «📖 Энциклопедия» — меню, на которое ссылается
приветствие, пропадает; чтобы дойти до «🧮 Калькулятор», надо снова вводить `/start`. Обещан
«быстрый доступ к функциям» (повторное использование), реализовано одноразовое.

**Замер:** 7 кнопок в клавиатуре, после первого нажатия доступно 0. Это единственная поверхность,
где врач видит `📊 Статистика чата` и `⚙️ Стиль общения`, ничего не читая: в `btn_mapping`
(2352-2360) их 7, а `/protocols` в клавиатуре нет вовсе (только в `/help` и в меню команд).

**Минимальная правка:** `single_use=False, persistent=True`.

---

## Находка 6. Шапка дайджеста называет числа, которых в чате не бывает

**Место:** `summarizer.py:912-930` — список `intros` (17 вариантов), из которого `random.choice`
(строка 996) подставляет вариант в тизер (1000-1005). Два варианта заявляют объём:
```python
"☕️ Сэкономьте 3 часа чтения чата. Вот сухой остаток дня.",     # строка 914
"🦷 Не успели прочитать 500 сообщений? Я сделал это за вас.",   # строка 918
```
Числа зашиты в литералы и ни от чего не зависят. Фактический объём окна лежит в той же области
видимости: `message_count=len(messages)` (строка 1015) — правда доступна за ноль работы.

**Сценарий отказа:** обычный день (91 сообщение) → выпадает «Не успели прочитать 500 сообщений?» →
заявка в 5.5 раза больше реального объёма. При варианте «Сэкономьте 3 часа чтения» — в 31 раз.

**Замер** (проба по stomat_archive.db: 981 день, 2023-05-10…2026-02-19, группировка по дате):

| | значение |
|---|---|
| сообщений в день, медиана | **91** (среднее 120.1, максимум 756) |
| дней, где сообщений ≥ 500 | **9 из 981 = 0.9%** |
| символов текста в день, медиана | **5 665** ≈ 5.7 минуты чтения при 1000 зн/мин |
| дней, где чтение ≥ 3 часов (≥ 180 000 символов) | **0 из 981** |

Обещание «500 сообщений» ложно в 99.1% дней, «3 часа чтения» — в 100%. Каждый вариант выпадает
с вероятностью 1/17, то есть в среднем каждый девятый выпуск открывается числом, которого чат
никогда не набирал. («за 24 часа» в варианте 11 — корректно, «007» — шутка, не заявка.)

**Минимальная правка:** подставить `len(messages)` в оба литерала (минуты считать из объёма) либо
убрать из них числа.

---

## Опровергнутые гипотезы (проверено, находкой не является)

- **«118 000+ постов»** в `AD_HINTS[3]` (`assistant.py:79`) и в промпте о возможностях
  (`assistant.py:3224`). Замер: `archive_messages` = 117 847, то есть завышено на 153 записи
  (0.13%), и архив на обычном вопросе в ЛС действительно опрашивается
  (`search_knowledge_corpus`). Самостоятельной находкой не считаю.
- **`/stats` со статичными числами** — уже исправлено. `_scan_topic_statistics`
  (`assistant.py:2062`) считает по живым базам, `render_topic_statistics` (2103-2118) печатает
  `scanned`, долю в процентах и оговорку «одно сообщение может попасть в несколько тем», пустой
  результат возвращает `None` вместо нулей (2106). Длинная операция (~5 с на 137 тыс.) прикрыта
  статусом «Считаю темы по архиву чата, это займёт несколько секунд» ровно когда кэша нет
  (2736-2743) — индикация на месте.
- **`/search` покрывает не те темы, что архив.** Замер по 30 клиническим терминам: только один
  (`реминерализация`) даёт 0 в вики при наличии в архиве. Проблема `/search` не в покрытии тем,
  а в регистре, пороге и лимитах (находки 1-3).
- **«Ищите свои фамилии в тексте!»** в недельном тизере (`summarizer.py:1363`). Замер:
  73 201 из 117 847 (62.1%) значений `sender_name` состоят из двух слов («Алиса Волошина»,
  «Денис Голяков»). Обещание выполнимо.
- **«Чтиво на 15 минут»** (`summarizer.py:1358`). Замер: сырой чат за неделю — медиана
  40 117 символов (663 сообщения), 126 из 148 недель дают ≥ 15 000 символов. Статически не
  опровергается и не подтверждается — нужен замер длины реального выпуска.
- **Провал распознавания голосового** — уже исправлено: и пустой результат
  (`assistant.py:2408`), и галлюцинация тишины (2404) отвечают текстом с указанием, что делать
  («повторите или напишите текстом»), статус-сообщение снимается в `finally` (2386-2388).
- **Снимок внутри `/case`** — уже исправлено: `assistant.py:2458-2469` прямо предупреждает, что
  файл не прочитан, и даёт выход (`/abort`).
- **Долгие `/quiz` и `/case` без индикации** — уже исправлено: статус-сообщение перед генерацией
  (2700, 2876), `bot_client.action(..., 'typing')` в `/quiz` (2714), удаление статуса и явная
  ошибка при провале (2718-2720, 2902-2904).
