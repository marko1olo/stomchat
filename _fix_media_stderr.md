# Лана media_stderr — диагностика зависшего ребёнка в `_run_tool`

Владею: `media_tools.py`, `test_media_stderr.py`.
Отчёт пишется ИНКРЕМЕНТАЛЬНО, сверху вниз, по ходу работы.

## Замер 0 — сколько медиа-отказов в живом журнале без причины

Скрипт `_measure_media_stderr.py` (разовый, не тест, не продакшн), построчный
разбор живых журналов, utf-8/replace.

| журнал | строк | «Ошибка обработки медиа» | из них БЕЗ причины | `prepare-image`/`extract-frame` | `stderr=` |
|---|---|---|---|---|---|
| `bot.log`   | 12 372  | 8 | **8** | 0 | 0 |
| `bot.log.1` | 109 801 | 0 | 0 | 0 | 0 |

Пример живой строки:

```
2026-05-19 12:43:25,016 - __main__ - ERROR - Ошибка обработки медиа 161181:
```

После двоеточия ПУСТО — во всех восьми случаях. То есть 8 из 8 медиа-отказов в
живом журнале не называют причину: 100 %.

Второй замер того же скрипта важнее: строк `prepare-image`, `extract-frame` и
`stderr=` в обоих журналах **ноль**. Правка лида (логирование stderr на путях
отказа) на боевой машине ещё ни разу не исполнялась — журналы старше кода.
Значит сегодня вся диагностика медиа-пути — это те 8 пустых строк, и любая дыра
в новом логировании выйдет наружу сразу, как код доедет.

`bot.log.1` содержит 1 строку «Ошибка подготовки фото: Невалидный файл …» — с
причиной. Это другой, ранний путь, он не про подпроцесс.

Догадка (НЕ доказательство, старого кода в дереве нет): восемь пустых строк
похожи на `f"...{msg_id}: {e}"`, где `e` — `asyncio.TimeoutError`. Проверено
живьём на этом интерпретаторе (3.13.14): `str(asyncio.TimeoutError())` == `''`.
То есть таймаут медиа печатается как пустое место. Текущий `main.py:1764`
использует `logger.exception`, так что этот конкретный дефект уже закрыт не мной.

## Находка 1 — stderr зависшего ребёнка теряется целиком (ПОДТВЕРЖДЕНО живьём)

Проба `_probe_media_stderr.py`: ребёнок пишет 6 строк в stderr, делает flush и
засыпает на 600 с; таймаут 1 с. Подменён ТОЛЬКО `media_tools.__file__` (путь к
дочернему скрипту), сам `create_subprocess_exec`, `wait_for`, `kill` — настоящие.

Результат на код ДО правки:

```
вернул payload=None error='prepare-image timeout' за 1.02 с
записей в журнале: 1
  prepare-image timeout после 1.0 с pid=24388 path=проба.jpg stderr=''
ХВОСТ STDERR В ЖУРНАЛЕ: НЕТ — дыра подтверждена
```

`stderr=''` при шести реально записанных и сброшенных строках. Потеряно 100 %.

Механика (CPython 3.13, `asyncio/streams.py`): `StreamReader.read(-1)` копит
куски в ЛОКАЛЬНОМ списке `blocks` и возвращает `b"".join(blocks)` только по EOF.
`wait_for` при таймауте отменяет `communicate()` — локальный `blocks` вместе с
уже вычитанными шестью строками выбрасывается. Второй `communicate()` после
`kill()` читает трубу заново, но там уже EOF, и возвращает `b''`.

Последствие для врача: снимок не разобран, в журнале «timeout» и пустое
`stderr=''`. Почему встало — не узнать; именно этот случай и надо
диагностировать. Тот же путь у `extract-frame`.

## Находка 2 — второй `communicate()` после `kill()` висит НАВСЕГДА (ПОДТВЕРЖДЕНО живьём)

Нашлась по ходу находки 1 и оказалась тяжелее её. Обработчик таймаута звал
`await proc.communicate()` БЕЗ потолка. `communicate()` читает до EOF, а EOF
приходит, когда трубу закроет последний, кто её держит. `kill()` убивает только
ребёнка. Если ребёнок успел породить внука с той же трубой — EOF не придёт
никогда.

Проба `_probe_reap_hang.py`: ребёнок пишет в stderr, порождает внука
(`stderr=sys.stderr`), засыпает. Таймаут инструмента 1 с, внешний потолок пробы
12 с:

```
НЕ ВЕРНУЛСЯ за 12 с при таймауте инструмента 1 с (прошло 12.01 с) — зависание подтверждено
записей в журнале: 0
```

Ноль записей: заклинило ДО `logger.warning`, то есть даже строки «timeout» нет.

Почему это дороже находки 1: `extract_first_frame_async` вызывается из
`main.py:1679` с `timeout=MEDIA_FRAME_TIMEOUT_SECONDS` (60) и **без** внешнего
`asyncio.wait_for` — ближайшие `wait_for` в `process_media_message` стоят на
скачивании (1667), на `describe_image` (1699) и на записи в базу (1713), но не
на извлечении кадра. `MEDIA_WORKER_COUNT` по умолчанию 1. Значит один такой
ребёнок останавливает разбор медиа НАВСЕГДА и без единой строки в журнале: все
следующие снимки врачей просто стоят в очереди до перезапуска.

Оговорка: воспроизведено на Windows/ProactorEventLoop. На боевой машине
(предположительно Linux) механика та же — `SIGKILL` ребёнку не закрывает fd
внука, — но там я это не запускал.

## Перепроверка волны 2 — замер и находки 1-2 подтверждены независимо

Прошлая волна этой ланы умерла, успев записать замер и находки 1-2 и наложить
правку на `media_tools.py`, но НЕ успев написать тест. Первое, что сделано в эту
волну — перепроверка её же чисел своими руками, а не доверие отчёту.

`rg -c "Ошибка обработки медиа" bot.log bot.log.1` → `bot.log:8`, в `bot.log.1`
совпадений нет вообще. `rg -o "Ошибка обработки медиа [0-9]+:.*$" bot.log` даёт
восемь строк, и после двоеточия во всех восьми пусто. Замер волны 2 сходится:
**8 из 8 медиа-отказов в живом журнале не называют причину, 100 %**.

Проба `_probe_media_stderr.py` на ТЕКУЩЕМ коде (уже с дренажом):

```
вернул payload=None error='prepare-image timeout' за 1.08 с
  prepare-image timeout ... stderr='CV2-OPEN-FAILED ЖАЛОБА ребёнка строка 0 | ... строка 5'
ХВОСТ STDERR В ЖУРНАЛЕ: ДА
```

Все шесть строк на месте — дыра находки 1 закрыта, и это проверено на живом
зависшем ребёнке, а не на исходнике.

## Находка 3 — внешняя отмена во время уборки съедала запись ЦЕЛИКОМ (ПОДТВЕРЖДЕНО живьём)

Дыра, оставшаяся после правки волны 2. Обработчик таймаута выглядел так:

```python
except asyncio.TimeoutError:
    _kill_quietly(proc)
    await _settle_after_kill(proc, drain, timeout)   # <-- отмена прилетает сюда
    logger.warning("%s timeout ... stderr=%r", ...)  # <-- и эта строка не исполняется
```

`_settle_after_kill` внутри глушит `except Exception`, но `CancelledError` — не
`Exception`, а `BaseException`. Отмена, пришедшая в окно уборки, проносится
сквозь обработчик мимо `logger.warning`.

Проба `_probe_settle_cancel.py`: ребёнок пишет в stderr, порождает внука с той
же трубой (чтобы EOF не пришёл и уборка заняла всю отсрочку), засыпает. Таймаут
инструмента 1 с → отсрочка `min(2, 1) = 1` с → с 1.0 по 2.0 с вызов сидит в
уборке. Отмена в 1.5 с.

ДО правки:

```
состояние задачи перед отменой: done=False
отменена за 0.02 с
записей в журнале: 0
```

**Ноль записей** при шести написанных ребёнком строках. Это ровно тот же провал,
который волна 2 закрыла на пути `CancelledError`, но на пути таймаута он остался.

Почему окно достижимо, а не теоретично — два живых пути:

1. `stop_media_analysis_workers()` (`main.py:952`) вызывает `task.cancel()` на
   каждой остановке бота. Воркер, стоящий в уборке, теряет запись.
2. Альбом. `vision.describe_image` (`vision.py:177`) готовит снимки ПО ОЧЕРЕДИ,
   каждый со своим `VISION_IMAGE_PREP_TIMEOUT_SECONDS` = 45 с, а внешний потолок
   `MEDIA_ANALYSIS_TIMEOUT_SECONDS` (`main.py:130`) ≥ 180 с и заведён с запасом
   «60 с на подготовку снимка (до 45 с) и накладные» — то есть на ОДИН снимок.
   На альбоме из пяти зависших снимков 5 × 45 = 225 с > 180 с: внешний потолок
   срывается посреди внутренней подготовки, и отмена приходит именно туда.

Последствие для врача: снимок не разобран, и в журнале об этом НЕ НАПИСАНО
НИЧЕГО — ни «timeout», ни хвоста stderr. Отличить «бот не увидел снимок» от
«бот подавился снимком» нельзя даже по факту существования строки.

Правка: `try/finally` вокруг уборки — запись уходит, а отмена летит дальше
вызывающему, как и обязана. ПОСЛЕ правки на той же пробе:

```
записей в журнале: 1
  prepare-image timeout после 1.0 с pid=42040 path=отмена.jpg stderr='...'
```

## Находка 4 — убитый ребёнок оставался НЕПОЖАТЫМ на пути отмены (ПОДТВЕРЖДЕНО живьём)

Всплыла из вывода той же пробы: `RuntimeWarning: coroutine 'Process.wait' was
never awaited` на `media_tools.py:161`. Причина в том, что кортеж ожиданий
вычислялся ЦЕЛИКОМ заранее:

```python
for waitable in (asyncio.shield(drain), proc.wait()):   # proc.wait() создан СРАЗУ
```

Отмена на первом ожидании выносит из цикла, и `proc.wait()` так и не запускается:
убитый ребёнок не пожат. На этой машине это не мелочь — измеренная цена
осиротевших процессов здесь 600-18905 с CPU каждый.

Правка: создавать ожидания ПООЧЕРЁДНО (`lambda: asyncio.shield(drain)`,
`proc.wait`). После правки предупреждение из вывода пробы исчезло.

Проверка этого сделана поведенчески, а не по исходнику: сценарий [5] теста ловит
`RuntimeWarning` через `warnings.catch_warnings(record=True)` + `gc.collect()`.
Что это ловится ДЕТЕРМИНИРОВАННО, а не по настроению сборщика мусора, проверено
отдельной пробой `_probe_neverawaited.py` на модели старого кода: 1 предупреждение
из 1 прогона.

## Тест — `test_media_stderr.py`, 39 проверок, 10 сценариев

Ребёнок свой, на `sys.executable`; ffmpeg не задействован нигде, поэтому SKIP не
понадобился. Подменяется ТОЛЬКО `media_tools.__file__` (путь к дочернему
скрипту) — `create_subprocess_exec`, `wait_for`, `kill`, трубы настоящие.

| # | сценарий | что доказывает |
|---|---|---|
| 1 | ребёнок пишет 6 строк и висит 600 с | убит по таймауту, хвост stderr в журнале, ребёнок пожат |
| 2 | 60 пронумерованных строк | в журнал уходит ХВОСТ (есть `СТРОКА-59`, нет `СТРОКА-00`), не больше 12 строк |
| 3 | 400 обновлений прогресса через `\r` + причина | `\r` не роняет чтение, причина после прогресса в журнале |
| 4 | отмена в 1.5 с при таймауте 1 с | запись есть, хвост есть, отмена проброшена |
| 5 | то же + `catch_warnings` | ни одной незапущенной корутины |
| 6 | валидный JSON в stdout + ворчание в stderr | успешный путь дренажом не сломан, жалоба в INFO |
| 7 | exit 3, stderr, пустой stdout | причина ВОЗВРАЩАЕТСЯ вызывающему (контракт волны 1) |
| 8 | ~1.2 МБ stderr + exit 1 | кольцо режет: в ошибке ≤ 70 000 символов, свежая причина цела |
| 9 | `prepare_image_for_analysis` / `extract_first_frame_async` | публичные точки отдают ошибку, а не исключение |
| 10 | три прогона, МИНИМУМ из трёх | перерасход вложен в отсрочку уборки |

Ловушка, на которую тест сам наступил и которая теперь закрыта в нём же: дочерние
скрипты пишут в `sys.stderr.buffer` с ЯВНЫМ utf-8. Текстовый `sys.stderr` на
Windows кодирует в cp1251, `_tail_lines` декодирует utf-8, и кириллица приезжает
мусором мимо любого якоря — первая версия пробы из-за этого ложно сообщила
«хвоста нет», хотя хвост был.

Вторая ловушка, тоже своя: первая версия сценария [4] брала путь к внуку как
`sys.argv[-1] + ".grand"`, а рабочий каталог ребёнка — корень репозитория, не
временный. Внук не рождался, EOF приходил, уборка заканчивалась мгновенно, и
проверка «отмена застала уборку» честно сообщила `FAIL: вызов успел вернуться
сам — проверка ничего не проверяет`. Именно для этого она в тесте и стоит:
сценарий отмены, в котором отменять уже нечего, выглядел бы зелёным.

## Саботаж — 8 диверсий, поймано 8

Каждая диверсия — точечная замена в `media_tools.py`, после каждой файл
восстанавливается из бэкапа ВНЕ репозитория (`C:\temp\stomchat_media_bak`).
Контроль на целом коде до и после серии: `exit=0`, упало 0.

| # | что сломано | упало проверок | поймано |
|---|---|---|---|
| 1 | дренаж публикует хвост только по EOF (семантика `communicate()`) | 2 | да |
| 2 | запись в журнал снова не защищена от отмены (находка 3) | 2 | да |
| 3 | кортеж ожиданий снова собран заранее (находка 4) | 1 | да |
| 4 | кольцо stderr без предела (`_STDERR_KEEP_BYTES = 10**9`) | 1 | да |
| 5 | в журнал уходит ГОЛОВА stderr вместо хвоста (`[:lines]`) | 3 | да |
| 6 | `proc.kill()` убран — ребёнок не убивается | 3 | да |
| 7 | `_STDERR_TAIL_LINES = 0` (срез `[-0:]` отдаёт список ЦЕЛИКОМ) | 2 | да |
| 8 | путь таймаута вообще не пишет в журнал | 13 | да |

Что здесь важнее гладкого результата — две оговорки.

**Диверсия 1 оказалась слабее, чем задумывалась.** Она НЕ уронила сценарий [1]:
когда внука нет, `kill()` закрывает трубу, EOF приходит, и дренаж успевает
опубликовать буфер за отсрочку уборки. То есть публикация-по-EOF ломается только
там, где EOF не наступает, — и различает эти два кода ровно сценарий [4] (внук
держит трубу). Сценарий [1] сам по себе этот вариант не отличает; знать это
полезнее, чем считать, что «упало 2 проверки, значит поймано надёжно».

**Первая версия диверсии 1 была негодной вовсе.** Я подменил вызов на
`proc.communicate()`, оставив дренаж запущенным, — получились два читателя одного
`StreamReader`, и тест умер с `RuntimeError: read() called while another coroutine
is already waiting for incoming data`. Формально `exit=1`, то есть поломка бы
заметилась, но это падение интерпретатора, а не поведенческая проверка: счётчик
упавших проверок показал 0, потому что до итоговой строки дело не дошло. Диверсия
переписана на корректную модель исходного дефекта.

## Сильнейшее доказательство — прогон теста на НАСТОЯЩЕМ коде до правки

Синтетические диверсии моделируют дефект, а `_bak_media_tools.py` — это он сам
(версия с `communicate()` до правки волны 2). Тест на нём:

```
[1]  [FAIL] ХВОСТ STDERR РЕБЁНКА в журнале -- stderr=''
[1]  [FAIL] stderr в записи не пустой -- stderr=''
[2]  [FAIL] последняя строка ребёнка попала в журнал -- stderr=''
[2]  [FAIL] строк в записи не больше предела -- оставлено 0 строк
[3]  [FAIL] причина ПОСЛЕ прогресса в журнале -- stderr=''
[4]  [FAIL] запись об отказе всё равно есть -- снимок врача исчез без единой строки
[4]  [FAIL] и в ней хвост stderr ребёнка -- в журнале: []
[8]  [FAIL] в ошибку не уехал весь мегабайт -- в тексте ошибки 1220036 символов
[9]  [FAIL] хвост stderr всё равно в журнале -- stderr=''
[9]  [FAIL] и здесь причина в журнале -- stderr=''
```

**11 проверок падают на настоящем старом коде**, и падают ровно с той подписью,
которую волна 2 замерила пробой: `stderr=''`. Плюс новое число, которого в отчёте
волны 2 не было: старый код возвращал вызывающему **1 220 036 символов** текста
ошибки на мегабайтном stderr — эта строка идёт в `logger.warning` в `vision.py:180`
и раньше уезжала в журнал целиком. Теперь ≤ 65 536 байт кольца.

## Замер границы диагностики — где хвоста НЕ хватит (честная граница)

`_measure_ring_eviction.py`: ребёнок называет причину, ПОСЛЕ неё сыплет прогрессом,
потом висит.

| прогресса после причины | причина в журнале |
|---|---|
| ~690 байт (10 строк) | ДА |
| ~32 540 байт (500 строк) | НЕТ, вытеснена |
| ~65 040 байт (1000 строк) | НЕТ, вытеснена |
| ~1 300 040 байт (20 000 строк) | НЕТ, вытеснена |

Связывающий предел — НЕ кольцо на 64 КБ, а `_STDERR_TAIL_LINES = 12`: причина
выживает, только если после неё меньше 12 строк. Вытеснение начинается уже на
32 КБ, не на 64 КБ.

Почему 12 всё-таки правильный выбор, а не недосмотр: ребёнок в `_run_tool` — это
ВСЕГДА `sys.executable` с `media_tools.py` (см. `create_subprocess_exec`), то есть
Python, а не ffmpeg. Его stderr — трассировка (три кадра = 8 строк) или
предупреждение PIL/cv2, и 12 строк берут её целиком с запасом. Прогрессом сыплет
ffmpeg, но ffmpeg на этом пути не запускается вообще. Граница станет реальной, если
дочерний скрипт когда-нибудь начнёт болтать, — тогда лучше не поднимать 12, а
логировать голову И хвост. Менять формат записи сейчас не стал: задача просила
последние N строк, и для настоящего ребёнка их достаточно.

## Гигиена процессов — своя же грязь, найдена и убрана

После серии саботажа в системе остались ПЯТЬ живых `python.exe` из моих временных
каталогов (`Win32_Process`, отфильтровано по `stomchat_(stderr|settle|ring)_`):

* три `loud_hanger.py` / `chatty_hanger.py` / `progress_hanger.py` — остаток
  диверсии 6, которая нарочно убирала `proc.kill()`. Ожидаемо: код под тестом
  ребёнка не убивал, значит его и не убили. Диверсия честная, остаток мой.
* два `grandchild.py` — внуки сценариев [4] и [5]. Они не убиваются ПО ЗАМЫСЛУ:
  весь смысл сценария в том, что `kill()` до внука не достаёт.

Все пять сняты вручную (`Stop-Process`). Чтобы остаток был ограничен и впредь,
сон в дочерних скриптах теста сокращён: 600 с → 45 с у зависающих, 120 с → 25 с
у внука и форкера. Проверку это не ослабляет — ребёнку нужно пережить таймаут 1 с
плюс отсрочку 2 с, а сценарий [1] отдельно требует возврата за 20 с. Проверено:
через 30 с после прогона живых процессов из моих каталогов — **0**.

## Прогон соседних наборов

| набор | проверок | упало |
|---|---|---|
| `test_media_stderr.py` (свой) | 39 | 0 |
| `test_media_diagnostics.py` | 18 | 0 |
| `test_media_recovery.py` | 18 | 0 |
| `test_media_selection.py` | 31 | 0 |
| `test_media_loop_guard.py` | 25 | 0 |
| `test_fix_subprocess.py` | 59 | 0 |
| `test_voice_pipeline.py` | 52 | 0 |
| `test_import_safety.py` | 260 | 0 |
| `test_budget_nesting.py` | 29 | 0 |

`run_all_tests.py` не запускался — его md5-охрана даёт ложную тревогу при
параллельной работе других агентов.

## Что осталось НЕ доказанным

1. **Ничего из этого не проверено на боевой машине.** Замер волны 2 показал: строк
   `prepare-image`, `extract-frame` и `stderr=` в `bot.log`/`bot.log.1` НОЛЬ —
   журналы старше кода, новое логирование там ни разу не исполнялось. Всё
   доказанное получено на Windows/ProactorEventLoop, боевая машина
   предположительно Linux/SelectorEventLoop.
2. **Незакрытый транспорт после отмены.** На пути отмены `proc.wait()` не
   доигрывает, транспорт убитого ребёнка закрывает сборщик мусора. На Windows это
   даёт шум `ValueError: I/O operation on closed pipe` из `__del__` (заглушен в
   тесте через `sys.unraisablehook`, боевой код не тронут). На Linux тот же
   незакрытый транспорт — это, возможно, утечка дескрипторов при частых отменах.
   НЕ проверял: нет Linux под рукой.
3. **cp1251 в stderr ребёнка на Windows.** `_tail_lines` декодирует utf-8 с
   `errors="replace"`, поэтому не падает никогда, но кириллическая трассировка
   ребёнка на Windows приедет в журнал мусором. На боевой машине с utf-8 локалью
   вопроса нет — не проверял.
4. **Восемь пустых строк «Ошибка обработки медиа» в `bot.log` не привязаны к
   подпроцессу доказательно.** Похоже на `f"...{e}"` с `asyncio.TimeoutError`
   (`str(asyncio.TimeoutError())` == `''` — проверено на этом интерпретаторе), но
   старого кода в дереве нет. `main.py:1764` сейчас использует `logger.exception`,
   так что этот дефект закрыт не мной.
5. **Отсрочка `_KILL_REAP_GRACE_SECONDS = 2.0` в бюджет извлечения кадра вложена
   формально, но у самого извлечения кадра внешнего потолка нет** (`main.py:1679`
   вызывает без `asyncio.wait_for`). Проверено только то, что перерасход после
   таймаута не выходит за `timeout + 2*grace` (сценарий [10], минимум из трёх
   прогонов).

## Для лида — правки ВНЕ моих файлов, я их не делал

1. **`vision.py:177` — подготовка снимков альбома идёт последовательно, и их сумма
   вылезает за внешний потолок.** `VISION_IMAGE_PREP_TIMEOUT_SECONDS` = 45 с на
   снимок, `MEDIA_ANALYSIS_TIMEOUT_SECONDS` ≥ 180 с и заведён с запасом «60 с на
   подготовку снимка (до 45 с) и накладные» — то есть на ОДИН снимок. Альбом из
   пяти зависших снимков: 5 × 45 = 225 с > 180 с. Внешний потолок срывается посреди
   внутренней подготовки. Записи в журнале это больше не стоит (закрыто находкой
   3), но остальные снимки альбома не будут разобраны, и врач получит разбор
   части альбома молча. Это класс «бюджет обязан вложиться в родительский»,
   найденный в проекте уже четырежды; `test_budget_nesting.py` его не ловит,
   потому что сравнивает потолки один-к-одному, а не сумму по числу файлов.
2. **`_sabotage_distill_scripts.py` и `_sabotage_gemini_knowledge.py` остались в
   дереве от других лан и перезаписывают боевые исходники.** У обоих на верхнем
   уровне есть `If` и НЕТ `raise SystemExit`-охраны. `test_import_safety.py`
   проверку [1] на них не срабатывает, потому что она ловит только литеральный
   `open("что-то.py", "w")`, а там путь лежит в переменной — то есть этот класс
   для неё невидим в принципе. Свой `_sabotage_media_stderr.py` я по этой причине
   удалил после прогона; результаты саботажа сохранены здесь в отчёте. Стоит либо
   удалить оба, либо расширить проверку [1] на переменные пути.
3. **`media_tools.py` больше не возвращает вызывающему неограниченный stderr**
   (было 1 220 036 символов, стало ≤ 65 536). Строка идёт в `logger.warning`
   в `vision.py:180`. Если где-то этот текст уходит в сообщение Telegram, там всё
   равно нужен свой обрез: жёсткий предел 4096 символов, и битая/длинная разметка
   отклоняет ВСЁ сообщение. Я в чужой файл не лез.

## Файлы

Изменён: `media_tools.py` (две правки, обе в разделе уборки после `kill()`).
Создан: `test_media_stderr.py` (39 проверок, 10 сценариев).
Разовые пробы/замеры (не тесты, не боевой код): `_probe_settle_cancel.py`,
`_probe_neverawaited.py`, `_measure_ring_eviction.py`.
Бэкап целого кода на время саботажа лежал ВНЕ репозитория:
`C:\temp\stomchat_media_bak\media_tools.py.good` (md5 совпадает с рабочим файлом).

