# Скептик-проверка лана reclass.py + videosi.py

Инкрементальные заметки. Автор лана — другой агент; здесь только то, что я запустил сам.
Продакшн-код НЕ правил. Правил ТОЛЬКО test_distill_scripts_safety.py (усиление, 89 -> 95).

## 1. Тест запускается и зелёный

`python test_distill_scripts_safety.py` -> exit 0, `PASSED: 89 FAILED: 0`, 45 с.
Заявлено 89 — совпало ровно. `rg -c 'check\('` = 89, то есть счётчик не врёт.

## 2. Тест не пустой

Ни одна из 89 проверок не смотрит в ТЕКСТ исходника: в файле нет ни `open('*.py')`,
ни `inspect.getsource`, ни поиска подстроки по коду. Все проверки — поведение:
возврат функции (`verify_copy`, `verify_provenance`, `clean_codes`, `already_imported`,
`save_to_db_safe`), содержимое строк базы после прогона, напечатанный журнал,
наличие/отсутствие файла копии, md5 боевых баз до/после.

## 3. Заявленные числа — перемерены независимо (`_skeptic_distill_measure.py`, mode=ro)

| заявлено | измерено мной | сходится |
|---|---|---|
| 12 784 факта | 12 784 | да |
| `is_reclassified = 0` у нуля фактов | 0 (у всех 12 784 стоит 1) | да |
| размер вики 9 158 656 байт | 9 158 656 | да |
| 53 видео-факта, id 12692..12744 | 53, 12692..12744 | да |
| 53/53 начинаются с VIDEO_MARKER, с `<b>` — 0 | 53 и 0 | да |
| архив 117 847 реплик, msg_id 5..139 701 | 117 847, 5..139 701 | да |
| 35 номеров нашлись как msg_id | 35 | да |
| из них has_media=1 у 6 | 6 | да |
| media_type='video' ровно у 1 (msg_id 1861) | 1, msg_id 1861 | да |
| verify_provenance обнуляет 52 из 53 | 52 обнулены, 1 подтверждён | да |
| UNIQUE в distilled_facts нет | `index_list` -> только `idx_cat`, unique=0 | да |
| факт 12693 ссылается на реплику про overcontour | source_ids='1548', текст 1548 — тот самый | да |
| `import_videos.py` пишет шапку с `<b>` | да, строка 101 | да |
| md5 боевых баз не изменились | 2207af…f129d / addae5…afe3 — те же | да |

Одно НЕ сошлось: «в боевой вике `source_ids IS NULL` встречается и сейчас».
Замер: `IS NULL` — 0 строк, пустая строка — 0 строк из 12 784. Вывод про
устойчивость savdel/filemake к пустому провенансу от этого не рушится (он держится
на `(s_ids or '')` и фильтре isdigit), но конкретное утверждение о боевых данных неверно.

Ещё: msg_id 1861 в архиве имеет ПУСТОЙ text и media_type='video'. То есть
единственная «подтверждённая» ссылка — действительно совпадение, как автор и написал.

## 4. Мои диверсии (12 штук, все отличаются от S1..S11 автора)

Драйверы: `_skeptic_distill_sabotage.py`, `_skeptic_distill_sabotage2.py`,
`_skeptic_distill_sabotage3.py`. Запись/восстановление БИНАРНЫЕ, после каждого
восстановления сверяется md5.

| диверсия | упало проверок | вердикт |
|---|---|---|
| M1 миграция не добавляет category_code_prev | набор оборвался traceback-ом, exit 1 | поймано, но без итоговой строки |
| M2 копия снимается ПОСЛЕ первого UPDATE | 5 | поймано |
| M3 нет таблицы -> нет своей ошибки | 1 | поймано |
| M4 факт не помечается is_reclassified = 1 | 1 | поймано |
| M5 provenance не смотрит media_type | 4 | поймано |
| M6 маркер без закрывающей скобки | 1 | поймано |
| M7 сверка числа фактов стала односторонней | 1 | поймано |
| M8 save_to_db_safe снова ждёт на любой OperationalError | 0 | НЕ ПОЙМАНО |
| M9 пустой прогон снова снимает копию | 2 | поймано |
| M10 backup_before_write не вызывает verify_copy | 0 | НЕ ПОЙМАНО |
| M11 миграция не коммитится | 0 | не дыра: DDL и так фиксируется |
| M12 verify_provenance не вызывается из main | 2 | поймано |

## 5. Усиление теста (моё, 89 -> 95)

- вызов `verify_copy` из `backup_before_write` теперь проверяется подменой-шпионом
  (факт вызова + число фактов из источника) и подменой, которая бракует копию;
- `save_to_db_safe` на базе без таблицы обязана упасть `OperationalError` с
  «no such table» и БЫСТРО (< 3 с при 5 попытках по секунде).
Перепроверено диверсиями M8/M10/M13 после усиления.

## 6. Дефект продакшна, найденный замером (`_skeptic_reclass_journal.py`)

Обрыв по лимиту ключей печатает `[СТОП] ... Переклассифицировано 0 из 4`, а ПОСЛЕДНЕЙ
строкой журнала — `--- BASE RECLASSIFIED SUCCESSFULLY ---`, код выхода 0.
Ветка `skipped` для пропусков есть, для `break` по ротациям — нет.

## 6a. Перепроверка усиления диверсиями

M8 -> 3 упавшие проверки, M10 -> 3, M13 (verify_copy молча принимает всё) -> 6.
До усиления M8 и M10 роняли ноль. Запас у тайминговой проверки: честный отказ
0.003-0.008 с (минимум из 5 прогонов), порог 3 с, сломанный код >= 5 с.

## 6b. Соседние наборы

test_import_safety.py 268/0 (у автора было 262 — плюс мои пять `_skeptic_*.py`).
test_distill_pipeline.py 144/0 — расхождение LEAF_CODES/CAT_MAP, о котором писал
автор, к моменту моей проверки уже починил другой лан.
test_isolation.py 107/2 — те же два чужих провала в test_media_loop_guard.py.
Набор перечисляет тесты через `os.listdir`, то есть мой лан он тоже смотрел и
нарушений изоляции не нашёл. run_all_tests.py не запускал (запрещён брифом).

## 6c. Лан УЖЕ в истории

Коммит `7d6f8f5` «Провенанс видео-протоколов оказался ложным на 52 фактах из 53»
(29.07 10:27) содержит reclass.py, videosi.py и test_distill_scripts_safety.py.
reclass.py и videosi.py в рабочем дереве побайтово равны HEAD. Не закоммичено
только моё усиление теста (+53 строки) и этот файл.

## 7. Наследил / не наследил

`wiki_backup_*.db`, `videos.txt`, `*.sabbak`, `*.skepbak` — не найдено (`fd`).
Мой первый драйвер диверсий (текстовый режим записи + молчаливое восстановление)
успел оставить в reclass.py CRLF и последнюю диверсию M9; восстановил байт-в-байт из
блоба индекса (`git cat-file -p`), `git status` по обоим файлам чист, тест 89/0 до
усиления. Это МОЙ след, не автора.
