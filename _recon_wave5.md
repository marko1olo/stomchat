# Разведка волны 5: что осталось после четырёх волн

Лана: РАЗВЕДКА. **Ни одного `.py` в репозитории не изменено и не создано.** Все
скрипты замера и песочница саботажа лежат ВНЕ репозитория, в `C:\temp\recon_wave5\`.
Боевые базы открывались только `mode=ro`. Git не вызывался ни разу (кроме `git status`
на чтение — и он тоже не понадобился, состояние снималось `fd`/`os.listdir`).

Путь: `C:\Users\Admin\Desktop\_Organized\08_Projects\stomchat`. Вложенная **полная
копия** репозитория в `stomchat/` исключена из ВСЕХ обходов: инвентарь берёт
`os.listdir` по корню (не рекурсивно), `rg` — с `-g '!stomchat' -g '!uploaded_media'
-g '!.git'`.

Замеры: 2026-07-29, 14:2x-15:xx. Дерево в этот момент стояло (последняя правка `.py` —
`_flake_old_metric.py` 13:55, боевые файлы 13:4x), поэтому числа устойчивее, чем в
волнах 2-3, где под замером работали семь агентов.

## Инвентарь дерева на старте волны 5 (замер, не пересказ)

```
всего .py в корне репозитория            237
  test_*                                  80   <- совпадает с «80 наборов» из задания
  артефакты сессии (_measure_/_probe_/
  _sabotage_/_skeptic_/_flake_/_bak_...)  117
  боевые модули + инструменты              40
ast.parse разобрал                        40 из 40  (benchmark.py — только как utf-8-sig, BOM на месте)
```

Против волны 3 (39 боевых + 77 тестов): появились `taxonomy.py`, `html_safe.py` вырос,
добавился `_run_reachable_suites.py` (58 строк, инструмент, не тест). Артефактов стало
117 против 58 — то есть **мусора в корне вдвое больше, чем боевого кода**.

---

## 1. НЕМЫЕ ПУТИ СЕГОДНЯ

Инструменты: `C:\temp\recon_wave5\excepts.py` (`ast.parse`, классификация
LOGGED/CARRIES/MUTE методом волны 3 плюс ВСЕ обёртки журнала, найденные в дереве:
`_throttled`, `throttled_log`, `say`, `dump_error`, `_log_watchdog_exit`,
`_log_tool_failure`, `_relay_child_log`, `_log_give_up`, `_log_task_result`,
`write_to_shadow_log`), `hot.py` (замыкание горячего пути), `show_mute.py` (печать тела
каждого обработчика — классификация по последствию сделана чтением, не по имени).

### 1.1 Голых `except:` — НОЛЬ. Заявка лида ПОДТВЕРЖДЕНА двумя независимыми способами

| способ | результат |
|---|---|
| `ast.parse` по 40 боевым/инструментальным модулям (`h.type is None`) | **0** |
| `rg -c '^\s*except\s*:' -g '*.py'` по ВСЕМУ дереву (237 файлов, вложенная копия исключена) | **0 файлов** |

`prompter.py:33` — тот единственный голый обработчик, который волны 2 и 3 называли
последним, — закрыт. Проверено чтением: там теперь `except (IndexError, ValueError)`.
Заодно `except BaseException` в дереве три штуки, и все три законны:
`tg_safety.py:416` (переброс после нашей же отмены), `test_tg_safety.py:684`,
`test_video_import.py:291` (обе — проверка «отказ вообще случился»).

**Больше эту метрику заказывать нечего.** Класс закрыт полностью.

### 1.2 Обработчиков всего и сколько немых

```
обработчиков except в 40 боевых/инструментальных модулях   292
  LOGGED  (пишет сам или через обёртку проекта)            190
  CARRIES (не пишет, но причина уезжает наружу/перебросом)  23
  MUTE    (причина не доходит ни до кого)                   79
голых except:                                               0

except Exception / except BaseException всего              220
  из них MUTE (без записи в журнал)                         57
  из них тело ровно pass                                    32
  из них тело ровно return (без значения)                    2
```

Три волны считали разное, поэтому таблица сравнения:

| замер | обработчиков | «немых» | голых |
|---|---|---|---|
| `_fix_observe.md` ЗАМЕР 2 (35 модулей) | 242 | 87 | 2 |
| `_recon_wave2.md` §3.3 (38 модулей) | 279 | 104 | 1 |
| `_recon_wave3.md` §3.1 (38 модулей, обёртки учтены) | 278 | 66 MUTE | 1 |
| **моя (40 модулей, обёрток учтено 10, а не 4)** | **292** | **79 MUTE** | **0** |

Рост 66 → 79 объясняется не деградацией, а тремя вещами: +2 модуля в счёте
(`taxonomy.py`, `_run_reachable_suites.py`), +14 обработчиков появилось с кодом волны 4
(`gemini_client.py` 14, `blocking_tools.py` 19 — те же, а `assistant.py` 86 против 86),
и мой счёт включает `distiller.py:227/231` (`say` считает сам себя обёрткой). Честная
поправка ниже.

### 1.3 Из 79 MUTE долгом является 60, на горячем пути — 26

19 MUTE-обработчиков **оправданы и убирать их нельзя**:

* 13 — преамбула `sys.stdout.reconfigure(encoding='utf-8')` на уровне модуля
  (`main.py:12`, `checker.py:16`, `debugdist.py:13`, `delme.py:12`, `deppd.py:17`,
  `filemake.py:13`, `id.py:12`, `inspector.py:13`, `pathcheck.py:12`, `run_all_tests.py:36`,
  `savdel.py:16`, `visionproc.py:23`, `_run_reachable_suites.py:14`). Правила проекта
  прямо запрещают её трогать: без неё `print` с эмодзи роняет процесс до настройки журнала.
* 6 — опциональный импорт (`main.py:41` psutil, `dental_vocab.py:27`,
  `distiller.py:346`, `runtime_guard.py:35`, `search_engine.py:13`, `tg_safety.py:153`).

Горячий путь = замыкание по ИМЕНАМ от 9 обработчиков Telethon в `main.py`
(`handle_new_message`, `handle_private_message`, `handle_callback_query`,
`handle_edited_message`, `handle_deleted_messages`, `dump_handler`, `get_chat_id`,
`manual_test_handler`, `manual_weekly_test`) — 445 имён. **Оговорка: это ВЕРХНЯЯ
оценка, разрешения типов нет** — та же оговорка, что у волн 2 и 3.

### 1.4 Горячие немые сайты: последствие для врача и приоритет

Тело каждого обработчика прочитано. Ниже только те, где отказ доходит до врача.

| сайт (14:3x) | что глохнет | ПОСЛЕДСТВИЕ ДЛЯ ВРАЧА | P |
|---|---|---|---|
| `assistant.py:2230` `check_and_trigger_assistant_media` | `datetime.fromisoformat(last_passive_media_str)`, тело `pass` | битая метка в state → пассивный кулдаун 120 мин НЕ применён: бот комментирует снимки подряд и флудит группу 749 врачей. Обходится немедленным `return` в успешной ветке — то есть отказ разбора ведёт себя как «кулдауна не было» | **P1** |
| `assistant.py:1342` `check_and_apply_silence` | `event.client.get_messages(chat_id, ids=reply_to_msg_id)`, тело `pass` | сеть икнула → бот не понял, что врач отвечает ЕМУ; режим тишины/извинения не применён, бот говорит там, где велено молчать. В журнале ни строки | **P1** |
| `assistant.py:5367` `check_and_trigger_referee` | `database.get_reply_chain_texts(msg_id, max_depth=5)`, тело `pass` | арбитр спора решает, вмешиваться ли, на ПУСТОМ контексте (ниже `if not context_msgs:` берёт запасной путь, но причина первого отказа неизвестна навсегда) | **P1** |
| `assistant.py:5842` `check_and_send_group_activity_pings` | `json.loads(text_res)`, тело `return` | молчаливый выход из ВСЕЙ рассылки пингов: никого не позвали, «почему бот молчит неделю» не имеет ни одной зацепки. Формально вне замыкания от обработчиков (зовёт планировщик), но врач видит именно это | **P1** |
| `assistant.py:2667`, `:2849`, `:2852`, `:3212` | `bot_client.delete_messages(status_msg.id)` и `os.remove(temp_path)`, все четыре тело `pass` | под готовым ответом навсегда остаётся «⏳ Думаю…» — вечный спиннер; плюс `temp_media` не чистится и диск течёт | **P2** |
| `assistant.py:5151` `handle_quiz_callback` | `json.loads(history_str)` → `{"votes": [0,0,0,0], "voters": {}}` | одна битая запись состояния обнуляет ВСЕ голоса викторины; проголосовавшие врачи могут голосовать снова, счёт врёт | **P2** |
| `assistant.py:2575` `handle_interactive_case_step` | `json.loads(user_state["history"])` → `[]` | интерактивный разбор клинического случая начинается заново посреди диалога, без объяснения | **P2** |
| `assistant.py:5904` `check_and_send_group_activity_pings` | `datetime.fromisoformat` дат активности → 2000-01-01 | врач с битой датой активности считается «молчащим с 2000 года» и получает пинг вне очереди | **P2** |
| `media_tools.py:166` `_settle_after_kill`, `:62` `_prepare_image_sync` | добор убитого ffmpeg / `img.close()` | оба с комментарием-обоснованием в коде (`:166` молчит СПЕЦИАЛЬНО, чтобы не забить строку с настоящей причиной) | P3 |
| `assistant.py:3325` | подсветка найденного (`re.sub`) → текст без выделения | терпимо | P3 |
| `assistant.py:3532` | `calculate_context_length_guidelines` → «кратко, 3-4 предложения» | подстановка разумная | P3 |
| `assistant.py:492` `save_state` | удаление `.tmp` после сбоя записи | вторично: главная причина записана `logger.error` строкой выше | P3 |
| `assistant.py:659` `claim_passive_slot` | `asyncio.current_task()` → `RuntimeError` → `task=None` | узкий класс, ветка предусмотрена | — |
| `assistant.py:2958` | `int(parts[1])` → `ValueError` → `count=10` | узкий класс, значение по умолчанию | — |
| `blocking_tools.py:344, 401, 417`; `media_tools.py:138` | `proc.kill()` на мёртвом ребёнке, закрытие пайпа, строка >64 КБ | все четыре — узкие классы С КОММЕНТАРИЕМ, объясняющим, что запись здесь СЪЕЛА БЫ настоящую диагностику | — |
| `blocking_tools.py:485, 513` | таймаут и битый JSON подпроцесса | **ложное срабатывание моего первого прогона**: оба пишут через `_log_tool_failure`. Исправлено добавлением обёртки в классификатор — отсюда 81 → 79 | — |
| `runtime_guard.py:113, 353, 355, 375` | настройка журнала и запись перед `os._exit(78)` | вынужденно: журнал и есть предмет отказа | — |

**Чем доказать, что немота закрыта (на каждый сайт, а не на файл):** тест подсовывает в
зависимость исключение и требует (а) записи в перехваченном журнале с ТИПОМ исключения,
(б) отрицательного результата вызывающему. Проверка «в файле нет
`except Exception:\n    pass`» НЕ считается: 25 из 57 широких MUTE — не `pass`, а
подстановка значения по умолчанию, такая проверка их не видит вовсе.

**Чем доказать честность счёта:** `LOGGED+CARRIES+MUTE` должно равняться числу
обработчиков, а список обёрток журнала — сверяться с деревом
(`rg 'def [_a-z]*log[_a-z]*\('`). Волна 2 получила 104 вместо 66 именно потому, что
знала 0 обёрток; я получила 81 вместо 79, потому что знала 4 из 10.

---

## 2. ЧТО ЕЩЁ НЕ ПОДКЛЮЧЕНО

Метод: `ast.parse` по всем 237 `.py` корня → граф локальных импортов → замыкание от
`main.py`; затем для каждой публичной функции модулей замыкания — есть ли ВЫЗОВ из
боевого модуля, из теста, из черновика (`C:\temp\recon_wave5\wired.py`).

### 2.1 Веб-поиск ПОДКЛЮЧЁН — подтверждаю волну 3

Замыкание импортов от `main.py` — **14 модулей** (в волне 2 было 13):

```
main, assistant, blocking_tools, config, database, dental_vocab, gemini_client,
html_safe, media_tools, runtime_guard, summarizer, tg_safety, vision, web_lookup
```

`web_lookup.py` (807 строк) внутри замыкания, `assistant.py:3397` вызывает
`blocking_tools.web_search_async`. Заявка волны 2 «у врача нет веб-поиска» закрыта.

### 2.2 Что осталось вне замыкания: 26 модулей, из них МЁРТВЫХ по-настоящему — 2

| модуль | строк | состояние | P |
|---|---|---|---|
| `search_engine_safe.py` | 26 | **не импортирует НИКТО** — ни боевой код, ни тест, ни черновик. `/web` зовёт `blocking_tools.web_search_async` напрямую. Оболочка `perform_search` мертва вместе с модулем | **P2** |
| `search_engine.py` | 68 | не импортирует никто; зарегистрирован в `test_dead_tools.py` как мёртвый груз (читается как ТЕКСТ). `from ddgs import DDGS` теперь под `try` (`:13`), то есть класс «падает импортом» закрыт | P3 |
| `taxonomy.py` | 405 | **вне замыкания от `main.py`** — и это не мусор, а дыра: см. §5.1. Единственный источник таксономии не доходит до модуля, который рисует кнопки врачу | **P1** |
| `gemini_knowledge.py` | 342 | корпусный синтез, вне бота — так и задумано | — |
| `distiller.py`, `reclass.py`, `videosi.py`, `savdel.py`, `checker.py`, `filemake.py`, `prompter.py`, `deppd.py`, `visionproc.py`, `import_videos.py`, `debugdist.py`, `pathcheck.py` | 47-827 | корпусные скрипты и экспортёры, запуск руками. Вне бота ПРАВИЛЬНО | — |
| `benchmark.py`, `delist.py`, `patch_assistant.py`, `patch_assistant_v2.py` | 13-134 | обезврежены `raise ImportError`/`SystemExit`, сторожит `test_import_safety.py` | — |
| `id.py`, `delme.py`, `inspector.py` | 32-66 | разовая печать списка чатов | — |
| `config.example.py` | 126 | шаблон | — |
| `run_all_tests.py` | 170 | раннер | — |
| `_run_reachable_suites.py` | **58** | **артефакт волны 4, оставленный в корне.** Не тест (`test_*` его не увидит), не боевой модуль, префикса `_measure_`/`_probe_` нет — то есть в любой инвентаризации он выглядит инструментом. Запускает 12 наборов подпроцессами | P3 |

### 2.3 Публичные функции, которых не зовёт ни один боевой модуль (только тесты)

`wired.py` даёт **11** таких имён, и почти все — законные:

| функция | вызовов из боевого кода | вердикт |
|---|---|---|
| `tg_safety.is_retryable`, `tg_safety.reset_cooldowns`, `runtime_guard.reset_throttle`, `gemini_client.get_last_failure` | 0 | сервис для тестов и диагностики, так и задумано |
| `main.handle_callback_query`, `handle_edited_message`, `handle_deleted_messages`, `patched_send_message` | 0 | их зовёт Telethon через декоратор, а не Python — ложное срабатывание метода |
| `database.get_messages_for_summary`, `database.count_clinical_bookmarks` | 0 | **настоящая находка: два запроса к базе, покрытые тестами, не зовёт никто.** `count_clinical_bookmarks` — счётчик закладок врача; `get_messages_for_summary` — выборка для сводки (боевой путь берёт `get_messages_for_period`) | P3 |
| `html_safe.clip_at_sentence_text` | 0 прямых | **ложное срабатывание моего метода**: `assistant.py:973` берёт её через алиас (`_clip_at_sentence = html_safe.clip_at_sentence_text`), `web_lookup.py:59` — через `import as`. Функция ЖИВАЯ, см. §5.2 |

**Вывод по пункту:** «построено, покрыто тестами и не вызывается» после волны 4 осталось
в двух местах — `search_engine_safe.py` (26 строк, тестов нет) и, по-настоящему больно,
`taxonomy.py` (405 строк, 1 тест, 63 проверки) не доходит до `assistant.py`.

**Чем доказать:** тест считает по AST замыкание импортов от `main.py` и падает, если
модуль, объявленный единственным источником, в него не входит. Проверка «в
`assistant.py` есть строка `import taxonomy`» не считается — нужен поведенческий тест
из §5.1.

---

## 3. ТЕСТОВ НЕТ: 12 модулей не тронуты ни одним тестом

Метод: «покрыт» = имя модуля появляется в `import` какого-нибудь `test_*.py` (ВЕРХНЯЯ
оценка, как у волн 2-3). Отдельно выделен класс «тест читает модуль как ТЕКСТ» — по
правилу проекта это не покрытие поведения. Инструмент
`C:\temp\recon_wave5\coverage.py`, замер по 40 боевым модулям и 80 тестам.

### 3.1 Не тронуты ни импортом, ни чтением как текста — 12

| модуль | строк | флаги опасности | опасен или разовый | P |
|---|---|---|---|---|
| `visionproc.py` | 238 | **SQL-WRITE + боевая БД + сеть + пишет файлы** | **ОПАСЕН**: пишет `vision_description` в боевой архив (117 847 реплик), ходит в Telegram и в зрение. Плюс делает `os.makedirs` при импорте | **P1** |
| `deppd.py` | 203 | **SQL-WRITE + боевая БД + сеть** | **ОПАСЕН**: выкачивает историю чата в `stomat_archive.db` — тот самый файл, из которого дистиллятор берёт корпус | **P1** |
| `pathcheck.py` | 73 | боевая БД | **ОПАСЕН формой доступа**: `:25` `aiosqlite.connect(DB_PATH)` на `stomat_archive.db` — соединение НА ЗАПИСЬ там, где делается только `SELECT ... ORDER BY RANDOM() LIMIT 50`. Правка на 12 символов (`file:...?mode=ro`), названа ещё в `_fix_import_safety.md` «для лида» п.3 и НЕ сделана | **P2** |
| `filemake.py` | 72 | боевая БД + пишет 280 файлов | **ОПАСЕН тихо**: шестой потребитель таксономии, который `taxonomy.py` не знает вовсе (см. §5.1) | **P2** |
| `run_all_tests.py` | 170 | боевая БД в списке md5-охраны | сторож целостности, сам не проверен ничем. Запускать его заданием запрещено, то есть проверить нечем и в этой волне | P2 |
| `prompter.py` | 100 | пишет файлы | разовый генератор промптов; голый `except:` из него убран | P3 |
| `search_engine_safe.py` | 26 | — | мёртв (см. §2.2) | P3 |
| `debugdist.py` | 62 | боевая БД (чтение) | разовый | P3 |
| `_run_reachable_suites.py` | 58 | — | артефакт волны 4 | P3 |
| `id.py`, `delme.py`, `inspector.py` | 32-66 | сеть (Telegram) | разовые, печать | P3 |

Против волны 3 (11 модулей): закрытых нет, добавились `_run_reachable_suites.py`
(новый) и `pathcheck.py`/`debugdist.py`/`prompter.py` считались и там.
**`visionproc.py` и `deppd.py` — два скрипта с правом записи в боевые данные и ноль
тестов — не тронуты ЧЕТЫРЬМЯ волнами.** Это самая старая незакрытая заявка отчётов.

### 3.2 Только как ТЕКСТ (импорта нет, поведение не проверено) — 7

`benchmark.py`, `delist.py`, `patch_assistant.py`, `patch_assistant_v2.py` (через
`test_import_safety.py`), `search_engine.py` (`test_dead_tools.py`),
`import_videos.py` (`test_video_import.py`), `config.example.py`
(`test_config_contract.py`, `test_report_targets.py`).

Для реестра мёртвых инструментов это законно — импортировать их и есть авария. Но
`import_videos.py` (47 строк, урезан волной 4) проверяется только чтением исходника,
а он умеет `INSERT INTO distilled_facts`: если урезание отменят, текстовая проверка
это увидит, а поведение — нет.

**Чем доказать:** для `visionproc.py` и `deppd.py` — тест вызывает по одной функции
каждого на ВРЕМЕННОЙ копии базы и требует, чтобы боевой файл не изменился ни по
`mtime`, ни по префиксу md5. Пересчёт `coverage.py` должен давать меньше 12.

---

## 4. ВЛОЖЕННОСТЬ БЮДЖЕТОВ: таблица по шести путям

Инвентарь (`C:\temp\recon_wave5\budgets.py`, `path.py`, `unbounded.py`):
**73 модульных константы бюджета** в 13 модулях, **77 литеральных `timeout=<число>`**
в вызовах (32 из них в `assistant.py`). Волна 3 видела 98 констант и 77 литералов —
литералы не сдвинулись ни на один.

Вычисленные значения (не литералы из отчёта, а арифметика по коду):

```
blocking_tools._SUBPROCESS_STARTUP_SLACK_SECONDS = 10
main.VOICE_TRANSCRIBE_TIMEOUT_SECONDS            = 60   -> дедлайн подпроцесса 70
main.VOICE_ITEM_BUDGET_SECONDS                   = 120 + 60 + 45 = 225
main.MEDIA_ANALYSIS_TIMEOUT_SECONDS              = max(180, 198 + 60) = 258
vision.vision_cascade_budget_seconds()           = 3 x 2 x (30 + 3) = 198
web_lookup.SEARCH_TOTAL_COST_SECONDS             = (45 + 10) x 2 = 110
web_lookup.LOOKUP_TOTAL_COST_SECONDS             = 110 + (90 + 10) + 5 = 215
assistant.WEB_COMMAND_TIMEOUT_SECONDS            = 20 + 215 + 90 + 15 = 340
main.SUMMARY_STALE_SECONDS                       = 2100 + 600 = 2700
telethon: timeout=30, request_retries=10, retry_delay=5 -> один await до ~500 с
```

### 4.1 Таблица

| # | путь | родительский дедлайн | дочерние таймауты | влезает | чем проверено |
|---|---|---|---|---|---|
| 1 | **доставка сводки** | **НЕТ на весь путь.** `SUMMARY_STALE_SECONDS` 2700 — это возраст файла статуса, при превышении `os._exit(79)` (`main.py:467`), а не срок операции | генерация 2100 → подпроцесс 2110, Telegraph 60, поиск дубля 90, отправка 90, повтор plain 90, закреп 30, БД 30+30 = **2520** | **ДА арифметически**, запас 180. Принудительно — нет: сторож ловит «статус не двигался», а не «путь идёт слишком долго» | `test_budget_nesting.py` 29 проверок; **§9 диверсия 5 волны 3 показала, что снятие `wait_for` с отправки не заметили 137 проверок** — то есть держит не тест, а сам код |
| 2 | **расшифровка голосового (группа)** | `asyncio.wait_for(..., item_budget)`, `item_budget = min(VOICE_ITEM_BUDGET_SECONDS=225, остаток)` (`main.py:1595`) | скачивание 120 + расшифровка 60 = 180 | **ДА**, запас 45. Плюс правильная конструкция: ребёнку выдаётся МИНИМУМ своего бюджета и остатка общего 1800 | `test_voice_offline.py` 56 проверок |
| 2a | ...внутри подпроцесса расшифровки | `blocking_tools._run_json_tool`: `deadline = timeout + 10` = **70** (`:482`) | `gemini_client.py:929` `subprocess.run(timeout=120)`; `:858` `_probe_ffmpeg timeout=30` × **до 4 кандидатов** (env, PATH, `imageio_ffmpeg`, `static_ffmpeg`) = до 120. Худший случай **240** | **НЕТ. 240 внутри 70.** Пятый случай класса, найденный волной 2, подтверждённый волной 3, **волной 4 НЕ ЗАКРЫТ** | ничем: `test_budget_nesting` эти два числа не сравнивает, а `test_voice_pipeline.py:425-428` сравнивает подстроку `"timeout=120"` с исходником — то есть **ЗАЩИЩАЕТ дефект** (моя диверсия 3, §9) |
| 3 | **зрение / медиа (снимок врача)** | **РОДИТЕЛЯ НЕТ**: `media_analysis_worker` запущен `runtime_guard.create_task` (`main.py:967`), а `create_task` (`runtime_guard.py:404`) — голый `asyncio.create_task` без `wait_for` | скачивание 120, кадр 60, `describe_image` 258, три записи в БД по 30 = **528** на один снимок | нечему влезать; каждый шаг ограничен, но **один снимок держит единственный воркер до 528 с** при очереди на 128 и одном воркере | `test_media_loop_guard.py` 25 проверок — про отметку, не про бюджет |
| 3a | ...внутри разбора снимка | `MEDIA_ANALYSIS_TIMEOUT_SECONDS` = **258**, ВЫВЕДЕН из каскада (`main.py:130`) | каскад зрения 198 + подготовка снимка 45 = **243** | **ДА**, запас 15. Правильное лечение класса: потолок производный, а не второе независимое число | комментарий `main.py:126-129` называет класс; сравнение чисел в `test_budget_nesting` |
| 4 | **веб-поиск `/web`** | `WEB_COMMAND_TIMEOUT_SECONDS` = **340**, и он **НЕ ИСПОЛЬЗУЕТСЯ НИ В ОДНОМ `wait_for`**: единственные потребители — `test_search_command.py:516-532` (арифметика) | статус 20, `run_lookup` 215, доставка 90, уборка статуса 15 = **340** | **ДА арифметически, принудительно НЕТ.** Сумма соблюдается только потому, что этапы последовательны и каждый ограничен сам | `test_search_command.py` — 5 проверок сравнивают ЧИСЛА (не строки), это правильный тип проверки |
| 4a | ...внутри `run_lookup` | 215 (`LOOKUP_TOTAL_COST_SECONDS`) | поиск 2 × (45+10) = 110, генерация = **ОСТАТОК** + 10, троттлинг 5 | **ДА** — единственный путь в проекте, где ребёнку выдаётся остаток, а не своё желаемое число | `test_web_lookup.py` 110 проверок |
| 5 | **викторина: генерация** `handle_group_quiz` (`assistant.py:4371`, 117 строк) | **РОДИТЕЛЯ НЕТ** | генерация 90 (`:4395`) + **5 сырых обращений к Telegram без границы** (`:4378, 4381, 4396, 4399, 4482`) | **НЕТ ПОТОЛКА.** По собственному замеру проекта (`assistant.py:44-49`) один сырой `await` висит до ~500 с | ничем |
| 5a | **викторина: нажатие кнопки** `handle_quiz_callback` | **РОДИТЕЛЯ НЕТ**: `main.handle_callback_query` зовёт `await assistant.handle_quiz_callback(...)` напрямую | правки сообщения ограничены 25 с через `edit_callback_message` (10 сайтов), но **16 сырых `event.answer()` без срока** | **НЕТ.** Спиннер на кнопке снимает именно `event.answer()`, и он единственный в этой функции не ограничен ничем | `test_tg_delivery.py` 71 проверка держит `edit_callback_message` (диверсия 3 волны 3: 26 провалов), но `answer()` не держит никто |
| 6 | **интерактивный кейс** `handle_interactive_case_step` (`assistant.py:2567`, 151 строка) — **этого пути в таблицах волн 2-3 НЕ БЫЛО** | **РОДИТЕЛЯ НЕТ**: зовётся из `assistant.handle_private_message:2931`, которую `main.py:2324` запускает через `create_task` → `run_pm_safe` под `_pm_user_lock` **без `wait_for`** | генерация 90 (`:2663`) + **6 сырых обращений к Telegram** (`:2583, 2616, 2666, 2670, 2680, 2695`) + 3 записи в БД | **НЕТ ПОТОЛКА, и это худший случай из шести**: замок `_pm_user_lock(chat_id)` держится всё время шага. Один зависший `send_message` (до ~500 с) → врач пишет второе сообщение, оно ЖДЁТ на замке молча, и так до 4 шагов кейса | ничем |

### 4.2 Формулировка «влезает» неприменима к 4 путям из 6

У путей 1, 3, 5, 5a, 6 родительского дедлайна НЕТ ВООБЩЕ. Ловушка проекта названа
правильно: «у многих путей родительского дедлайна нет вовсе, и тогда „влезает“ —
неверная формулировка». Замер: `runtime_guard.create_task` (`runtime_guard.py:404-409`)
это `asyncio.create_task` + имя + `_log_task_result`. Через него запущено **15**
долгоживущих задач, из них на пути врача — `run_pm_safe` (личный вопрос),
`run_assistant_safe` (ответ в группе), `run_media_assistant_safe` (снимок),
`_bg_portrait`.

**P1 по совокупности:** не «нарушение вложенности», а отсутствие внешнего контура.
Цена конкретная и измеримая: `_pm_user_lock` (`main.py:2300`) держится столько, сколько
идёт шаг, а потолка у шага нет.

### 4.3 Обращения к Telegram без границ: 98 в `assistant.py`, не изменилось

| файл | обращений | внутри `wait_for` | БЕЗ ГРАНИЦ | по видам |
|---|---|---|---|---|
| `assistant.py` | 98 | 0 | **98** | 69 `send_message`, 16 `answer`, 8 `delete_messages`, 4 `get_messages`, 1 `get_me` |
| `main.py` | 14 | 3 | **11** | 3 `send_message`, 2 `iter_messages`, 2 `answer`, 2 `delete_messages`, 1 `get_me`, 1 `get_messages` |
| `summarizer.py` | 4 | 4 | **0** | — |

Плюс сверх этих 98 — обращения ЧЕРЕЗ хелперы с границей внутри: `edit_callback_message`
10, `tg_safety.edit_message` 6, `tg_safety.send_message` 4, `tg_safety.delete_messages` 1
= **21 прикрытый сайт** (волна 3 считала 11 — принятие выросло вдвое).

Но **число «98 без границ» совпало с волной 3 ДО ЦИФРЫ**: волна 4 закрывала другие
оси и на этой не сдвинулась ни на один сайт.

**Чем доказать по пункту 4:** тест сравнивает пары ЧИСЕЛ (не строк исходника) для
каждой строки таблицы и падает при `>=`; для 2a — сравнение `gemini_client`
`subprocess.run(timeout=...)` плюс `_probe_ffmpeg × число кандидатов` с
`VOICE_TRANSCRIBE_TIMEOUT_SECONDS + _SUBPROCESS_STARTUP_SLACK_SECONDS`; для 5a и 6 —
поведенческий тест с зависшим `answer()`/`send_message()`, требующий, чтобы обработчик
завершился в срок и отпустил `_pm_user_lock`. **Капкан:** правильная починка 2a УРОНИТ
`test_voice_pipeline.py:425-428` — его надо править в той же волне (доказано моей
диверсией 3, §9).

---

## 5. ЧТО ОСТАЛОСЬ ОТ ДУБЛЕЙ: проверены все четыре, найдена ПЯТАЯ копия у двух из них

Инструменты: `C:\temp\recon_wave5\dupes.py` (AST: одноимённые функции + md5 тела,
одинаковые наборы литералов), `tax.py` (копии таксономии исполнены в изолированном
пространстве, боевые модули не импортированы), `greedy.py` (правила разбора JSON
прогнаны на одних данных).

### 5.1 Таксономия: СВЕДЕНА НА ТРИ ПОТРЕБИТЕЛЯ ИЗ ШЕСТИ. Пятая копия — та, что видит врач. P1

| потребитель | состояние | кодов |
|---|---|---|
| `distiller.py:166` | `KNOWLEDGE_TREE = taxonomy.KNOWLEDGE_TREE`, 7 ссылок на `taxonomy.*` | из источника |
| `checker.py:27` | `CAT_NAMES = taxonomy.DISPLAY_NAMES`, 3 ссылки | из источника |
| `savdel.py:7` | `import taxonomy`, 6 ссылок | из источника |
| **`assistant.py:4514` `WIKI_TREE`** | **СВОЯ КОПИЯ. `rg -c 'taxonomy' assistant.py` = 0** | **53** |
| **`reclass.py:31` `KNOWLEDGE_TREE`** | **СВОЯ КОПИЯ** (простыня в строковом литерале) | **62** |
| **`videosi.py:47` `KNOWLEDGE_TREE`** | **СВОЯ КОПИЯ** | **62** |
| `filemake.py` | седьмой потребитель: экспортирует вику в 280 файлов и имён категорий не знает ВООБЩЕ | 0 |
| `taxonomy.py:45` (источник) | — | **65** |

Расхождение измерено, а не оценено:

| копия | есть в копии, нет в источнике | есть в источнике, нет в копии |
|---|---|---|
| `assistant.WIKI_TREE` | **`6.1.2`** | 13: `1.1, 1.2, 1.3, 2.1, 2.2, 2.3, 2.4, 3.1, 3.2, 3.3` (промежуточные коды групп — кнопок у них и не должно быть) + **`8.1.1, 9.1.1, 10.1.1`** |
| `reclass.KNOWLEDGE_TREE` | 0 | `8.1.1, 9.1.1, 10.1.1` |
| `videosi.KNOWLEDGE_TREE` | 0 | `8.1.1, 9.1.1, 10.1.1` |

**Смысл расхождения по `6.1.2`:** кнопка у врача ЕСТЬ, а единственный источник этого
кода не знает. То есть дистиллятор его врачу не предложит, `checker` не посчитает,
`savdel` не выгрузит — раздел заморожен на том, что в нём уже есть. Волна 2 нашла это
как «знает только `assistant`»; после сведения трёх потребителей дефект не исчез, он
ПОМЕНЯЛ СТОРОНУ: теперь это единственный код, который есть у врача и отсутствует в
источнике.

`8.1.1 / 9.1.1 / 10.1.1` — обратный случай: источник их предлагает, кнопки нет, и
живых фактов под ними **0 каждый** (замер по боевой вике).

**Чем доказать:** `assistant.py` берёт подписи кнопок из `taxonomy.py`; поведенческий
тест: (а) каждый код, под которым в боевой вике есть хотя бы один факт, достижим
кнопкой; (б) каждый код, который дистиллятор предлагает модели, имеет имя в отчёте
`checker`; (в) число кнопок равно числу листьев источника. Проверка «в `assistant.py`
есть `import taxonomy`» НЕ считается.

### 5.2 Обрезка по границе предложения: СВЕДЕНА ПОЛНОСТЬЮ. Подтверждаю волну 4

`html_safe.py` — единственная реализация: `clip_at_sentence` (`:89`, возвращает
`(текст, отброшено)`) и однострочная обёртка `clip_at_sentence_text` (`:143`,
`return clip_at_sentence(text, limit)[0]`). Потребители:

```
assistant.py:973   _clip_at_sentence = html_safe.clip_at_sentence_text   (алиас)
assistant.py:1230  html_safe.clip_at_sentence(plain, _ARTICLE_SHOWN_MAX_CHARS)
distiller.py:127   from html_safe import clip_at_sentence
web_lookup.py:59   from html_safe import clip_at_sentence_text as clip_at_sentence
```

Своих копий `rfind('.')`/`rsplit` по границе предложения в дереве **нет ни одной**
(AST-поиск по всем 40 боевым модулям). Класс закрыт. **Оговорка про мой инструмент:**
первый прогон `wired.py` объявил `clip_at_sentence_text` «не вызывается боевым кодом» —
это ложное срабатывание, вызов идёт через алиас и `import as`. Метод «поиск вызовов по
имени» такие случаи не видит, и это ограничение всех трёх волн.

### 5.3 Разбор JSON от модели: ПЯТАЯ КОПИЯ ЖИВА, и она на пути врача. P1, НОВОЕ

Правильный сканер `distiller._iter_json_objects` (`:423`) существует, и `reclass.py:11`
его теперь ИМПОРТИРУЕТ (`from distiller import _iter_json_objects`) — это правка волны 4,
подтверждаю. `videosi.py` жадного разбора больше не содержит вообще (ни `rfind`, ни
`clean_json_raw`).

**Но в `assistant.py` живут ЧЕТЫРЕ сайта того же правила**, и их не назвала ни одна
волна:

| сайт | функция | что решается этим JSON |
|---|---|---|
| `assistant.py:1470` | `check_response_quality` | пускать ли черновик ответа врачу |
| `assistant.py:1538` | `check_llm_triage` | **отвечать ли вообще на вопрос врача в группе** |
| `assistant.py:5278` | `check_referee_triage` | вмешиваться ли в спор |
| `assistant.py:5838` | `check_and_send_group_activity_pings` | звать ли врачей в горячую дискуссию |

Правило в трёх из четырёх: `if "```" in text: start = text.find("{"); end =
text.rfind("}")`. Замер на восьми реалистичных ответах модели
(`C:\temp\recon_wave5\greedy.py`, сканер извлечён из `distiller.py` через `ast` и
исполнен без импорта модуля):

| вход | правило `assistant` (триаж) | правило `assistant` (валидатор) | сканер `distiller` |
|---|---|---|---|
| чистый объект | ok | ok | ok |
| в заборе ```` ```json ```` | ok | ok | ok |
| **болтовня ПОСЛЕ объекта в заборе** | **JSONDecodeError** | **JSONDecodeError** | ok |
| **болтовня ПЕРЕД объектом БЕЗ забора** | **JSONDecodeError** | ok | ok |
| **два объекта в заборе** | **JSONDecodeError** | **JSONDecodeError** | ok (2 объекта) |
| **объект + пояснение со скобкой** | **JSONDecodeError** | **JSONDecodeError** | ok |
| смайл-скоба внутри `reason` | ok | ok | ok |
| обрыв по длине | JSONDecodeError | ValueError | 0 объектов (восстановить нечего) |
| **ИТОГО отказов** | **5 из 8** | **4 из 8** | **1 из 8** |

Все восемь `json.loads` в `assistant.py` под `try` — падения нет (проверено
`jsonrisk.py`), но **есть деградация с последствием для врача**:

* `check_llm_triage` при отказе разбора возвращает `False` → **бот молча не отвечает на
  клинический вопрос**. В журнале `logger.error` есть, врач видит тишину.
* `check_referee_triage` → `False`, арбитр не вмешивается.
* `check_and_send_group_activity_pings` → **немой `return`** (это же сайт из §1.4).
* `check_response_quality` → `_unavailable`, черновик не провалидирован.

Итого: правильная реализация лежит в `distiller.py` в 15 строках, а на горячем пути
работает правило, которое отваливается на 5 входах из 8. **P1: цена — молчание бота на
вопрос врача.**

**Чем доказать:** все четыре сайта зовут одну функцию (её место — `html_safe.py` или
новый `json_safe.py`, а не `distiller.py`, потому что `assistant.py` дистиллятор не
импортирует); тест подаёт ответ с болтовнёй после объекта и требует, чтобы решение
триажа было ПРИНЯТО, а не выродилось в `False`.

### 5.4 Создание клиента к модели: владелец ОДИН, мимо него ходят ПЯТЬ. P2

| модуль | как создаёт | знает ли учёт `gemini_client` |
|---|---|---|
| `gemini_client.py:141/274` | `get_openai_client` → `get_provider_client` — владелец | да (баны, кулдауны, `_is_retryable_*`) |
| `vision.py:296` | `AsyncOpenAI(...)` напрямую | нет |
| `gemini_knowledge.py:213` | `genai.Client(api_key=...)` | нет |
| `reclass.py:385` | `genai.Client(api_key=current_key)` | нет |
| `videosi.py:133` | `genai.Client(api_key=api_key)` | нет |
| `blocking_tools.py:173` | `TavilyClient(api_key=...)` — другой провайдер, учёта нет вовсе | нет |
| `search_engine.py:12` | `TavilyClient(...)` | мёртвый модуль |
| `benchmark.py:38`, `delist.py:9` | `Groq` / `genai.Client` | обезврежены `SystemExit` |

Волна 4 (`_fix_key_health.md`) сделала публичный учёт в `gemini_client` и перевела на
него расшифровку голосового, а в своём же разделе «для лида» назвала `vision.py` и
`gemini_knowledge.py` как несделанное. **Замер подтверждает: не сделано.** Число «шесть
мест» из волны 3 держится, состав изменился только тем, что владелец стал явным.

Последствие для врача то же, что назвала волна 3: квота, которую съедает корпусный
скрипт или путь зрения по уже забаненной модели, — та же квота, из которой платится его
ответ.

### 5.5 Дубли, которых НИКТО НЕ НАЗЫВАЛ

| дубль | замер | P |
|---|---|---|
| **Список галлюцинаций Whisper — ДВЕ копии, БАЙТ-В-БАЙТ (10 фраз)**: `main.SILENCE_HALLUCINATIONS` (`main.py:1250`, модульная константа с комментарием «правило ОДНО на все места пути») против **локального `silence_hallucinations` внутри `assistant.handle_private_message` (`assistant.py:2857`)**. Волна 3 §5.4 объявила эту гипотезу снятой («пересечение 0, разные задачи») — она сравнивала `_SILENCE_DEMANDS`/`_SILENCE_PHRASES` (требование врача молчать), а НЕ этот локальный набор. Локальную переменную внутри функции не видит ни один обход присваиваний верхнего уровня — отсюда и промах | множества совпадают полностью в обе стороны. Последствие: голосовое в ГРУППЕ фильтруется по `main.SILENCE_HALLUCINATIONS`, голосовое в ЛС — по локальной копии. Сегодня расхождения нет; первая же правка «канонического» списка (у него есть объясняющий комментарий, значит править будут его) оставит ЛС на старом наборе, и врач получит `🎤 Распознано: «Субтитры сделал…»` как свою реплику | **P2** |
| `_env_int` **байт-в-байт в трёх модулях**: `gemini_client.py:25`, `main.py:47`, `vision.py:35` (md5 тела `ed614b6db0` у всех трёх) | 5 строк × 3; у всех трёх немой `except ValueError: return default` — то есть опечатка в переменной окружения молча даёт значение по умолчанию в трёх местах | P2 |
| `_RU_SUFFIXES` (25 строк) `assistant.py:801` против `RU_SUFFIXES` (25) `dental_vocab.py:205` | множества идентичны | P3 (назвала волна 3, не закрыто) |
| `get_env` и `parse_keys` байт-в-байт в `config.py` и `config.example.py` | так и задумано: шаблон обязан повторять форму живого конфига. **Не дубль-долг** | — |
| `_json_exit` `blocking_tools.py:88` (5 строк) против `media_tools.py:24` (4) | расходятся; оба — точка выхода подпроцесса | P3 |
| `init_db` `database.py:126` (180 строк) против `deppd.py:28` (23) | разные схемы разных баз, не дубль | — |
| `perform_search` `search_engine.py:33` против `search_engine_safe.py:10` | оба модуля мертвы (§2.2) | P3 |
| `patch_assistant.py` / `patch_assistant_v2.py` | 134 и 129 строк, оба обезврежены | P3 |
| `filemake.py` / `savdel.py` / `prompter.py` | три экспортёра вики в три каталога | P3 |

**Вложенная копия репозитория.** Волна 3 нашла `stomchat/stomchat/` (981 МБ, свой
`.git`). Каталог **на месте**: `fd -t d '^stomchat$'` в корне даёт его, `.gitignore`
его содержит. Как долг это уже принято (задание волны 5 предупреждает о нём в первом
абзаце), но обходы всё ещё надо фильтровать руками — и на этом «попались двое».

---

## 6. ДАННЫЕ, КОТОРЫЕ ВРАЧ НЕ УВИДИТ: пять чисел лида, из них ДВА неверны

Замер: `C:\temp\recon_wave5\data.py` и `data2.py`, обе базы строго
`sqlite3.connect('file:...?mode=ro', uri=True)`. Состояние баз:
`stomat_wiki.db` 9 158 656 Б, mtime **2026-02-19 17:48**;
`stomat_archive.db` 47 587 328 Б, mtime **2026-02-19 17:43**. За четыре волны в них
не писал никто.

### 6.1 Таблица

| что | число лида | МОЙ ЗАМЕР | вердикт | последствие для врача | P |
|---|---|---|---|---|---|
| «выжженные» реплики: помечены обработанными, но ни один факт на них не ссылается | 69 583 | **67 819** | **НЕ ПОДТВЕРЖДАЮ** — ни при одном из шести определений (см. 6.2) | 67 819 реплик коллег прошли через дистиллятор и не оставили ни одного факта. Второй прогон их не возьмёт: флаг стоит. Врач их не найдёт ни поиском, ни кнопкой | **P1** |
| «запертые» — не обработаны и не могут быть | 444 | **444** | **ПОДТВЕРЖДАЮ ТОЧНО** | все 444 — `media_type='file'`, все 444 `has_media=1`, у всех 444 `vision_description` пусто. Очередь дистилляции даёт **0 кандидатов**: 117 403 помечены, 444 непомеченных все до одного медиа без зрения | **P2** |
| факты без кнопки у врача | 51 | **51** | **ПОДТВЕРЖДАЮ ТОЧНО** | 51 факт под кодами, которых нет ни в одной копии дерева (`2.1` — 15, `2.0.0` — 13, `1.1.0` — 12, `1.0.0` — 9, `3.0.0` — 9, `2.2` — 9, `2.0` — 8, `6.3` — 7, `1.1` — 6, `10.1` — 5 и обрубки). Следы ранних прогонов с другой разметкой | P3 |
| коды в вике без имени в единственном источнике | 49 | **49** | **ПОДТВЕРЖДАЮ ТОЧНО** | из 110 кодов-токенов живой вики 49 отсутствуют в `taxonomy.KNOWLEDGE_TREE`: `1.0.0, 1.1.0, 1.2.0, 1.3.0, 1.4, 1.6, 10.1, 2.0, 2.0.0, 2.1.0, 2.1.5, ... 6.1.2, 6.1.3, 6.2, 6.3, 6.3.3, 6.5.1, 7.0.0` и др. Среди них **`6.1.2`, у которого кнопка у врача ЕСТЬ** (§5.1) | P2 |
| видео-протоколы с пустым провенансом | 52 из 53 | **0 из 53 пустых** | **ОПРОВЕРГАЮ формулировку.** Число 52 верно, слово «пустой» — нет | см. 6.3 | **P2** |

Плюс подтверждено без изменений (те же числа, что у волн 2-3, база не менялась):
`media_links` непусто — **0 из 12 784**; фактов, где ВСЕ токены `source_ids` нечисловые —
**353**; нечисловых токенов — **2 073**; последняя дата архива — **2026-02-19 13:13:49**
(**160 дней** корпус не пополнялся); индексов в вике один (`idx_cat`); колонок
`category_code_prev` и `content_hash` НЕТ.

Ловушки задания перемерены и все три сошлись: значений `category_code` с запятой —
**12 667 из 12 784**; различных значений — **6 711**; кодов-токенов — **110**.

### 6.2 Почему «69 583» не сходится: шесть определений, ни одно не даёт этого числа

```
всего реплик в архиве                                          117 847
упомянуто хотя бы одним фактом (уникальных id в source_ids)     50 139
   из них РЕАЛЬНО существует в архиве                           49 597
   ссылок на msg_id, которых в архиве НЕТ ВООБЩЕ                    542   <- новая находка

A. все реплики минус упомянутые                                 68 250
B. помеченные обработанными минус упомянутые                    67 819
C. реплики с непустым текстом минус упомянутые                  61 286
D. помеченные И с текстом минус упомянутые                      61 238
E. помеченные, с текстом, без медиа, минус упомянутые           58 456
F. помеченные, текст длиннее 30 символов, минус упомянутые      34 830
```

Ближайшее к 69 583 — вариант A (68 250), разница 1 333. **Скорее всего число лида взято
до какого-то прогона либо считалось по другому полю.** Правильная формулировка, которую
я готова защищать: «**67 819 реплик помечены обработанными, и ни один из 12 784 фактов
на них не ссылается**» (вариант B — он единственный отвечает на вопрос «сеялка прошла и
ничего не оставила»). Если считать только те, из которых В ПРИНЦИПЕ можно было извлечь
факт (непустой текст) — **61 238**.

**Побочная находка, которую не назвала ни одна волна: 542 ссылки на `msg_id`, которых
в архиве нет.** Это не «нечисловые токены» (те 2 073 посчитаны отдельно) — это
корректные числа, ведущие в пустоту. Врач, попросивший «покажи источник», получит
ссылку на несуществующее сообщение.

### 6.3 Видео-протоколы: провенанс не ПУСТОЙ, а ЛОЖНЫЙ — и это хуже

```
видео-протоколов в вике (content LIKE '%ВИДЕО-ПРОТОКОЛ%')        53
   source_ids ПУСТО                                              0
   source_ids ссылается на СУЩЕСТВУЮЩУЮ реплику архива           35
      ...и у той реплики has_media = 1                            6
      ...и media_type = 'video'                                   1
   source_ids ссылается на НЕСУЩЕСТВУЮЩИЙ msg_id                 18
```

То есть **52 из 53 имеют провенанс, который не выдерживает проверки** (единственный
выживший — тот самый `msg_id 1861`, который назвала волна 4), но ни один не пуст.
Пустое поле говорит «неизвестно»; число 1377 говорит «это сообщение 1377» — и ведёт в
никуда (18 случаев) или на текстовую реплику без видео (29 случаев).

Последствие для врача: ссылка на «первоисточник» видео-разбора в 52 случаях из 53 либо
не открывается, либо открывает чужое текстовое сообщение. Правка кода волны 4 лечит
БУДУЩИЕ импорты; эти 53 строки лечит только UPDATE по боевой базе, которого не делал
никто (и правильно — база только на чтение).

**Чем доказать:** миграция под бэкап (`reclass.backup_before_write` — проверенный
механизм) обнуляет `source_ids` у 52 строк, не прошедших строгий фильтр
(существует + `has_media=1` + `media_type='video'`), и тест на копии проверяет, что
после миграции строк с ложным провенансом ноль, а с пустым — 52.

### 6.4 Оговорка, без которой вся эта таблица вводит в заблуждение

`.gitignore` содержит `*.db` (строка 34-40). Значит **`stomat_wiki.db` и
`stomat_archive.db` на боевой машине — ДРУГИЕ ФАЙЛЫ**, и все числа этого раздела (и
разделов волн 2-3) описывают февральский снимок на ЭТОЙ машине. Там, где `reclass.py`
или `videosi.py` реально что-то перезапишут, состав данных другой и, скорее всего,
свежее. Ни одна волна не могла это проверить, и я тоже.

---

## 7. РИСКИ ПЕРВОГО ЖИВОГО ЗАПУСКА

Ни одного живого прогона бота в этой сессии не было. Ниже — что вероятнее всего сломается
и как проверить ДЕШЕВО, без Telegram. Инструменты: `C:\temp\recon_wave5\cfg.py`,
`cfg2.py`.

### 7.1 Главный риск: `config.py` в `.gitignore`, значит на боевой машине он СТАРЫЙ

Замер контракта конфигурации по замыканию импортов `main.py` (13 модулей):
**16 имён читаются ЖЁСТКО** (`config.X`), **3 через `getattr` с запасом**.

| имя | где читается | что будет, если старый `config.py` его не объявляет |
|---|---|---|
| `API_ID`, `API_HASH`, `SESSION_NAME` | **уровень модуля** `main.py:912-928` | `AttributeError` при импорте — **бот не поднимается**. Риск ноль: бот раньше работал, значит они есть |
| `SOURCE_CHAT_ID` | **уровень модуля** `main.py:1801-1809` | то же; риск ноль по той же причине |
| **`SEARCH_PROVIDER`, `TAVILY_API_KEY`** | `blocking_tools.py:169` — **внутри `_web_search_sync`, ВНЕ окружающего `try`** (`try` начинается строкой ниже, на 170) | **P1. Бот поднимется, а первая же команда `/web` от врача упадёт.** Отказ не молчаливый: `_web_search_sync` живёт в подпроцессе, верхний `except Exception` (`blocking_tools.py:1004`) превращает его в `_json_exit({"ok": false, "error": ...}, 1)`, и родитель это запишет. Но **врач получит «Ошибка поиска» на 100 % запросов, пока `config.py` на боевой машине не обновят**, и ни одна проверка при старте об этом не скажет |
| `REPORT_TARGETS` | `main.py:591`, **под `try: ... except Exception` с громким `logger.error`** («ни дайджест, ни недельная сводка не уйдут НИКОМУ») | **защищено правильно** — валидатор П4 ловит и `AttributeError`. Единственное имя из новых, сделанное как надо |
| `TELEGRAPH_TOKEN`, `GEMINI_MODEL`, `GROQ_MODEL`, `GOOGLE_KEYS`, `GROQ_KEYS`, `BOT_TOKEN`, `DB_PATH`, `REPORT_CHAT_ID`, `REPORT_HOUR` | внутри функций | старые имена, почти наверняка есть |
| `DENTAL_KEYWORDS` (`dental_vocab.py:92`) | `getattr` с запасом, **нигде не объявлено** | работает на встроенном списке 276 слов ВСЕГДА; настройкой не управляется |
| `GROQ_VISION_MODEL`, `SUMMARY_INTERVAL`, `MAX_MSGS` | объявлены в `config.py` и **не читаются никем** | мёртвый груз в контракте |

**Поправка к моему же инструменту, честно.** Первый прогон `cfg.py` объявил `API_ID`,
`REPORT_TARGETS` и `SOURCE_CHAT_ID` «не объявленными в `config.py`» — это ЛОЖНАЯ
ТРЕВОГА: мой сборщик читал только присваивания верхнего уровня, а в `config.py` эти три
собираются внутри `try`/`if`. Пересчёт с учётом вложенных присваиваний: локальный
`config.py` объявляет **23 имени**, `config.example.py` — **те же 23**. То есть шаблон
волны 4 форму живого конфига повторяет точно.

### 7.2 Дешёвая проверка на все 16 имён, которой сейчас нет

Самопроверка при старте, ДО подключения к Telegram: пройти по списку из 16 жёстко
читаемых имён, `hasattr(config, имя)`, и записать одной строкой ERROR те, которых нет,
назвав последствие («`/web` не будет работать», «сводка не уйдёт»). Это ~10 строк в
`main.py` и превращает «врач жмёт `/web` и видит ошибку» в одну строку в журнале при
загрузке. `test_config_contract.py` (489 строк) проверяет ШАБЛОН — на боевой машине
шаблон никто не читает.

Проверить дёшево, без Telegram, можно уже сейчас:
`python -c "import ast,sys; ..."` по списку из моего замера против боевого `config.py`
(файл на боевой машине, отдать список именами — значений не нужно).

### 7.3 Второй риск: эмодзи в аварийной печати

| файл | в `.gitignore` | эмодзи в `print`/`raise` | вердикт |
|---|---|---|---|
| `config.py` | **да** (строка 5) | **0** в локальной копии | на боевой машине копия ДРУГАЯ и её содержимое неизвестно. Если там есть `print("❌ ...")` при отсутствии переменной, он упадёт `UnicodeEncodeError` под `chcp 866`/`1251` ДО настройки журнала, и в логе не останется ничего |
| `id.py` | **да** (строка 6) | **3** (`:17` 🚀, `:23` 📋, `:35` ℹ️) | модуль не импортирует никто, запускается руками. Риск только при ручном запуске, но он РЕАЛЬНЫЙ: у `id.py` нет преамбулы `reconfigure` (у 13 других инструментов она есть) |
| `main.py`, `runtime_guard.py`, `config.example.py` | нет | **0** | преамбула `reconfigure(encoding='utf-8')` на `main.py:9-12` стоит первой, до всего остального. Это правильное лечение и его нельзя убирать |

**Дешёвая проверка:** `chcp 866 && python -c "import id"` — нет, `id.py` без `__main__`
защиты не годится. Правильно: `cmd /c "chcp 866 & python config.py"` на боевой машине
(модуль исполним и ничего не делает, кроме чтения окружения) — если упадёт
`UnicodeEncodeError`, эмодзи там есть.

### 7.4 Третий риск: ffmpeg. Заявка волны 2 в этой части НЕВЕРНА

Волна 2 (§6.4, P1) нашла рабочий `ffmpeg.exe` 99 264 000 Б в корне репозитория и
предложила добавить корень скрипта в список кандидатов `ffmpeg_binary()`.

**Замер: `ffmpeg.exe` стоит в `.gitignore` (строка 60).** То есть он ЛОКАЛЬНЫЙ и на
боевую машину не уезжает вовсе. Добавление корня в кандидаты не починит ничего там, где
это нужно, — оно только замаскирует проблему на машине разработчика.

Состояние `ffmpeg_binary()` (`gemini_client.py:870-911`) на сегодня: 4 кандидата —
`STOMCHAT_FFMPEG_PATH`, `shutil.which("ffmpeg")`, `imageio_ffmpeg`, `static_ffmpeg`;
корня репозитория среди них нет; отказ пишет громкий `logger.warning` с названием
последствия («нештатные контейнеры и файлы больше N Б расшифрованы НЕ БУДУТ») и
подсказкой про переменную. **Это правильная форма отказа.**

Настоящий незакрытый пункт: `.env.example` **не существует** (`ls` — файла нет), при
том что `.gitignore:4` содержит `!.env.example` — исключение написано под файл, которого
нет. Значит имя `STOMCHAT_FFMPEG_PATH` (как и 16 имён конфигурации) на боевой машине
взять негде. Это заявка `_fix_config_contract.md` «для лида» п.1, не сделана.

**Дешёвая проверка:** `python -c "import gemini_client as g; p=g.ffmpeg_binary(); print(p)"`
на боевой машине плюс `p -version` — но `gemini_client` тянет конфиг, поэтому дешевле
`where ffmpeg` и `echo %STOMCHAT_FFMPEG_PATH%`.

### 7.5 Четвёртый риск: `/web` целиком не проверен на живом провайдере

Веб-поиск сшит в волне 3, покрыт `test_web_lookup.py` (110 проверок) и
`test_search_command.py`, но каждый отчёт волны 4 честно пишет: транспорт заглушён.
На боевой машине не проверено: (а) стоит ли пакет `ddgs`/`tavily`, (б) верен ли
`SEARCH_PROVIDER`, (в) отвечает ли провайдер из этой географии.
Волна 2 замерила отдельно: `gemini_client.py:88` считает `failed_precondition`
временной ошибкой, а живой журнал показывал 45 записей
`400 FAILED_PRECONDITION ... User location is not supported` — **этот пункт до сих пор
не сделан** (см. §8).

**Дешёвая проверка без Telegram:** `python blocking_tools.py web_search '{"query":"..."}'`
— модуль имеет `__main__` и печатает JSON. Один запуск на боевой машине отвечает сразу
на (а), (б) и (в).

### 7.6 Пятый риск: состояние на диске переживает откат кода

`.gitignore` держит вне версий `assistant_state.json`, `banned_models.json`,
`key_cooldowns.json`, `bot_*.json`, `*.session`, `*.db`, `*.log`. Комментарий в
`.gitignore` это объясняет правильно. Следствие для первого запуска: **боевой
`assistant_state.json` был записан СТАРЫМ кодом**, а немые обработчики §1.4
(`assistant.py:2230`, `:5904`, `:5151`, `:2575`) — это ровно разбор полей этого
состояния. Если форма поля изменилась за четыре волны, врач получит не отказ, а
молчаливый сброс кулдауна/голосов/истории кейса.

**Дешёвая проверка:** прогнать боевой `assistant_state.json` (файл, не содержимое —
в нём реальные Telegram id) через загрузчик `assistant.load_state` на машине
разработчика и напечатать, какие поля не разобрались. Это единственный из пяти рисков,
который проверяется полностью офлайн и мгновенно.

---
