# Recon: содержимое stomat_wiki.db как продукт (read-only)

База: `stomat_wiki.db`, таблица `distilled_facts`, 12784 строки, ни одной пустой.
Длина `content`: min 38 / med 236 / p90 359 / max 5477 символов.

---

## Находка 1. Русские аббревиатуры в БЗ недостижимы поиском: SQLite LIKE регистрозависим для кириллицы

**Место:** `assistant.py:510` (`text.lower()`) + `assistant.py:793`
(`WHERE content LIKE ?`), тот же дефект на `assistant.py:2846`, `4043`, `4070`.

**Механика.** `extract_keywords` приводит запрос к нижнему регистру
(`assistant.py:510`: `re.sub(r"[^\w\s-]", " ", text.lower())`). Поиск идёт
`content LIKE '%внчс%'`. Дефолтный SQLite LIKE регистронезависим **только для
ASCII** — для кириллицы он регистрозависим. Латиница поэтому работает
(`LIKE '%bopt%'` = `LIKE '%BOPT%'` = 163 строки), а кириллические аббревиатуры,
которые в базе записаны капсом, не находятся никогда.

**Замер** (левая колонка — сколько строк реально вернёт бот, правая — сколько
фактов в базе содержат термин без учёта регистра):

| термин | находит бот | есть в базе | потеряно |
|---|---|---|---|
| ВНЧС | **0** | 88 | 88 |
| ПММА | 3 | 183 | 180 |
| ММА | 65 | 246 | 181 |
| КЛКТ | **0** | 21 | 21 |
| МРТ | **0** | 39 | 39 |
| ВертиПреп | 297 | 370 | 73 |
| КТ | 3264 | 3401 | 137 |
| СИЦ | 4 | 29 | 25 |
| ТРГ | **0** | 5 | 5 |
| ОПТГ | **0** | 3 | 3 |
| БОПТ | **0** | 4 | 4 |
| ЭОД | **0** | 2 | 2 |

Итого только по этим 12 терминам недостижимо 758 попаданий. Общий масштаб:
**8522 из 12784 фактов (66.7%)** содержат хотя бы одно слово (>=4 букв),
которое встречается в факте ИСКЛЮЧИТЕЛЬНО с большой буквы, то есть для
нижнерегистрового ключа этого слова факт невидим. Топ потерянных клинических
токенов: `пмма` 179 фактов, `внчс` 86, `вертипреп` 60, `препарирование` 179,
`диагностика` 103, `удаление` 61.

**Сценарий отказа.** Врач пишет «болит ВНЧС после протезирования».
`extract_keywords` даёт ключ `внчс` (4 символа, проходит порог
`assistant.py:519`, и он же помечен как dental — `dental_vocab.is_dental_keyword('внчс')`
= True, значит уходит в поиск ПЕРВЫМ, `assistant.py:536-538`). Запрос
`content LIKE '%внчс%'` возвращает 0 строк из 88 существующих. Бот отвечает без
справки, хотя в базе 88 фактов ровно по теме.

**Минимальная правка.** Искать по нормализованному тексту в обе стороны:
заменить `WHERE content LIKE ?` на `WHERE content LIKE ? COLLATE NOCASE`
не поможет (NOCASE тоже ASCII-only) — нужно либо
`conn.create_function('lc',1,str.lower)` + `WHERE lc(content) LIKE ?`, либо
хранить рядом колонку `content_lc` и искать по ней.

**Побочно (тот же запрос, отдельная причина):** `мрт`, `трг`, `эод`, `сиц` —
3 символа, отбрасываются порогом `assistant.py:519` до всякого SQL, потому что
их нет в `dental_vocab.SHORT_DENTAL_TERMS` (там всего 8 элементов: `бор, зуб,
игл, кт, мту, свш, сдг, сст`).

---

## Находка 2. Рубрикатор врёт: в половине подтем большинство статей не о теме кнопки

**Место:** рубрики в БД (`category_code`, проставлен `reclass.py:171-180`),
показываются кнопками из `assistant.py:3819-3892` (`WIKI_TREE`), листаются
`assistant.py:4008-4055`.

**Замер.** Для каждой подтемы взял её факты по атомарному коду и посчитал долю,
в тексте которой есть хоть одно слово самой темы (широкий набор корней +
латиница + синонимы):

| кнопка (что видит врач) | статей | о теме | доля |
|---|---|---|---|
| 📸 Фотопротокол | 1836 | 155 | **8.4%** |
| 💧 Спиртовой протокол | 1296 | 42 | **3.2%** |
| ✨ Полировка | 1316 | 153 | **11.6%** |
| 💊 Фармакология | 1250 | 174 | **13.9%** |
| 🖨 3D-печать | 316 | 73 | **23.1%** |
| 🔬 Оптика и оборудование | 1697 | 423 | **24.9%** |
| ⚖️ Юридические вопросы | 352 | 93 | **26.4%** |
| 🦷 ВНЧС, сплинты и шины | 505 | 160 | **31.7%** |
| 💎 Виниры | 1426 | 485 | **34.0%** |
| 🧪 Адгезия и IDS | 2002 | 714 | **35.7%** |
| 🗣 Психология и общение | 531 | 227 | **42.7%** |
| 💰 Экономика и цены | 620 | 288 | **46.5%** |
| 🧴 Фиксация и цементы | 2466 | 1161 | **47.1%** |
| 📐 Окклюзия | 1878 | 972 | 51.8% |
| 👑 Коронки | 3734 | 2236 | 59.9% |
| 🔩 Имплантация | 1451 | 1358 | 93.6% (норма) |
| 🦷 Полные протезы | 239 | 215 | 90.0% (норма) |

14 подтем из 24 проверенных — ниже 50%. Пять из них ниже 25%.

**Проверка выборкой (не синонимы, реально другая тема):**
- `id 8472`, код `6.3.1, 6.1.1` (Фотопротокол): «отклеивание окуляров от оправы
  в микроскопах UniVet, гарантийный срок 2 года».
- `id 11841`, код `2.4.2, 6.3.1, 2.4.1` (Фотопротокол): про ретромолярные
  бугорки и съёмные протезы.
- `id 7396`, код `2.2.1, 1.2.6, 2.1.2, 2.2.2` (Полировка): про вертипреп
  с уступом и без.
- `id 6209`, код `2.2.6, 6.2.1, 2.1.2` (Фармакология): про благородные сплавы
  в металлокерамике.
- `id 7278`, код `1.2.2, 1.2.1, 2.1.2` (Спиртовой протокол): про механические
  свойства фрезерованного дисиликата.

**Причина, механически.** `reclass.py` переклассифицировал **все 12784 факта**
(`is_reclassified=1` у 12784/12784) промптом `reclass.py:102-107`, где правило 1
требует «укажи ВСЕ подходящие коды», а лимит — 5. Результат: 3 кода у 6077
фактов, 4 кода у 3952, 5 кодов у 958; в среднем ~3.4 рубрики на факт. При 110
кодах и таком мультитеге любая узкая рубрика становится свалкой. **2171 факт
(17%) висит в кодах сразу из 3+ РАЗНЫХ глав** дерева (Терапия/Ортопедия/
Хирургия/Общее/Менеджмент), 224 (1.8%) — из 4+.

**Отдельно: расхождение таксономий.** Одни и те же цифры значат разное в трёх
местах:

| код | `distiller.py:28,31` (чем факты размечались изначально) | `reclass.py:26-30,38-39` (чем переразметили) | `assistant.py:3830,3844-3845` (что видит врач) |
|---|---|---|---|
| 1.2.2 | Морфология/Бугры/Фронт | Спиртовой протокол | 💧 Спиртовой протокол |
| 1.2.4 | Полировка/Финиш | Матричные системы | 📎 Матрицы |
| 2.1.2 | Накладки/Вкладки | Коронки | 👑 Коронки |
| 2.1.3 | Коронки/Мосты | Мостовидные протезы | 🌉 Мосты |

`WIKI_TREE` совпадает с `reclass.py`, так что подписи кнопок формально
согласованы с последней разметкой — но 1.2.2 «Спиртовой протокол» с 3.2%
попаданием в тему выглядит именно как остаток старой семантики
(1.2.2 = морфология/композит: в выборке ровно композитные факты).

**Минимальная правка.** Ограничить мультитег до 1-2 кодов и хранить первый как
primary (`category_code_primary`), листать рубрики по нему, а не по `LIKE`;
переразметку гонять от одной таксономии, совпадающей с `WIKI_TREE`.

---

## Находка 3. `set()` в reclass.py размножил один и тот же набор кодов в 6711 строк-перестановок

**Место:** `reclass.py:132` — `return ", ".join(list(set(clean_codes)))`.

**Замер.** Уникальных наборов кодов — 4788, а уникальных строк `category_code` в
базе — **6711**. 1159 наборов лежат в базе в двух и более порядках; худший набор
`{2.1.1, 2.1.2, 2.2.1, 2.2.2}` записан **9 разными строками** (`'2.1.1, 2.2.1,
2.1.2, 2.2.2'`, `'2.1.2, 2.1.1, 2.2.2, 2.2.1'`, ...). Причина — порядок обхода
`set()` зависит от `PYTHONHASHSEED`, а переразметка шла по одному факту за
запрос (`reclass.py:166-187`), процессами/сессиями.

**Сценарий отказа.** Строка `category_code` уходит врачу в справку как ярлык
факта: `assistant.py:801` — `_corpus_entry(f"[{row[0]}]", row[1])`. Один и тот же
факт получает разный ярлык `[2.1.2, 2.2.2, 2.2.1]` / `[2.2.1, 2.1.2, 2.2.2]`, и
любая группировка/дедуп по `category_code` (а не по телу) считает их разными
рубриками. Тот же дефект уже ловили в дедупе корпуса —
см. комментарий `assistant.py:757-767`; в самих данных он не исправлен.

**Побочно:** `reclass.py:123,132,139` при сбое LLM пишут фиктивный код `10.1`,
которого нет ни в одной таксономии — 5 фактов. И регулярка `reclass.py:128`
разрешает 2-уровневые коды вопреки правилу «Выбирай L3» (`reclass.py:104`),
из-за чего **51 факт (0.4%) не имеет ни одного кода из `WIKI_TREE`** и в
рубрикаторе недостижим совсем (коды `2.1` — 15 фактов, `2.0.0` — 13,
`1.1.0` — 12, `10.1` — 5, ...).

**Минимальная правка.** `sorted(dict.fromkeys(clean_codes))` вместо
`list(set(...))`; отбрасывать коды, которых нет в белом списке L3.

---

## ОПРОВЕРГНУТО (проверял, дефекта нет)

- **Факты-вопросы.** Ни один из 12784 фактов не заканчивается на `?` (0.00%).
  Фильтр `NOT LIKE '%?%'` нужен архиву (`assistant.py:827`), вики — нет.
- **Факты-обрубки.** Без финальной пунктуации — 1 факт из 12784 (`id 12713`).
  Начинающихся с маленькой буквы — 0. Короче 80 символов — 108 (0.84%),
  минимальная длина 38 символов. Пустых `content` — 0.
- **Дубли по смыслу.** Точных дублей тела — 6 строк (0.05%, 3 тела, максимум
  5 копий: факт про BOPT, `id 2025/4144/7060/12615`). Near-duplicate пар с
  Jaccard>=0.75 по токенам 4+ букв — 22 пары, вовлечено 20 фактов (0.16%).
  Дедуп в `assistant.py:797-800` их и так снимает по нормализованному телу, а
  пагинация — через `GROUP BY content` (`assistant.py:4034`).
- **Ложные попадания `category_code LIKE '%код%'`.** Проверил все 24 подтемы:
  0 фактов, попавших в подтему из-за подстрочного совпадения кода.
  Трёхуровневые коды в этой базе не являются подстроками друг друга.
