# _fix_timing — флакующая тайминговая проверка в test_search_case.py

Лана: флакующая проверка, которая портит доверие к полному прогону.
Мои файлы: `test_search_case.py`, `test_case_simulator.py`.
Git не трогаю. Продакшн-код (`assistant.py`) НЕ ТРОГАЮ — ни на диске, ни на минуту.

## 0. Базовое состояние (замер, не оценка)

md5 ДО любых правок:

```
5a2f5a71efc8bf0db5d5372f242c9b6a  test_search_case.py
169a0d759e154b0116bdc9e050e0c0f4  test_case_simulator.py
16042cc80625599ea1fdf86d97f571d5  assistant.py   (НЕ мой, только контроль неизменности)
```

Одиночный прогон `test_search_case.py` на тихой машине: PASSED 33 / FAILED 0.
Замер секции [7] в этом прогоне: было 29.9 мс, стало 71.0 мс -> отношение **2.37**
при пороге `naive*4 + 5` = 124.6 мс.

Python 3.13.14, sqlite3 3.50.4.

## 1. Почему падало именно ОТНОШЕНИЕ (замер, 12 пар подряд, `_measure_timing.py` D)

Каждый замер уже брался как минимум из 7 прогонов. Всё равно:

| величина | min | max | медиана | разброс max/min |
|---|---|---|---|---|
| naive (1 LIKE), мс | 29.26 | 97.96 | 31.59 | **3.35** |
| fixed (3 LIKE), мс | 61.31 | 84.47 | 74.30 | 1.38 |
| отношение fixed/naive | 0.77 | 2.79 | 2.23 | **3.64** |

Вывод числом: отношение дрожит СИЛЬНЕЕ каждого из двух замеров (3.64 против 3.35 и
1.38), потому что ошибки числителя и знаменателя независимы и умножаются. Запас до
порога 4.0 по худшему наблюдённому отношению — всего **1.43x**, и в полном прогоне
свиты его съело: 17.7 против 77.8 мс = 4.40.

Отдельно стоит заметить: в упавшем прогоне сломался ЗНАМЕНАТЕЛЬ. naive показал 17.7 мс
против 29.3 мс минимума на тихой машине — то есть минимум из 7 не защищает от того,
что в другом процессе запрос окажется в 1.7 раза БЫСТРЕЕ (тёплые страницы, состояние
турбо-частоты). Минимум устойчив только к замедлению, а отношение ломает и ускорение.

## 2. Что выбрано и почему: ЧИСЛО ШАГОВ SQLite вместо времени

Из четырёх предложенных вариантов взяты два, оба не тайминговые:

**(а) Счётчик шагов виртуальной машины SQLite** — `Connection.set_progress_handler(fn, 1)`
вызывает `fn` на каждую инструкцию VDBE. Это цена запроса в «попугаях», не зависящая
от нагрузки ВООБЩЕ.

Детерминированность, 5 прогонов подряд (`_measure_timing.py` A):

```
naive (1 LIKE): [51177, 51148, 51148, 51148, 51148]   <- первый прогон +29 шагов (разбор схемы)
fixed (3 LIKE): [127679, 127678, 127678, 127678, 127678]
```

Первый вызов дороже на 0.06% из-за холодной схемы — поэтому в тесте перед замером
идёт прогревочный прогон. После прогрева на продакшн-пути дрожание **0.0000%** на 5
прогонах (`_measure_timing2.py` A: 2249257 шагов пять раз подряд, ни одного разброса).

**Модель стоимости оказалась ТОЧНОЙ** (`_measure_timing3.py` A), и это ключ к порогу:

| k форм LIKE | шагов | на строку | формула 1+3k | отношение к k=1 |
|---|---|---|---|---|
| 1 | 51148 | 4.001 | 4 | 1.000 |
| 2 | 89501 | 7.001 | 7 | 1.750 |
| 3 | 127854 | 10.001 | 10 | **2.500** |
| 4 | 166207 | 13.001 | 13 | **3.250** |
| 5 | 204560 | 16.001 | 16 | 4.000 |
| 6 | 242913 | 19.001 | 19 | 4.750 |
| 8 | 319619 | 25.001 | 25 | 6.250 |

Отношение к одиночному LIKE = (1+3k)/4 с точностью до 0.001. Величина нормирована на
строку, поэтому от РАЗМЕРА базы не зависит: вика вырастет с 12 784 до 20 000 фактов —
отношение останется 2.500.

Порог поставлен **3.0** — между тремя формами (2.500) и четырьмя (3.250). Это делает
проверку СТРОЖЕ прежней, а не мягче: прежний порог 4.0 пропускал и четвёртую форму
(3.25 < 4.0) и пятую (4.00 <= 4.0+запас). Новый ловит четвёртую.

**(б) Число обращений к базе на один поиск** — `Connection.set_trace_callback`, считаем
SQL-операторы на реальном `search_knowledge_corpus`. Замер (`_measure_timing3.py` B):

```
ключей=1: операторов 4, SELECT 2, PRAGMA 2   -> 2.00 SELECT на ключ
ключей=2: операторов 6, SELECT 4, PRAGMA 2   -> 2.00 SELECT на ключ
ключей=4: операторов 10, SELECT 8, PRAGMA 2  -> 2.00 SELECT на ключ
```

Ровно одна выборка на ключ в вике и одна в архиве. Это структурный инвариант
(`_CORPUS_TOTAL_ROW_BUDGET=48` < `_CORPUS_CANDIDATE_CAP=60`, поэтому цикл по ключам
никогда не обрывается), и он ловит целый класс регрессий, которого тайминг не видел.

От медианы и от «больше прогонов» отказался: они уменьшают дрожание, но не убирают
его — источник в самой величине. Абсолютный тайминговый потолок ОСТАВЛЕН (см. ниже),
потому что он ловит то, чего шаги не видят.

## 3. Проверка «убери LIMIT» — задание не подтвердилось наполовину, и это важно

Задание предлагало доказать ловлю регрессии снятием LIMIT. Замерил
(`_measure_timing2.py` C и `_measure_timing4.py` C), три ключа:

- по ШАГАМ снятие LIMIT стоит всего **+11%** (2 249 257 -> 2 485 523). Порог «не более
  чем втрое» этого НЕ ловит и ловить не может.
- зато по ЧИСЛУ ЗАПРОСОВ ловится начисто: **SELECT-ов 6 -> 2**. Первый же ключ набирает
  `_CORPUS_CANDIDATE_CAP=60` кандидатов, срабатывает `break`, и остальные два ключа
  врача В БАЗУ НЕ УХОДЯТ ВООБЩЕ.
- и выдача при этом становится ХУЖЕ, а не «просто дороже»: справка 20 строк -> 16,
  архив 20 строк -> 13. То есть последствие для врача — не задержка, а молча
  проигнорированные ключи вопроса.

Так что «убери LIMIT -> проверка падает» верно, но не по той причине, что
предполагалась: ловит это счётчик запросов, а не счётчик цены.

## 4. Что изменено

`test_search_case.py`, секция [7] — сравнение двух таймингов заменено на:

- `счётчик цены детерминирован: два прогона дают одно число` — сам анти-флак-инвариант
  теперь проверяется, а не предполагается;
- `счётчик действительно считает, а не молчит` (шагов >= числа строк) — иначе при
  отключённом progress handler оба числа стали бы нулями и проверка отношения превратилась
  бы в пустышку, «зелёную» при любой поломке;
- `три формы дороже одной не более чем втрое` (замер 2.496 при пороге 3.0);
- абсолютный потолок по часам ОСТАВЛЕН (`fixed_ms < 500`, замер max 84 мс за 12 прогонов,
  запас 6x): он ловит то, чего шаги не видят — если медленным станет диск или база уйдёт
  в WAL под чужим писателем, число шагов не изменится, а врач будет ждать секунды;
- `вариантов LIKE ровно три` — оставлено как было.

`test_search_case.py`, новая секция [7a] — продакшн-путь `search_knowledge_corpus`
под трассировкой операторов:

- `на каждый ключ по одной выборке в вике и в архиве` (6 SELECT на 3 ключа);
- `каждая выборка ограничена LIMIT`;
- `строк на ключ не больше запаса кандидатов (60)` — ловит и снятый, и раздутый LIMIT;
- `справка по трём ключам не пуста` — чтобы трасса не «проходила» на пустом результате.

Трассирующее соединение попутно уводит боевые базы в `mode=ro`: продакшн открывает их
через `sqlite3.connect("stomat_wiki.db")`, то есть НА ЗАПИСЬ, и на время замера эта
ручка становится физически нерабочей на запись.

`test_case_simulator.py` — ruff F841: `_corpus_error = sqlite_error = RuntimeError(...)`
-> `_corpus_error = RuntimeError(...)`. Сама проверка не тронута.

Заодно в `test_search_case.py` убраны два ruff E741 (`l` как имя переменной -> `ln`).
После правок в моих двух файлах остаётся только E402 (3 шт.) — это репозиторный
паттерн, `config.DB_PATH` обязан ставиться до `import database`; по всему репозиторию
E402 встречается 128 раз.

Прогоны после правки: `test_search_case.py` **39/0** (было 33/0),
`test_case_simulator.py` **24/0**, `test_rag_quality.py` **53/0**.

md5 ПОСЛЕ правок:

```
c53c1d248181116e713615c69e9435b2  test_search_case.py
3a59c94052663a19df7279811fe2ab84  test_case_simulator.py
16042cc80625599ea1fdf86d97f571d5  assistant.py   (НЕ изменился, совпадает с исходным)
```
