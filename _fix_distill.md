# Правки конвейера дистилляции

Владение: `distiller.py`, `dental_vocab.py`, `test_distill_pipeline.py`. Всё остальное — патчем ведущему.
БД открывались ТОЛЬКО через `file:...?mode=ro` URI. Ни одной записи в боевые БД не сделано:
`stomat_wiki.db` mtime Feb 19 17:48 / md5 2207af764967c505f1bfccdba63f129d,
`stomat_archive.db` mtime Feb 19 17:43 / md5 addae58b925e222e50cf129ce92aafe3 — как в начале сессии.
Боевой `distiller.log` (414874 байт, Jul 28 08:59) тоже не тронут: тест уводит лог в temp через
`STOMCHAT_DISTILLER_LOG`. Никаких `-wal`/`-journal` у вики и архива не появилось.

## Замер конвейера end-to-end (2026-07-29, живые БД, read-only)

| Этап | Метрика | Значение |
|---|---|---|
| 0 | реплик в архиве | 117 847 (msg_id 5..139701, 2023-05-10..2026-02-19) |
| 1 | `is_processed_for_wiki=1` | 117 403 |
| 1 | `is_processed_for_wiki=0` | 444 (все — `has_media=1 AND vision_processed=0`) |
| 1 | кандидатов к дистилляции СЕЙЧАС | **0** |
| 2 | вызовов модели (distiller.log) | 1 731 (200: 1 636, 429: 72, 404: 19, 400: 4) |
| 2 | провалов парсинга JSON | 0 |
| 3 | фактов получено | 12 784 (7.8 на успешный вызов) |
| 4 | уникальных msg_id в `source_ids` | 48 361; есть в архиве 47 832 |
| 4 | **покрытие архива** | **40.59 %** |
| 4 | **никогда не дистиллировано** | **70 015 (59.41 %)**, из них >150 симв. 3 974, с медиа 9 794 |

## Порча

| Что | Замер |
|---|---|
| фактов с `MSG_`-провенансом | 353 (2.76 %), 2 073 битых токена, восстановимо 1 972 id |
| фактов со ссылкой на несуществующий msg_id | 759 (5.94 %), 2 049 ссылок |
| различных `category_code` при 52 легальных | 110; 393 присвоения на 58 самопальных; 51 факт невидим для экспорта |
| столкновений смысла дерево 4.0 vs savdel CAT_MAP | 14 из 34 общих кодов |
| протечка примера промпта в факты | 23 «Методика BOPT», 16 дословных фраз, 40 «коррекции зенитов» |
| точных дублей `content` | 3 группы / 9 строк (6 лишних) — база пока чистая |
| почти-дублей по 120 норм. символам | 10 групп / 24 строки (14 лишних) |
| фактов с обрывом фразы | 1 из 12 784 |
| фактов с символами вне cp1251 | 65 (0.51 %); реплик архива — 10 074 из 107 316 (9.4 %) |
| `media_links` непусто | 0 из 12 784 |
| ложных «клинических» реплик в dental_vocab | 28 из 107 316 |

## Сделано

distiller.py: F1 весь вывод через `say()` + печать вынесена из транзакции; F2 статусы
`no_response/parse_error/salvaged/ok`, повтор до 3 раз, лог `ПОТЕРЯ`/`ПУСТАЯ ПАЧКА` с диапазоном
msg_id и клинической плотностью; F3 `normalize_source_ids` + сверка с id пачки; F4 дерево 5.0
(совпадает с savdel CAT_MAP код-в-код) + `normalize_category`; F5 `reply_to_msg_id` в SELECT и промпте;
F6 детектор протечки промпта; F7 пустые реплики не занимают слот; F10 дедуп по хешу; F11 абсолютные
пути; спасение обрезанного JSON; границы MSG_CHAR_CAP/PROMPT_CHAR_BUDGET/CONTENT_CHAR_CAP с логом;
обрезка по границе предложения; модуль импортируется без побочных эффектов.

dental_vocab.py: F9 `NON_CLINICAL_PREFIXES` + guard в `is_dental_keyword` и `has_dental_term`
(−28 ложных, 0 потерь клиники: 42 219 -> 42 191); мягкий `import config`.

test_distill_pipeline.py: 144 проверки, 12 секций, offline. Саботаж: 29 мутаций, поймано 29/29,
md5 файлов восстановлены, контрольный прогон зелёный.

## Итог прогонов

    python test_distill_pipeline.py  -> PASSED 144  FAILED 0  SKIPPED 0   exit=0
    python test_search_case.py       -> PASSED 31   FAILED 0              exit=0
    регресс: test_dental_vocab 138/0, test_rag_quality 53/0,
             test_digest_window 17/0, test_import_safety 187/0
